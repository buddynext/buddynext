# Possible Bugs — Triage & Fix Plan

Source: Basecamp project 47683682, column **Possible Bug** (9995234561), 87 cards
(snapshot taken during the 1.0.0-beta1 prep). Most cards are auto-generated by a
static-analysis / journey-doc audit pass, so each is **verified at code level
before fixing** (Rule Zero: reports can be wrong).

Verdict legend: ✅ REAL (fix) · ❌ FALSE-POSITIVE (by design / report wrong) ·
🟡 ALREADY-FIXED (not present in current code) · ❓ NEEDS-REPRO/INFO.

Grouped by nature so we fix one seam at a time.

---

## G1 — Search (`includes/Search`, search store.js)
| Card | Title | Verdict | Notes |
|---|---|---|---|
| 10014121731 | reindex_all_sync() WP-Cron fallback only indexes users (LIMIT 500) | | High per card |
| 10014121622 | SearchService::deindex() unchecked $wpdb->delete() return | | |
| 10014121548 | SearchService::index() unchecked $wpdb->query() return | | |
| 10014121812 | SearchController::search() instantiates new SearchService() not container | | Low |
| 10013868535 | Search store.js wrong endpoint paths for space join/leave | | |

## G2 — Bridges (Jetonomy / WPMediaVerse / Gamification, `includes/Bridges`)
| Card | Title | Verdict | Notes |
|---|---|---|---|
| 10013757839 | JetonomyBridge — 9+ direct SQL into partner jt_* tables | | partner tables = by design? |
| 10013756987 | JetonomyBridge — N+1 in @mention parsing | | |
| 10013749808 | JetonomyBridge — CSRF on state-changing GET (maybe_provision_and_redirect) | | |
| 10013757308 | WPMediaVerseBridge — follow re-entrancy guard not try/finally | | |
| 10013757164 | WPMediaVerseBridge — 39 lines dead enqueue_lightbox() | | dead code |
| 10013749653 | WPMediaVerseBridge — missing method_exists guard on is_restricted/is_muted | | |
| 10013756617 | GamificationBridge — register_actions() against NOOP_HOOK never fired | | |
| 10013749964 | GamificationBridgeListener — buddynext_service('notifications') no null guard | | |

## G3 — Race conditions / TOCTOU (credits, webhooks, cron, polls, shares, rate-limit)
| Card | Title | Verdict | Notes |
|---|---|---|---|
| 10013813837 | RoleService add/spend/deduct_credits race | | |
| 10013813955 | OutboundWebhookService::dispatch() wp_next_scheduled race → dup webhooks | | |
| 10013814166 | OnboardingListener nudges wp_next_scheduled race | | |
| 10013739560 | Comment rate-limit transient TOCTOU | | |
| 10013688794 | PollService counter race → inflated votes | | |
| 10013688722 | ShareService TOCTOU → duplicate shares | | |

## G4 — Pro (AI, Labels, Drip, BulkMod) — `buddynext-pro`
| Card | Title | Verdict | Notes |
|---|---|---|---|
| 10013784604 | BulkModService — no capability check on bulk actions | | security |
| 10013785010 | LabelService/LabelAssignmentService — no cap checks on CRUD/assign | | security |
| 10013784901 | DripService::process_due_enrollments() no locking → dup emails | | |
| 10013784763 | DripService::create_sequence() stores steps without validate_step() | | |
| 10013814605 | AI ReplyController::bump_usage() get_user_meta RMW race bypasses daily cap | | |

## G5 — Data integrity (`$wpdb` returns, cache busting, orphans, system-group guard)
| Card | Title | Verdict | Notes |
|---|---|---|---|
| 10013951076 | $wpdb->insert() return not checked in NotificationService::create() | | |
| 10013791325 | System groups deletable via delete_group() (no is_system guard) | | |
| 10013791150 | create_field() auto-created group doesn't bust all_groups cache | | |
| 10013774607 | Space deletion leaves orphaned bn_space_{id}_* options | | |

## G6 — Notifications / email (`includes/Notifications`)
| Card | Title | Verdict | Notes |
|---|---|---|---|
| 10013951360 | buddynext_notification_created inconsistent data merge vs insert | | |
| 10013950941 | Master email channel toggle not respected in send path | | |
| 10013950863 | buddynext_notification_should_send filter bypassed in group merge | | |

## G7 — Profile fields / directory (`includes/Profile`, MemberDirectory)
| Card | Title | Verdict | Notes |
|---|---|---|---|
| 10013790759 | save_profile() silently discards WP_Error from validation | | |
| 10013791670 | create_field() REST missing show_on_register/is_searchable/visibility | | |
| 10013790923 | Repeater field search mirrors never written | | |
| 10013917855 | matching_user_ids() doesn't exclude suspended/shadow-banned | | |
| 10013917760 | online_now() doesn't exclude suspended/shadow-banned/opt-out | | |
| 10013916235 | Headline field hidden on profile view despite editable | | |

## G8 — Spaces / moderation (`includes/Spaces`)
| Card | Title | Verdict | Notes |
|---|---|---|---|
| 10013774705 | SpaceMemberService::ban() fires _removed not _banned event | | |
| 10013774962 | SpaceMemberService::unban() fires wrong event | | |
| 10013932477 | Space notification pref "All activity" → could not save | | |
| 10013774875 | Space moderation Reports tab action buttons inert | | UI |

## G9 — Feed / comments / reactions / shares / polls / connections
| Card | Title | Verdict | Notes |
|---|---|---|---|
| 10013662266 | FollowController::unfollow() missing capability check | | security |
| 10013662982 | ConnectionController::withdraw_request() conflates withdraw/decline | | |
| 10013697659 | Open reactors popover blocks Share/action clicks | | |
| 10013688879 | CommentService soft-delete orphans reply threads | | |
| 10013565266 | Reaction summary chips don't update after toggle | | |
| 10013718859 | ReactionController::list_reactors() N+1 hydration | | perf |
| 10012439940 | "Share to Feed" creates empty activity entry | | |
| 10013569501 | PHP warnings on hashtag feed (object notation on array) | | |

## G10 — Hashtags (`includes/Hashtags`)
| Card | Title | Verdict | Notes |
|---|---|---|---|
| 10013703491 | HashtagService::sync() omits created_at → NULL timestamps | | |
| 10013703291 | HashtagService::get_trending() NOW() vs UTC_TIMESTAMP() | | |

## G11 — DM / messaging
| Card | Title | Verdict | Notes |
|---|---|---|---|
| 10013977576 | Both users delete DM convo → new messages not delivered | | |
| 10013970934 | Member card popup + Delete Conversation modal together | | |
| 10013840441 | Reaction picker on sent messages → horizontal scrollbar | | UI |

## G12 — UI / front-end (templates, CSS)
| Card | Title | Verdict | Notes |
|---|---|---|---|
| 10014128480 | All shortcodes render without hub-shell wrapper — CSS scoping | | |
| 10014140456 | Post button silent no-op: media attached + empty text | | |
| 10014111630 | Dynamically added Work Exp/Education entries unstyled | | |
| 10013918143 | Profile stats-row hover underline / tab hover wrong | | (relates to our nav CSS) |
| 10013782549 | Infinite scroll spinner renders as bordered card box | | |
| 10013929948 | Orphaned media uploads — removing preview doesn't delete server | | |
| 10013934556 | Onboarding Spaces step grid gap 12px vs v2 spec | | |
| 10013932750 | Onboarding Spaces step legacy .bn-card token conflict | | |
| 10013716266 | Poll "ends" date icon invisible in BuddyX dark mode | | |

## G13 — Nav (relates to our just-shipped Nav API)
| Card | Title | Verdict | Notes |
|---|---|---|---|
| 10014014523 | "See all" link in profile navigates to REST endpoint → JSON 401 | | likely our area |

## G14 — Auth / registration
| Card | Title | Verdict | Notes |
|---|---|---|---|
| 10013840323 | [buddynext_auth] shortcode login/register broken — missing enqueue | | |
| 10013787924 | Registration page broken: UI/JS conflict + 'registration not allowed' | | |

## G15 — Theme compat (Reign / BuddyX)
| Card | Title | Verdict | Notes |
|---|---|---|---|
| 10013932484 | Reign Customizer typography font not applied to BN components | | |
| 10013838106 | Reign header notification bell not showing when BN active | | |
| 10013782328 | Reign: gap between header and onboarding content | | |
| 10013626483 | BuddyX global button selector applies accent bg to BN buttons | | |
| 10013634209 | BuddyX/Pro: inject BN user dropdown in header (parity w/ Reign) | | feature, not bug |

## G16 — Docs / journey-doc mismatches (doc edits, not code)
| Card | Title | Verdict | Notes |
|---|---|---|---|
| 10013951215 | Journey doc REST paths missing /me/ prefix for notifications | | |
| 10013917983 | Journey doc says {id} but member-types uses {slug} | | |
| 10013791527 | Journey doc claims profile-fields/groups POST don't exist — they do | | |
| 10013718968 | reactions.md two doc mismatches | | |
| 10013646424 | runbook sidebar.md step 11 checks nonexistent file | | |
| 10013646284 | runbook hashtags.md unfollow is DELETE not POST | | |
| 10013673246 | scheduled-posts.md backdate SQL uses NOW() (non-UTC) | | |

## G17 — NEEDS-INFO (older, pre-tagged; verify still reproducible)
| Card | Title | Verdict | Notes |
|---|---|---|---|
| 10010498716 | Space owner posts sent for moderation when approval enabled | | reactive-moderation rule? |
| 10010425778 | Notification dropdown blank in dark mode | | |
| 10010367439 | Save option not working on Feed/Home | | |
| 10010357621 | No notice + console error when exceeding space create limit | | |
| 10010339386 | Saved posts not appearing in Bookmarks | | |
| 10010123239 | Messages page UI breaks on refresh | | |
| 10004890159 | Share Media action does not work on profile gallery | | |
| 10004784905 | Default avatar not applied after configuration | | |
| 10001372535 | Members grid hover text not visible — not reproduced | | |

---

## Fix order (by confidence + value)
1. **Security caps** (G4 BulkMod/Label, G9 FollowController) — clear, high value.
2. **Data integrity** (G5, G10 hashtag UTC/created_at) — deterministic.
3. **Race/TOCTOU** (G3, G4 drip/AI) — confirm real vs theoretical, fix with locks/atomic.
4. **Functional logic** (G6, G7, G8 events, G9 comment/reaction).
5. **Docs** (G16) — quick doc edits.
6. **UI/theme** (G12, G15) — browser-verify each at 390px + dark.
7. **NEEDS-INFO** (G17) — repro on live site; close stale.

Verdicts filled from parallel code-verification agents; ❌/🟡 cards get a one-line
"why not a bug" note and are moved out of Possible Bug.

---

## Verified verdicts — pass 1 (code-level)

### Search + Bridges
- ✅ 10014121731 P2 — reindex_all_sync() cron fallback only indexes users; route to SearchIndexListener::handle_reindex_all().
- ✅ 10013868535 P2 — search store.js space join/leave hits GET-only `/members`; use `/spaces/{id}/join` POST/DELETE.
- ✅ 10014121622 P3 — SearchService::deindex() check $wpdb->delete() return + log.
- ✅ 10014121548 P3 — SearchService::index() check $wpdb->query() return + log.
- ✅ 10013757308 P3 — WPMediaVerseBridge follow-mirror flag: wrap in try/finally.
- ✅ 10013757164 P4 — delete dead enqueue_lightbox().
- ✅ 10013756987 P4 — JetonomyBridge @mention N+1; batch lookups.
- ❌ 10014121812 — "use container not new" : no decorator mechanism exists; stateless service; style nit.
- ❌ 10013757839 — direct SQL into jt_* is the by-design partner-table pattern (prepared).
- ❌ 10013756617 — NOOP_HOOK registration is a documented intentional pattern.
- ❌ 10013749964 — claimed null-fatal can't occur (buddynext_service throws, never null; service always bound); P4 optional hardening only.

### Profile fields / directory / data
- ✅ 10013917855 P2 — matching_user_ids() leaks suspended/shadow-banned; add NOT EXISTS subqueries (mirror list_members).
- ✅ 10013917760 P2 — online_now() same leak (distinct method).
- ✅ 10013791325 P2 — delete_group() no is_system guard; block system-group delete.
- ✅ 10013951076 P2 — NotificationService::create() unchecked insert; guard + skip action on failure.
- ✅ 10013785010 P2 — Pro LabelService/LabelAssignmentService no cap checks.
- ✅ 10013790759 P3 — save_profile() swallows WP_Error; accumulate + return true|WP_Error.
- ✅ 10013791670 P3 — create_field() REST missing show_on_register/is_searchable/visibility.
- ✅ 10013790923 P3 — repeater searchable sub-fields never written to search mirror.
- ✅ 10013791150 P4 — create_field() auto-group doesn't bust all_groups cache.
- ✅ 10013791527 P4(docs) — profile-fields.md wrongly says POST endpoints don't exist (code right).
- ✅ 10013917983 P4(docs) — member-directory.md says {id}, controller uses {slug}.

### Notifications / email / auth / credits
- ✅ 10013950941 High — master email channel toggle ignored in EmailSender::send().
- ✅ 10013950863 Med-High — should_send filter bypassed in group-merge path.
- ✅ 10013951360 Low — notification_created payload differs merge vs insert (hoist send_at).
   (G6 trio shares ONE root: merge path returns before channel/should_send/send_at logic.)
- ✅ 10013813837 High — RoleService credits read-modify-write race (monetary).
- ✅ 10013814166 Med — onboarding nudge cron race (documented 79-email incident).
- ✅ 10013840323 High — [buddynext_auth] shortcode no asset enqueue (forms dead off-hub); Gutenberg half NEEDS-REPRO.
- ✅ 10013787924 High — registration: bn-base.css bleeds onto wp-login + buddynext_reg_mode never syncs core users_can_register.
- ✅ 10013814605 Low — Pro AI bump_usage() quota race (off-by-one).
- ✅ 10013951215 Low(docs) — notifications-email.md REST paths missing /me/ prefix.
- 🟡/by-design 10013813955 Low — webhook dispatch cron race; author accepts (consumer dedup header exists).

Pattern: the static-audit cards are mostly ACCURATE (real), but the "[Free] X — unchecked/N+1/direct-SQL" style yields the false-positives (by-design partner SQL, fictional mechanisms).

### Spaces / moderation / Pro
- ✅ 10013791325 P2(HIGH, data loss) — delete_group() no is_system guard (admin DELETE destroys system group + all values).
- ✅ 10013774705 P3 — SpaceMemberService::ban() (live via settings.php) fires buddynext_space_member_removed not _space_user_banned.
- ✅ 10013932477 P3 — notification pref "All activity" → false "Could not save" ($wpdb->update returns 0 on unchanged value; treat 0 as success).
- ✅ 10013784901 P3 — DripService::process_due_enrollments() no locking → dup emails; add WHERE current_step guard + require rows_affected===1.
- ✅ 10013951076 P3 — NotificationService::create() unchecked insert (dup of G5).
- ✅ 10013774607 P4 — space deletion leaves orphaned bn_space_{id}_* options.
- ✅ 10013782549 P4(cosmetic) — infinite-scroll spinner uses .bn-skeleton (bordered box); drop the class.
- ✅ 10013774962 P4 — unban() fires wrong hook BUT method is dead code (REST uses unban_from_space); latent only.
- ❌ 10013784763 — create_sequence() XSS: no caller passes `steps`; steps go via validate_step(). Defense-in-depth only.
- ❌ 10013784604 — BulkModService caps: both entry points already gate manage_options. Defense-in-depth only.
- 🟡 10013774875 — moderation Reports buttons: already wired (data-wp-interactive + module enqueue present).

---

## MOVED TO DONE (pass 1 — not reproducible / already fixed)
Per "issues we can't replicate → Done" — commented with reasoning, moved 2026-06-19:
10014121812, 10013757839, 10013756617, 10013749964 (Search/Bridge false-positives),
10013784763, 10013784604 (Pro caller-gated, not exploitable), 10013774875 (already fixed).

## MOVED TO DONE (pass 2 — code-verified not reproducible)
10013970934 (member-card popup mechanism absent), 10010339386 (already fixed d0092e6),
10010367439 (save wired end-to-end), 10004890159 (gallery share works in lightbox),
10001372535 (reporter "not reproduced" x2), 10010425778 (dropdown is dark-tokenized),
10010123239 (messages refresh restores server-side), 10004784905 (avatar contract correct).
→ Suggestions: 10013634209 (BuddyX dropdown = feature, not bug).

## NOT BN-FIXABLE — flag to other repos/teams (do NOT fix in BN)
- 10013977576 — DM "messages not delivered after both delete" = upstream **wpmediaverse** MessagingService (BN is a pure consumer).
- 10013838106 — Reign notification bell stripped when BN-active-without-BP = **reign-theme** (Header_Fields else-branch).
- 10013932484 RC1/RC2 — Reign typography presets not emitted = **reign-theme**; RC4 (BN tokens) IS ours.
- 10013626483 / 10013782328 — **BuddyX** ships no BN compat layer (button bg bleed + onboarding gap); BN-side mitigations possible (bn-app button bg reset; .bn-ob-wrap top reset).

## STILL NEEDS-REPRO (kept; not code-decidable)
- 10012439940 — Share-to-feed only prefills @mention (no profile card); UX-expectation gap.
- 10010498716 — per-space approval is gone (obsolete) BUT site-wide buddynext_premod_mode hold has no space-owner exemption — policy review vs "reactive moderation only".

---

REMAINING REAL — BN-fixable (to fix, by priority):
- **P2/High:** 10013791325 (delete_group system guard), 10013917855 + 10013917760 (directory leaks), 10013951076 (notif insert), 10013785010 (Pro Label caps), 10013950941 (master email toggle), 10013840323 + 10013787924 (auth/registration), 10013813837 (credits race), 10014121731 + 10013868535 (search reindex + endpoint).
- **P3:** 10013950863, 10013951360, 10013790759, 10013791670, 10013790923, 10013774705, 10013932477, 10013784901, 10013757308, 10013814166.
- **P4 / cosmetic / docs:** 10013791150, 10013757164, 10013756987, 10013774607, 10013782549, 10013774962, 10013814605, 10013951215, 10013791527, 10013917983, 10013813955(by-design).

---

## FIX-WAVE PROGRESS (commit log)

Batches 1–4 + Waves shipped to master in verified increments (lint → full PHPUnit
green → commit → push). Each fix verified at code level (no guesswork).

- **Batch 1** (43b0776): delete_group is_system guard (10013791325), notif merge/insert
  guards (10013951076 + G6 root), FollowController unfollow cap (10013662266).
- **Batch 2** (5af201c): directory privacy leaks matching_user_ids + online_now
  (10013917855, 10013917760), SearchService index/deindex write checks
  (10014121622, 10014121548).
- **Batch 3** (86ba31f): PollService + ShareService counter races (10013688794, 10013688722).
- **Batch 4** (6445fa1): master email toggle (10013950941), space-pref false-on-unchanged
  (10013932477). + Confirmed G6 trio (10013950863, 10013951360) already closed by
  Batch 1's gate-hoist above the merge.
- **Wave 5** (b33efc1): save_profile() true|WP_Error honesty (10013790759), create_field/
  update_field persist show_on_register + is_searchable + field_updated hook
  (10013791670), repeater searchable mirror aggregation (10013790923). +4 tests.
- **Wave 6** (PENDING commit): hashtag created_at UTC in both sync() inserts +
  get_trending() UTC_TIMESTAMP() window (10013703491, 10013703291). +1 test.
- **Wave 7** (PENDING commit): hashtag feed.php object-notation-on-array PHP warnings
  (10013569501), CommentService soft-delete orphan — keep deleted top-level tombstone
  only when it has a surviving reply (10013688879), list_reactors N+1 → cache_users()
  (10013718859).

### New FALSE-POSITIVE verdicts (move to Done)
- 10013662982 — withdraw/decline "conflation": VERIFIED separated across all layers.
  Service: decline_request() sets status=declined (recipient); withdraw_request()
  deletes pending outgoing (requester) + controller falls back to remove_connection
  (accepted/disconnect). Routes: POST /connect/decline vs DELETE /connect. JS: blocks.js,
  follow-store.js (buddynext/connection-requests.decline → /connect/decline),
  members/store.js (declineConnection → /connect/decline) all distinct. Not a bug.

### Moved to browser-verified UI wave (Wave 12)
- 10013916235 — headline editable but rendered nowhere (hero intentionally drops it +
  detail renderer skips it). Fix = LinkedIn-style tagline; needs 390px + dark verify.
- 10013565266 — reaction summary chips don't update after toggle (reactive JS state).
