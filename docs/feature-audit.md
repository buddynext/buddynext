# BuddyNext — Feature Audit: Plan vs Owner-Expectation vs Reality

_Generated 2026-05-30 · 44 features · one agent each · desk review (docs + code)._

**Status legend:** 🟢 shipped (plan met) · 🟡 partial (core there, gaps) · 🟠 stub (scaffolded only) · 🔴 missing.  
**Confidence:** `confirmed-in-code` = read & proven · `needs-live-verification` = looks absent but may be runtime-wired (don't trust without a walk).

## Totals

- Features: **44** (27 Free, 17 Pro)
- Status: 🟢 shipped **11** · 🟡 partial **33**
- Gaps: **250** total — critical **14**, high **57**, medium **92**, low **87**
- Confidence: confirmed-in-code **211** · needs-live-verification **39**

## Scoreboard

| Feature | Repo | Status | Gaps (C/H/M/L) | Worst | Headline |
|---|---|---|---|---|---|
| Bookmarks | free | 🟡 partial | 1/1/0/1 | critical | Bookmarks is fully built end-to-end (service, REST, post-card toggle, hub) but the saved-posts hub query depends on a… |
| PWA | free | 🟡 partial | 1/2/2/1 | critical | PWA is fully wired at runtime (manifest + SW + 2 REST routes, booted via Plugin.php) but is shipped-broken: the manif… |
| Engagement / Leaderboard | free | 🟡 partial | 2/2/3/1 | critical | Leaderboard UX is fully built and routed, but it is end-to-end non-functional: it queries WBGamification tables/colum… |
| Post Composer & Posts | free | 🟡 partial | 0/1/3/2 | high | Robust post backend (CRUD, polls, mentions, content-warnings, scheduling, privacy gates) but the composer UI under-wi… |
| Comments | free | 🟡 partial | 0/2/4/2 | high | Comments core is fully shipped and UX-wired (CRUD, react, reply, edit, pin, report, notifications), but @mentions are… |
| Polls | free | 🟡 partial | 0/1/2/2 | high | Core inline poll create/vote/results flow is fully shipped and wired end-to-end; gaps are a UI-capped 5th option, a d… |
| Shares / Reposts | free | 🟡 partial | 0/1/2/2 | high | Repost is shipped end-to-end (REST, table, modal, feed card, notification) but is one-way: no unshare in the UI, no p… |
| Member Profiles | free | 🟢 shipped | 0/1/2/3 | high | Member Profiles is genuinely shipped and mature — full custom-table model, data-driven field engine, server-side visi… |
| Member Types | free | 🟡 partial | 0/1/1/2 | high | Member Types is shipped and fully wired (tables, service, 7 REST routes, admin CRUD + per-member assignment, director… |
| Follows | free | 🟡 partial | 0/1/0/2 | high | Follows backend and primary UX surfaces are production-grade and fully wired; the only real gap is two Gutenberg bloc… |
| Blocking & Muting | free | 🟢 shipped | 0/1/1/3 | high | Blocking & Muting is shipped and genuinely wired end-to-end (service, REST, broad block enforcement, action buttons, … |
| Email System | free | 🟡 partial | 0/1/2/2 | high | Email pipeline is genuinely shipped and wired (immediate async send, prefs, cron digests, unsubscribe, 21 seeded temp… |
| Moderation | free | 🟡 partial | 0/2/2/2 | high | Moderation is broadly shipped end-to-end (service + full REST + routed front-end queue + report modals + safeguards +… |
| Authentication & Verification | free | 🟢 shipped | 0/1/2/2 | high | Auth + verification is shipped end-to-end (services, 7 REST routes, wired Interactivity templates, schema, cron) — bu… |
| Onboarding | free | 🟡 partial | 0/2/3/3 | high | Both wizards and the invite tool are genuinely built and runtime-wired, but the bulk-invite half is hollow: invite li… |
| Search | free | 🟡 partial | 0/2/4/2 | high | Search ships a working FULLTEXT index, public REST endpoint, and a full v2 results UI — but the UI and the REST/servi… |
| Outbound Webhooks | free | 🟡 partial | 0/1/2/3 | high | Backend and admin CRUD shipped per BLOCK 7, but the UI only exposes 5 of 14 dispatchable events (one of which is neve… |
| Realtime Updates | free | 🟡 partial | 0/2/2/1 | high | Free realtime is one polished surface (notification badge polling) plus a Pro-only transport seam; three of the four … |
| Privacy Framework | free | 🟡 partial | 0/3/2/1 | high | Field-level field privacy is fully shipped and wired; the social-graph privacy preferences (who_can_follow/connect, p… |
| Private Messaging (planned) | free | 🟡 partial | 0/2/4/0 | high | Messaging ships as a WPMediaVerse UI bridge exactly as the plan scoped it (no BN tables, no BN message REST), but BN'… |
| Activity Feed | free | 🟢 shipped | 0/0/2/3 | medium | Activity Feed is fully shipped end-to-end (REST + service + templates + Interactivity-wired UX); remaining gaps are a… |
| Reactions | free | 🟢 shipped | 0/0/3/2 | medium | Reactions is shipped and genuinely wired end-to-end (service, REST, post + comment + DM UX, notifications, gamificati… |
| Hashtags | free | 🟢 shipped | 0/0/2/3 | medium | Hashtags is fully shipped and UX-wired (extraction, autolinking, per-tag page with sort tabs, follow, trending widget… |
| Member Directory | free | 🟡 partial | 0/0/3/3 | medium | Core directory shipped and well-wired (reactive grid, search, sorts, relation/type filters, viewer-aware exclusions, … |
| Connections | free | 🟢 shipped | 0/0/2/4 | medium | Connections is shipped end-to-end (service + REST + wired Interactivity UI); gaps are polish/parity edges — unwired c… |
| Spaces (Communities) | free | 🟢 shipped | 0/0/2/2 | medium | Spaces is shipped end-to-end (services, REST, routing, templates, Interactivity wiring, admin, schema); the only real… |
| Notifications | free | 🟢 shipped | 0/0/2/4 | medium | Notifications is a genuinely shipped, end-to-end feature (triggers, aggregation, REST, prefs, digests, admin template… |
| Email Broadcasts | pro | 🟡 partial | 1/3/1/1 | critical | Broadcast scaffolding is fully built and wired, but the actual send path delivers nothing — recipients are marked "se… |
| Drip / Welcome Sequences | pro | 🟡 partial | 2/1/2/2 | critical | Fully scaffolded and admin-wired, but the core promise breaks: drip step emails almost certainly never send (no bn.dr… |
| Membership Tiers & Gated Spaces | pro | 🟡 partial | 1/2/1/1 | critical | Backend, gating, webhook sync, and admin are real and wired; the member-facing half — paywall rendering, front-end ch… |
| Stripe Payments | pro | 🟡 partial | 1/2/2/1 | critical | Stripe backend (SDK, webhook sync, checkout/portal REST, admin keys + tier price linker) is built, tested, and wired … |
| Advanced Profile Field Types | pro | 🟡 partial | 1/2/2/1 | critical | Backend types, validation, and the admin option UI are genuinely wired to live Free seams — but the admin config neve… |
| Custom Reactions | pro | 🟡 partial | 1/2/1/1 | critical | Admin CRUD + filter merge are fully built and wired, but custom reactions are invisible end-to-end: the front-end pic… |
| Advanced Search Filters | pro | 🟡 partial | 3/1/0/2 | critical | Backend is built and wired (table, CRUD, REST, Free SQL seam) but the feature is dead end-to-end: no UI, the five fil… |
| AI (ranking, embeddings, semantic search, replies) | pro | 🟡 partial | 0/1/3/3 | high | All four P2 AI features are genuinely wired end-to-end (services, REST, admin, Interactivity island) — not stubs — bu… |
| Push Notifications | pro | 🟡 partial | 0/3/1/1 | high | Server-side FCM push is fully built and wired, but the member-facing delivery loop is incomplete: wrong body copy for… |
| Bulk Moderation | pro | 🟡 partial | 0/1/2/4 | high | Core bulk moderation is built and wired (admin page + REST + JS + real Free service calls), but the 'Remove Content' … |
| Auto / Rule-based Moderation | pro | 🟡 partial | 0/2/4/2 | high | Core rule engine, REST CRUD, admin UI, DB table and Free hook-wiring are all shipped and genuinely wired — but the jo… |
| Member Labels | pro | 🟡 partial | 0/2/3/1 | high | Backend is complete and correctly wired (CRUD, REST, admin label management, DB, data injection); the feature is half… |
| Scheduled Posts | pro | 🟡 partial | 0/1/2/2 | high | Backend, REST, cron, admin page, and Free-side notification/feed guards are all shipped and wired; the gap is the mis… |
| Multi-pin Posts | pro | 🟢 shipped | 0/1/1/2 | high | Shipped as planned: a clean filter-only Pro extension that raises the pin cap to 10, fully wired and tested in code —… |
| White Label | pro | 🟡 partial | 0/2/3/1 | high | Hue/font/custom-CSS white-label and admin relabeling are shipped and fully wired, but the logo never renders on the f… |
| Unlimited Webhooks | pro | 🟡 partial | 0/1/1/2 | high | Cap-removal half is genuinely shipped and wired; the promised Pro-event catalog wiring is missing, so Pro events fire… |
| Analytics | pro | 🟢 shipped | 0/0/4/2 | medium | Analytics is shipped and wired end-to-end (collector, service, 6 REST endpoints, charted 4-view admin dashboard, memb… |

## Priority gaps (critical + high)

| Sev | Feature | Gap | Kind | Confidence | Evidence |
|---|---|---|---|---|---|
| **critical** | Bookmarks | The bookmarks hub query is broken against the shipped schema. templates/feed/bookmarks.php (lines 64, 68, 70, 81-83) selects 'b.id AS bookmark_id', orders by 'b.id DESC', and bu… | both | `confirmed-in-code` | includes/Core/Installer.php:420-425 (no id column) vs templates/feed/bookmarks.php:64,6… |
| **critical** | PWA | Manifest references icon assets that do not exist. get_manifest() (PwaService.php:122-130) points icons at plugins_url('assets/images/icon-192.png', ...) and 'assets/images/icon… | expectation-vs-reality | `confirmed-in-code` | includes/PWA/PwaService.php:120-131; assets/ dir listing (no images/ subdir, no icon PN… |
| **critical** | Engagement / Leaderboard | The leaderboard template queries WBGamification tables/columns that do not exist. Template uses {prefix}wbg_user_points, {prefix}wbg_user_badges, {prefix}wbg_badges with columns… | both | `confirmed-in-code` | templates/gamification/leaderboard.php lines ~69-71 (wbg_* table names, awarded_at, eve… |
| **critical** | Engagement / Leaderboard | The outbound bridge fires do_action('wb_gamification_event', $type, $user, $ctx) but WBGamification never listens to that action. WBGam ingests events only through wb_gamificati… | both | `confirmed-in-code` | includes/Bridges/GamificationBridge.php:141-150 (fire() -> do_action('wb_gamification_e… |
| **critical** | Email Broadcasts | Broadcasts do not deliver the campaign's content. send_pending() (BroadcastService.php line 450-457) calls EmailSender::send_now($user_id, 'bn.broadcast', ['campaign_id'=>..., '… | both | `confirmed-in-code` | /Users/vapvarun/dev/repos/buddynext-pro/includes/Email/BroadcastService.php lines 447-4… |
| **critical** | Drip / Welcome Sequences | Drip step emails almost certainly never send on a default install. DripService::process_due_enrollments() calls $sender->send_now($user_id,'bn.drip_step',$data) (DripService.php… | both | `confirmed-in-code` | /Users/vapvarun/dev/repos/buddynext-pro/includes/Email/DripService.php:317; /Users/vapv… |
| **critical** | Drip / Welcome Sequences | Even if a 'bn.drip_step' template existed, the per-step subject/body_html would be ignored unless the template body literally contains {{subject}}/{{body_html}} tokens. send_now… | expectation-vs-reality | `confirmed-in-code` | /Users/vapvarun/dev/repos/buddynext/includes/Notifications/EmailSender.php:119-120,187-… |
| **critical** | Membership Tiers & Gated Spaces | PaywallRenderer is never instantiated anywhere in the codebase. The gated-join denial in Free's SpaceMemberService returns a generic WP_Error ('You cannot join this space.', Spa… | both | `confirmed-in-code` | grep for 'new PaywallRenderer'/'PaywallRenderer(' returns zero callers; includes/Member… |
| **critical** | Stripe Payments | The member-facing checkout flow is not wired to the API. The only front-end paywall surface (PaywallRenderer) outputs a static <a> whose href is an admin-typed URL; no template,… | both | `confirmed-in-code` | includes/Membership/PaywallRenderer.php (lines 71-75, static cta_url); only assets/admi… |
| **critical** | Advanced Profile Field Types | Admin per-type option inputs are never persisted. The Pro admin renders inputs named bn_field_options[allowed_mime/max_size_mb/unit/min/max/step/trigger_field_id/trigger_value/c… | both | `confirmed-in-code` | buddynext/includes/Admin/Members/ProfileFieldsManager.php:421-430 (only $_POST['options… |
| **critical** | Custom Reactions | Custom reactions never appear in the front-end reaction picker. The picker template hardcodes the 6 built-in reactions in a local $reaction_labels array (post-actions.php lines … | both | `confirmed-in-code` | /Users/vapvarun/dev/repos/buddynext/templates/parts/post-actions.php lines 115-132 (har… |
| **critical** | Advanced Search Filters | No member-facing UI for advanced search filters or saved searches. Pro repo has no templates/ directory and no search-related JS; the only JS is admin/realtime/ai-reply/profile-… | both | `confirmed-in-code` | /Users/vapvarun/dev/repos/buddynext-pro/ (no templates/ dir; find for search JS returns… |
| **critical** | Advanced Search Filters | Advanced filter args are dead at runtime. The five args (tier_slug, space_id, member_label, joined_after, active_within_days) are honored by Free's SQL builder but are never sou… | both | `confirmed-in-code` | /Users/vapvarun/dev/repos/buddynext-pro/includes/Search/AdvancedSearchFilters.php:89-13… |
| **critical** | Advanced Search Filters | SavedSearchService::run_search() does not apply the saved filters. It extracts only query/type/per_page/page from the stored query_args and calls SearchService::search(); it nev… | both | `confirmed-in-code` | /Users/vapvarun/dev/repos/buddynext-pro/includes/Search/SavedSearchService.php:239-251 |
| **high** | Post Composer & Posts | Edit-post UX is a dead redirect. store.js editPost() (L897-901) navigates to '/activity/?edit='+postId, but PageRouter.php has no route honoring an ?edit= param for posts (its o… | both | `confirmed-in-code` | assets/js/feed/store.js editPost L897-901; includes/Core/PageRouter.php L795-823 (no po… |
| **high** | Comments | @mentions in comments are not parsed. Mention parsing + buddynext_user_mentioned only fires in includes/Feed/PostService.php (lines 169-184); the comment create path in CommentS… | both | `confirmed-in-code` | includes/Comments/CommentService.php (create, no mention parse); includes/Feed/PostServ… |
| **high** | Comments | Privacy inheritance not enforced on the comment create path. Spec: 'Inherits parent object's privacy. Can't comment on content you can't see.' CommentService::create() inserts o… | both | `confirmed-in-code` | includes/Comments/CommentService.php:48-120 (create, no visibility check); includes/Com… |
| **high** | Polls | The 'Allow polls' admin toggle (buddynext_allow_polls) is registered and rendered but never read or enforced anywhere — PostService::create() and the composer do not check it. D… | expectation-vs-reality | `confirmed-in-code` | grep of buddynext_allow_polls returns only Settings.php:49,693-696; no read in includes… |
| **high** | Shares / Reposts | No unshare/undo in the UI. The DELETE /posts/{id}/share endpoint exists in ShareController but no JS code path calls it — grep for unshare/DELETE-share in assets/js/feed/store.j… | expectation-vs-reality | `confirmed-in-code` | /Users/vapvarun/dev/repos/buddynext/includes/Feed/ShareController.php (DELETE registere… |
| **high** | Member Profiles | Required-field enforcement is incomplete. ProfileController::validate_profile_payload (lines 657-696) only validates display_name, a hardcoded list of URL fields, and audience e… | both | `confirmed-in-code` | includes/Profile/ProfileController.php:657-696 (validate_profile_payload), includes/Pro… |
| **high** | Member Types | Plan promised 'Profile field groups can be scoped to a specific type.' The type_restriction column exists in the schema and is cleared on type-delete in MemberTypeService::delet… | both | `confirmed-in-code` | grep across /Users/vapvarun/dev/repos/buddynext returns type_restriction only in includ… |
| **high** | Follows | The Gutenberg block templates templates/blocks/member-card.php (line 78) and templates/blocks/profile-header.php (line 99) render their Follow button with only data-action="bn-t… | both | `confirmed-in-code` | templates/blocks/member-card.php:78, templates/blocks/profile-header.php:94-106, assets… |
| **high** | Email System | Token mismatch between the admin Email Template Editor catalogue and the runtime renderer. EmailEditor::get_catalogue() (includes/Admin/EmailEditor.php:91-252) advertises and se… | both | `confirmed-in-code` | includes/Admin/EmailEditor.php:97-100 (catalogue tokens) vs includes/Notifications/Emai… |
| **high** | Moderation | Suspension is NOT enforced on the posting path. The journey (Step 14) promises a suspended user gets 403 when calling POST /posts, but PostController::create_post (includes/Feed… | both | `confirmed-in-code` | includes/Feed/PostController.php:94, includes/Feed/PostService.php:70, includes/Moderat… |
| **high** | Moderation | Appeal API has duplicated, divergent paths that don't match the journey contract. The journey uses POST /me/appeals with a suspension_id body and expects the suspension to be li… | plan-vs-reality | `confirmed-in-code` | includes/Moderation/ModerationController.php:1264; includes/Moderation/ModerationServic… |
| **high** | Authentication & Verification | No enforcement gate. is_verified() exists but is never called to restrict any action — grep shows no buddynext_can('post')/comment check or any guard consulting buddynext_email_… | both | `confirmed-in-code` | includes/Core/FeatureRegistry.php:174 + includes/Admin/Settings.php:627 promise gating;… |
| **high** | Onboarding | Invite status never transitions past 'pending'. InviteService::mark_registered() and mark_bounced() have ZERO callers anywhere in includes/ (grep returns only the function defin… | both | `confirmed-in-code` | includes/Onboarding/InviteService.php:103-115 (mark_registered/mark_bounced defined); g… |
| **high** | Onboarding | Invite links are non-functional on the registration flow. The plan requires the invite token to pre-fill the email field and bypass the invite-only gate. AuthController.php has … | both | `confirmed-in-code` | grep for bn_invite/get_by_token/InviteService in includes/Auth/ returns nothing; templa… |
| **high** | Search | The admin reindex REST endpoint POST /buddynext/v1/search/index/{type} and SearchController::reindex() promised in the journey do not exist. SearchController only registers /sea… | both | `confirmed-in-code` | includes/Search/SearchController.php (only /search and /members registered); grep for '… |
| **high** | Search | The user-facing search page (templates/search/results.php) bypasses SearchService entirely and runs its own raw SQL with a DIFFERENT exclusion model. SearchService excludes susp… | expectation-vs-reality | `confirmed-in-code` | templates/search/results.php:94-115 (usermeta bn_suspended/bn_shadow_banned + bn_blocks… |
| **high** | Outbound Webhooks | Auto-generated signing secret is unrecoverable from the UI. POST /webhooks returns the generated secret in the 201 body, but settings.js add handler (assets/js/admin/settings.js… | expectation-vs-reality | `confirmed-in-code` | assets/js/admin/settings.js lines 111-122; includes/Admin/Settings.php lines 1232-1290;… |
| **high** | PWA | No web push notifications and no background sync. The SW (PwaService.php:166-214) registers only install/activate/fetch handlers — no 'push' or 'notificationclick' listener, no … | expectation-vs-reality | `confirmed-in-code` | includes/PWA/PwaService.php:166-214 (no push/notificationclick handlers); no other PWA … |
| **high** | PWA | Feature violates the master plan's own 'no partial implementations' standard. A booted, wired PWA whose manifest points at 404 icons is shipped-but-broken at the install step — … | plan-vs-reality | `confirmed-in-code` | docs/MASTER_DEVELOPMENT_PLAN.md:104 ('No partial implementations'); includes/PWA/PwaSer… |
| **high** | Realtime Updates | Spec promises Free DM conversations poll every 5s, but BuddyNext ships no DM polling — assets/js/messages/store.js is a 9-line stub that hands all messaging to WPMediaVerse. The… | both | `confirmed-in-code` | assets/js/messages/store.js (lines 1-9, intentionally empty); P3-realtime-websocket.md … |
| **high** | Realtime Updates | Spec promises a Free feed new-posts bar polling every 60s, but the feed pill (assets/js/feed/store.js:2374-2423) only listens for the Pro-dispatched bn:realtime:post-new event a… | both | `confirmed-in-code` | assets/js/feed/store.js:2366-2423 (comment 'No-op when no realtime layer is active (the… |
| **high** | Engagement / Leaderboard | Template hand-rolls ranking SQL instead of using WBGam's own LeaderboardEngine / LeaderboardController REST API and wb_gam_leaderboard_cache snapshot table. This bypasses the si… | expectation-vs-reality | `confirmed-in-code` | templates/gamification/leaderboard.php (raw $wpdb->get_results SUM/GROUP BY queries) vs… |
| **high** | Engagement / Leaderboard | Leaderboard opt-out / privacy is not honored. WBGam stores wb_gam_member_prefs.leaderboard_opt_out and its own LeaderboardEngine filters opted-out users; the BuddyNext template … | expectation-vs-reality | `confirmed-in-code` | /Users/vapvarun/dev/repos/wb-gamification/src/Engine/Installer.php:177-186 (wb_gam_memb… |
| **high** | Privacy Framework | PrivacyService's three core preference keys (who_can_follow, who_can_connect, profile_visibility) have NO UI to set them and NO save path. The edit-profile form (edit.php) and P… | both | `confirmed-in-code` | includes/SocialGraph/PrivacyService.php lines 37-39 vs grep of includes/ + templates/ f… |
| **high** | Privacy Framework | profile_visibility is read by view.php->can_view_profile() to decide 'This profile is private', but since no UI ever writes profile_visibility, it is permanently 'public'. The w… | expectation-vs-reality | `confirmed-in-code` | templates/profile/view.php line 34 calls can_view_profile(); PrivacyService.php line 18… |
| **high** | Privacy Framework | GDPR/data-rights is admin-settings-only with no backend. The 'Allow members to export their data' / 'Allow members to delete their account' / 'Anonymise on delete' toggles (Sett… | both | `confirmed-in-code` | grep of includes/ finds register_personal_data / wp_privacy / personal_data_export = 0 … |
| **high** | Private Messaging (planned) | Deep-link to a conversation is broken: PageRouter maps /messages/{id}/ to the query var bn_conv_id (register_messages_rules, includes/Core/PageRouter.php ~line 1108), but templa… | expectation-vs-reality | `confirmed-in-code` | includes/Core/PageRouter.php (bn_conv_id, lines ~1108 and ~844); templates/messages/thr… |
| **high** | Private Messaging (planned) | Without the WPMediaVerse plugin active, BuddyNext Free has zero messaging: list.php shows a 'Dependency required' notice (templates/messages/list.php lines 42-53) and thread.php… | expectation-vs-reality | `confirmed-in-code` | templates/messages/list.php lines 29-53; templates/messages/thread.php line 31; docs/MA… |
| **high** | AI (ranking, embeddings, semantic search, replies) | Semantic search has NO backfill/reindex path for pre-existing content. EmbeddingIndexer only embeds on buddynext_post_created going forward; there is no admin 'reindex' button a… | both | `confirmed-in-code` | includes/AI/EmbeddingIndexer.php:66-104 (only post_created/deleted + tooling hook); gre… |
| **high** | Email Broadcasts | No per-campaign unsubscribe link is injected into sent broadcasts. BroadcastUnsubscribe::unsub_url() exists but is never called from the send path; no add_filter('buddynext_emai… | both | `confirmed-in-code` | /Users/vapvarun/dev/repos/buddynext-pro/includes/Email/BroadcastUnsubscribe.php line 11… |
| **high** | Email Broadcasts | Admin segment selector only sets a bare {type:...} with no parameters; by_space/by_tag/by_join_date/by_member_label all require space_ids/tags/dates/label_ids which the admin fo… | expectation-vs-reality | `confirmed-in-code` | /Users/vapvarun/dev/repos/buddynext-pro/includes/Admin/BroadcastAdmin.php lines 132-133… |
| **high** | Email Broadcasts | Scheduling is recorded but never auto-dispatched. create_campaign() sets status='scheduled' with scheduled_at, and BroadcastAdmin exposes a schedule field, but no cron/handler s… | expectation-vs-reality | `confirmed-in-code` | /Users/vapvarun/dev/repos/buddynext-pro/includes/Email/BroadcastService.php lines 121-1… |
| **high** | Drip / Welcome Sequences | Delay model contradicts the plan and the code's own docblock. Journey (drip-welcome.md lines 110-142) and DripService docblock (lines 264-266) describe delays measured from last… | plan-vs-reality | `confirmed-in-code` | /Users/vapvarun/dev/repos/buddynext-pro/includes/Email/DripService.php:308-309,341; /Us… |
| **high** | Membership Tiers & Gated Spaces | No front-end member-facing UI for the feature. The Pro plugin ships no templates/ directory; no template, block, or JS calls POST /me/checkout, lists tiers, or renders an upgrad… | both | `confirmed-in-code` | templates/ and assets/ greps for 'me/checkout','tier_slug','upgrade' return nothing; Pr… |
| **high** | Membership Tiers & Gated Spaces | No admin UI to set a space's required_ability. Gating a space requires direct SQL / WP-CLI; the Paywall Settings page only lists already-gated spaces read-only and cannot assign… | both | `confirmed-in-code` | docs/journeys/membership-gated-spaces.md step 4 'only settable via direct SQL or WP-CLI… |
| **high** | Stripe Payments | No member-facing billing portal entry point. POST /me/portal exists and is tested, but no front-end template/button lets a paying member open the Stripe Billing Portal to manage… | both | `confirmed-in-code` | includes/Membership/Controllers/CheckoutController.php (handle_portal) has no front-end… |
| **high** | Push Notifications | Notification body copy mismatch: the dispatcher's build_snippet() switch matches 'follower'/'bn.follower', 'reaction'/'bn.reaction', 'comment'/'bn.comment', but Free actually em… | expectation-vs-reality | `confirmed-in-code` | /Users/vapvarun/dev/repos/buddynext-pro/includes/Push/PushDispatcher.php lines 159-199 … |
| **high** | Push Notifications | Admin 'Send test push' button is dead — its data-bn-push-test attribute has no JS handler anywhere. assets/admin/ contains realtime-test.js and tier-stripe-test.js but no push-t… | both | `confirmed-in-code` | /Users/vapvarun/dev/repos/buddynext-pro/includes/Admin/PushAdmin.php lines 242-245; ass… |
| **high** | Push Notifications | No web-push delivery path despite admin UI advertising 'web, Android, and iOS' (PushAdmin subtitle + 'web' platform enum). There is no VAPID key, no service worker, and no clien… | expectation-vs-reality | `confirmed-in-code` | /Users/vapvarun/dev/repos/buddynext-pro/includes/Admin/PushAdmin.php line 163 ('web, An… |
| **high** | Bulk Moderation | 'Remove Content' does not remove content. BulkModService::bulk_remove() calls Free's resolve() (BulkModService.php:55), which only set_status() to 'resolved' (ModerationService.… | both | `confirmed-in-code` | /Users/vapvarun/dev/repos/buddynext-pro/includes/Moderation/BulkModService.php:54-56; /… |
| **high** | Auto / Rule-based Moderation | The journey's central flag-severity behavior is contradicted by the code. Journey Part 3 (steps 7-9) promises severity=flag ALLOWS the post (HTTP 200/201) and auto-creates a pen… | both | `confirmed-in-code` | buddynext-pro/includes/Moderation/RulesService.php:441-445,494-498 + buddynext/includes… |
| **high** | Auto / Rule-based Moderation | threshold_remove 'suspend' action targets the wrong ID. evaluate_post_report sets user_id = report['object_id'] (RulesService.php:335-337), but object_id for a content report is… | expectation-vs-reality | `confirmed-in-code` | buddynext-pro/includes/Moderation/RulesService.php:335-339 |
| **high** | Member Labels | Labels are not rendered on the member profile. ProfileLabelInjector::inject_labels() writes $extra['labels'] as a {slug,name,color,icon} array onto the buddynext_profile_extra_d… | both | `confirmed-in-code` | includes/Members/ProfileLabelInjector.php:134-153 (writes to buddynext_profile_extra_da… |
| **high** | Member Labels | No UI to assign a label to a member. Assignment exists only via the REST endpoints POST/DELETE /users/{id}/labels/{slug} (manage_options). The admin page (MemberLabelsAdmin) doe… | expectation-vs-reality | `confirmed-in-code` | includes/Admin/MemberLabelsAdmin.php (only add/edit/delete label handlers; no assignmen… |
| **high** | Advanced Profile Field Types | No client-side JS/CSS ships for any advanced type. Renderer emits a .bn-location-map-placeholder, a .bn-location-search input, a conditional wrapper with data-trigger-field/data… | both | `confirmed-in-code` | buddynext-pro/includes/Profile/AdvancedFieldRenderer.php:228-372; grep of buddynext-pro… |
| **high** | Advanced Profile Field Types | File upload type is non-functional end-to-end. Renderer outputs <input type=file name="{name}_upload"> but no handler exists: grep for wp_handle_upload/media_handle_upload/_FILE… | both | `confirmed-in-code` | buddynext-pro/includes/Profile/AdvancedFieldRenderer.php:256-279 (file input) and Advan… |
| **high** | Custom Reactions | Stored label and color are never consumed by any renderer. The filter merges slug strings only (extend_reaction_types returns slugs), Free's reaction_types() is a flat slug arra… | both | `confirmed-in-code` | /Users/vapvarun/dev/repos/buddynext-pro/includes/Reactions/CustomReactionsService.php l… |
| **high** | Custom Reactions | No mechanism for an admin to supply a glyph/SVG/image for a custom reaction. Built-in reactions rely on vendored Fluent SVGs keyed by slug; the admin form collects only slug/lab… | expectation-vs-reality | `confirmed-in-code` | /Users/vapvarun/dev/repos/buddynext-pro/includes/Admin/CustomReactionsAdmin.php lines 2… |
| **high** | Advanced Search Filters | Filter set mismatch between roadmap and code. Roadmap P12.1 names profile_field={k}={v}, activity_since=7d, in_space={id}, member_tier={slug}, has_label={slug}; code ships tier_… | plan-vs-reality | `confirmed-in-code` | docs/PRO-ROADMAP.md:550 vs /Users/vapvarun/dev/repos/buddynext/includes/Search/SearchSe… |
| **high** | Scheduled Posts | No member-facing UX to schedule a post. The composer/feed front end (Free templates and assets/js/feed/store.js) contains no 'Schedule' button or call to POST /buddynext-pro/v1/… | expectation-vs-reality | `confirmed-in-code` | grep across /Users/vapvarun/dev/repos/buddynext-pro/assets,templates,src and /Users/vap… |
| **high** | Multi-pin Posts | The journey's central scenario (Part 2 'Pin 10 posts in the space', plus the 'Profile scope vs space scope' edge case) cannot be exercised through the REST API: PostController::… | plan-vs-reality | `confirmed-in-code` | /Users/vapvarun/dev/repos/buddynext/includes/Feed/PostController.php:282 (no space_id p… |
| **high** | White Label | Configured logo_url is stored and previewed in admin but NEVER rendered on the front-end. HueOverride::render() emits only --bn-hue, --bn-font-ui, and custom CSS; logo_url is in… | both | `confirmed-in-code` | includes/WhiteLabel/HueOverride.php (render() lines 109-129 omit logo); grep across inc… |
| **high** | White Label | Per-space Brand tab is hidden from the very space owners it targets. SpaceBrandAdmin::register_brand_tab adds the tab with cap => 'manage_options' (line 126), but the nav part b… | plan-vs-reality | `confirmed-in-code` | includes/Admin/SpaceBrandAdmin.php:126 (cap 'manage_options'); buddynext/templates/part… |
| **high** | Unlimited Webhooks | Roadmap P13.1 claims Pro events (subscription_created, broadcast_dispatched, label_assigned) are subscribed into Free's outbound dispatcher 'so the full event catalog (Free + Pr… | both | `confirmed-in-code` | includes/Outbound/UnlimitedWebhooksIntegration.php (only file; cap filter only); grep s… |
| **high** | Bookmarks | Live table may have been patched out-of-band. The hub's b.id dependency is a hard code-level mismatch with Installer, but the local DB connection was closed and could not be que… | expectation-vs-reality | `needs-live-verification` | mcp__local-wp__mysql_schema wp_bn_bookmarks returned connection-closed error; Installer… |
| **high** | Blocking & Muting | Whether block fully severs DIRECT MESSAGING (the blocked user cannot open/continue a DM thread with the blocker) is not provable from the Free repo alone — messaging is a Pro/br… | expectation-vs-reality | `needs-live-verification` | templates/messages/thread.php, templates/messages/requests.php (block referenced; send-… |
| **high** | Stripe Payments | invoice.paid / invoice.payment_failed handlers key off invoice['subscription'], but Stripe API versions >= 2025-03 moved the subscription reference into invoice.parent.subscript… | expectation-vs-reality | `needs-live-verification` | includes/Stripe/WebhookController.php lines 356-358 and 403-405 read $invoice['subscrip… |

## Per-feature detail

### 🟡 Bookmarks (free) — partial

**Headline:** Bookmarks is fully built end-to-end (service, REST, post-card toggle, hub) but the saved-posts hub query depends on a bn_bookmarks.id column that the shipped schema never creates — a likely fatal SQL error on the hub page.

**Plan source:** No dedicated bookmarks plan doc. Coverage spread across docs/MASTER_DEVELOPMENT_PLAN.md (checklist lines 633-634, 668), docs/specs/features/02-activity-feed.md (table spec line 114), docs/journeys/activity-feed.md (REST flow lines 202-297), docs/QA-FRONTEND-ACTIONS.md (line 93).

**Plan promised:** Bookmarks are scoped as a sub-feature of the Activity Feed, not a standalone module. The plan promises a BookmarkService + BookmarkController (master plan checklist marks both done), a private per-user bn_bookmarks table, a save/unsave toggle on each post card via REST (POST/DELETE /posts/{id}/bookmark), and a bookmarks hub listing saved posts. docs/specs/features/02-activity-feed.md:114 explicitly specifies the table columns as "id, user_id, post_id, created_at". The master plan's standard section (MASTER_DEVELOPMENT_PLAN.md:97-102) demands production-ready, WPCS+PHPStan-validated code.

**Owner expects:**
- Logged-in members can save/unsave any post they can see, with an obvious toggle on the post card (BuddyPress/BuddyBoss favorites parity)
- A private 'Saved/Bookmarks' page listing the member's saved posts, newest-first, paginated
- Bookmarks are private to the saving user
- Removing a bookmark (from card or hub) reflects immediately
- Deleted/unavailable posts do not break the saved list; visibility respects privacy/blocks
- Reliable behavior on a real DB — the saved-posts page must actually load without SQL errors

**What we have:** Full vertical slice is built and wired. DB table created in includes/Core/Installer.php:420-425 (bn_bookmarks, PRIMARY KEY (user_id, post_id), columns user_id/post_id/created_at). Service includes/Feed/BookmarkService.php: bookmark()/unbookmark() with INSERT IGNORE + $wpdb->delete, is_bookmarked(), user_bookmarks() (cache group buddynext_bookmarks, 600s TTL), fires buddynext_post_bookmarked / _unbookmarked actions only on actual row change. REST controller includes/Feed/BookmarkController.php registers POST/DELETE /posts/{id}/bookmark and GET /me/bookmarks (supports ?expand=posts with pagination + visibility hydration), registered in includes/REST/Router.php:70. UX is genuinely wired (not just API): templates/partials/post-card.php:264-335 computes is_bookmarked per post and passes bookmarked/bookmarkNonce into the Interactivity context; templates/parts/post-actions.php:173-182 renders the toggle button with data-wp-on--click="actions.toggleBookmark"; assets/js/feed/store.js:785-801 implements optimistic toggle calling the REST endpoint with X-WP-Nonce. Hub at templates/feed/bookmarks.php with cursor pagination + full visibility gates (blocks, secret-space, followers-only, private, suspended/shadow-banned authors); routed via includes/Core/PageRouter.php:249/782/1369. PostService.php:323 cascades bookmark deletion when a post is hard-deleted.

**Gaps:**
- **[critical/both/confirmed-in-code]** The bookmarks hub query is broken against the shipped schema. templates/feed/bookmarks.php (lines 64, 68, 70, 81-83) selects 'b.id AS bookmark_id', orders by 'b.id DESC', and builds its entire cursor pagination on b.id. But the authoritative table schema in includes/Core/Installer.php:420-425 has NO id column — the PRIMARY KEY is the composite (user_id, post_id). The query 'SELECT b.id ... ORDER BY b.created_at DESC, b.id DESC' will fail with Unknown column 'b.id', so the saved-posts page errors out for every user. The spec (docs/specs/features/02-activity-feed.md:114) lists an id column, but it was never added to Installer; FREE-PRO-CONTRACT.md:181 names Installer.php as the authoritative schema source. _(includes/Core/Installer.php:420-425 (no id column) vs templates/feed/bookmarks.php:64,68,70,81-83 (depends on b.id))_
- **[high/expectation-vs-reality/needs-live-verification]** Live table may have been patched out-of-band. The hub's b.id dependency is a hard code-level mismatch with Installer, but the local DB connection was closed and could not be queried (SHOW COLUMNS failed). If a manual ALTER added an id column on the live site, the hub would load there while a fresh install would still break. Needs a live SHOW COLUMNS FROM wp_bn_bookmarks to confirm production impact severity. _(mcp__local-wp__mysql_schema wp_bn_bookmarks returned connection-closed error; Installer.php:420-425 is the only schema definition in repo)_
- **[low/plan-vs-reality/confirmed-in-code]** Documented BookmarkService::toggle() method does not exist. docs/journeys/activity-feed.md:229 states 'BookmarkService::toggle() inserts/deletes bn_bookmarks row', but the service exposes only bookmark()/unbookmark() (includes/Feed/BookmarkService.php:39,76). Toggle semantics live in the JS store (assets/js/feed/store.js:787, method chosen by ctx.bookmarked) and the REST layer, not the service. Doc-vs-code drift only; behavior is achievable. _(docs/journeys/activity-feed.md:229 vs includes/Feed/BookmarkService.php:39-105)_

### 🟡 PWA (free) — partial

**Headline:** PWA is fully wired at runtime (manifest + SW + 2 REST routes, booted via Plugin.php) but is shipped-broken: the manifest points at icon-192/512 PNGs that do not exist, so the app is not installable, and there is no push, no offline fallback, and no admin UI — and no plan doc ever scoped it.

**Plan source:** No dedicated PWA plan doc exists. MASTER_DEVELOPMENT_PLAN.md has no PWA section (only one offline-related mention at line 382, about SVG-initials avatars, unrelated to PWA). docs/v2 Plans/v2/mobile.html is a mobile-UI mockup with zero PWA/installability/service-worker/push references. Intent is therefore inferred only from the code's own structure (includes/PWA/PwaService.php).

**Plan promised:** There is no explicit written PWA plan. The feature appears to be an unsolicited/self-scoped addition: a single service (PwaService) that emits a Web App Manifest link, registers a service worker, and exposes two REST routes to serve the manifest JSON and SW script. The master plan's only relevant directive is the global quality bar ("No partial implementations — features are either complete or not started"), which this feature is measured against.

**Owner expects:**
- Site is installable to home screen: valid manifest with name, start_url, display=standalone, and real icon assets at 192px and 512px (Chrome/Android refuses install without these)
- Service worker that provides a usable offline experience — an offline fallback page at minimum, not just caching the homepage
- Manifest theme/background colors that match the site's actual brand (BN OKLCH tokens), not a hardcoded WordPress-blue default
- Web push notifications for activity/messages — table-stakes for a community PWA and the main reason owners adopt PWA over a plain mobile site
- An admin toggle/settings screen to enable PWA, set icons, name, colors (parity with BuddyBoss App / PWA plugins); filter-only configuration is not credible for a non-developer owner
- Per the master plan standard: complete, not partial — broken installability counts as a failed feature

**What we have:** includes/PWA/PwaService.php (single 273-line class, only file in includes/PWA/). It is booted: includes/Core/Plugin.php:668 binds 'pwa' => new PwaService(), and Plugin.php:253 calls ->init(). init() hooks: wp_head->output_manifest_link (emits <link rel=manifest> to /wp-json/buddynext/v1/pwa/manifest, line 148), wp_footer->output_sw_registration (inline JS registering the SW, gated by is_admin() check and the buddynext_pwa_register_sw filter, lines 66-97), and rest_api_init->register_routes (lines 227-247) registering GET /buddynext/v1/pwa/manifest and GET /buddynext/v1/pwa/sw, both permission_callback __return_true. get_manifest() (line 109) builds name/short_name/description/start_url/display=standalone/colors/icons/categories, filterable via buddynext_pwa_manifest. get_service_worker_script() (line 166) returns an inline SW: install caches only ['/'], network-first for /wp-json/, cache-first for everything else, no offline fallback. rest_service_worker() sets Service-Worker-Allowed: / header. No DB tables, no admin screen (grep of includes/Admin/ for 'pwa' = 0 hits), no push notifications, no background sync.

**Gaps:**
- **[critical/expectation-vs-reality/confirmed-in-code]** Manifest references icon assets that do not exist. get_manifest() (PwaService.php:122-130) points icons at plugins_url('assets/images/icon-192.png', ...) and 'assets/images/icon-512.png'. The assets/images/ directory does not exist (assets/ contains only css, emoji, fonts, icons, svg, js). No icon-192.png / icon-512.png exists anywhere in the repo. Chrome/Android require a valid 192px AND 512px icon to offer 'Add to Home Screen'; with 404 icons the app is effectively NOT installable — the core promise of a PWA. _(includes/PWA/PwaService.php:120-131; assets/ dir listing (no images/ subdir, no icon PNGs found via find))_
- **[high/expectation-vs-reality/confirmed-in-code]** No web push notifications and no background sync. The SW (PwaService.php:166-214) registers only install/activate/fetch handlers — no 'push' or 'notificationclick' listener, no subscription endpoint. Push is the primary reason a community owner chooses a PWA over a responsive site; BuddyBoss App ships it. Absent here. _(includes/PWA/PwaService.php:166-214 (no push/notificationclick handlers); no other PWA file exists in includes/PWA/)_
- **[high/plan-vs-reality/confirmed-in-code]** Feature violates the master plan's own 'no partial implementations' standard. A booted, wired PWA whose manifest points at 404 icons is shipped-but-broken at the install step — the exact 'partial' state the standard forbids. There is no written plan promising PWA, so this is plan-vs-reality only in the sense that the project's stated quality bar is not met. _(docs/MASTER_DEVELOPMENT_PLAN.md:104 ('No partial implementations'); includes/PWA/PwaService.php:120-131 (broken icon refs))_
- **[medium/expectation-vs-reality/confirmed-in-code]** Service worker provides no real offline experience. get_service_worker_script() (PwaService.php:174-176) precaches only ['/'] and uses cache-first/network-first with no offline fallback page; sub-pages a user has not previously visited will simply fail offline. Owners expect at least an offline fallback. Functional but minimal. _(includes/PWA/PwaService.php:166-214)_
- **[medium/expectation-vs-reality/confirmed-in-code]** No admin UI. A non-developer owner cannot enable PWA, upload icons, or set name/colors — configuration is filter-only (buddynext_pwa_manifest, buddynext_pwa_register_sw). grep of includes/Admin/ for 'pwa' returns zero hits. Below parity with PWA/BuddyBoss-App tooling. _(includes/PWA/PwaService.php:26,56,75,140; grep 'pwa' includes/Admin/ = 0 matches)_
- **[low/expectation-vs-reality/confirmed-in-code]** Hardcoded brand colors that ignore the site theme. theme_color '#0073aa' (WordPress default blue) and background_color '#ffffff' are static defaults (PwaService.php:116-117), not derived from BN's OKLCH theme tokens. The install splash/title-bar will not match the community's brand unless a developer adds a filter. _(includes/PWA/PwaService.php:116-117)_

### 🟡 Engagement / Leaderboard (free) — partial

**Headline:** Leaderboard UX is fully built and routed, but it is end-to-end non-functional: it queries WBGamification tables/columns that do not exist and the event bridge fires an action WBGam never listens to, so no points ever accrue and the page always shows empty.

**Plan source:** docs/MASTER_DEVELOPMENT_PLAN.md (Phase 10 WBGamification Bridge, lines 865-868, 1092, 1679) and docs/specs/features/12-wbgamification-bridge.md (Status: Locked).

**Plan promised:** BuddyNext does not own gamification. The plan is to BRIDGE to the WBGamification plugin: BuddyNext fires buddynext_* actions, the GamificationBridge translates them into WBGam events so WBGam's rules engine awards points/badges/levels; WBGam events flow back into the BuddyNext notification bell; and a "Leaderboard template integrated" surface plus profile/directory/space UI injection display WBGam data. All point/badge logic is meant to live in WBGam, nothing hard-coded in BuddyNext.

**Owner expects:**
- A reachable Leaderboard page ranking members by contribution, with period filters (week/month/all-time)
- Current user sees their own rank/points even when off the top-N list
- Points actually accrue from real community activity (posts, follows, connections, space joins) without manual admin scripting
- Badges/levels earned are visible on the leaderboard and surface as notifications
- Member opt-out / privacy respected on a public ranking (GDPR-adjacent)
- Leaderboard reflects the same numbers the gamification plugin's own admin/leaderboard shows (single source of truth)
- Reasonable performance at scale (cached ranking, not full-table scans per pageview)

**What we have:** A full-featured leaderboard template exists and is routed end-to-end: templates/gamification/leaderboard.php (hero stats, level meter, period tabs week/month/alltime, category tabs contributors/connectors/rising, ranked rows with badge ribbons, sidebar widgets for badges/points-breakdown/next-milestone). It is routed via includes/Core/PageRouter.php (resolve_hub_template case 'leaderboard' -> 'gamification/leaderboard.php'; rewrite rule at ~line 948 '<activity>/leaderboard/'; leaderboard_url() at line 1339), linked in primary nav (includes/Core/Plugin.php:541), exposed as a shortcode (includes/Shortcodes/ShortcodeService.php:82), feature-gated (includes/Core/FeatureRegistry.php:191), and styled (assets/css/bn-gamification.css, 598 lines) with an Interactivity store (assets/js/gamification/store.js). The outbound event bridge (includes/Bridges/GamificationBridge.php) maps buddynext_user_followed/connection_accepted/post_created/space_member_joined/strike_issued/profile_completion_changed -> do_action('wb_gamification_event', ...). The inbound listener (includes/Bridges/GamificationBridgeListener.php) creates bn.badge_awarded / bn.level_up notifications from wb_gamification_badge_awarded / wb_gamification_level_changed. SessionTracker (includes/Engagement/SessionTracker.php) fires buddynext_user_session_started and buddynext_user_daily_login as the streak driver. HOWEVER, verified against the installed WBGamification source (/Users/vapvarun/dev/repos/wb-gamification): (a) the leaderboard template queries tables that do NOT exist in WBGam — it uses wbg_user_points/wbg_user_badges/wbg_badges with columns awarded_at/event_type, but WBGam's Installer.php creates wb_gam_points (col created_at, action_id), wb_gam_user_badges (col earned_at), wb_gam_badge_defs (col image_url); (b) WBGam has NO add_action('wb_gamification_event', ...) listener anywhere — the bridge fires that action into the void; WBGam ingests only via wb_gamification_register_action() which binds a real WP hook to Engine::process(), and there are zero bn_* action manifests in WBGam.

**Gaps:**
- **[critical/both/confirmed-in-code]** The leaderboard template queries WBGamification tables/columns that do not exist. Template uses {prefix}wbg_user_points, {prefix}wbg_user_badges, {prefix}wbg_badges with columns awarded_at and event_type. WBGam's Installer.php actually creates {prefix}wb_gam_points (columns created_at, action_id, points), {prefix}wb_gam_user_badges (column earned_at), and {prefix}wb_gam_badge_defs (column image_url). Every SELECT in the template will error / return nothing against a real WBGam install, so the page always renders the 'No leaderboard data yet' empty state regardless of activity. _(templates/gamification/leaderboard.php lines ~69-71 (wbg_* table names, awarded_at, event_type) vs /Users/vapvarun/dev/repos/wb-gamification/src/Engine/Installer.php lines 50-97 (wb_gam_points/created_at/action_id, wb_gam_user_badges/earned_at, wb_gam_badge_defs/image_url))_
- **[critical/both/confirmed-in-code]** The outbound bridge fires do_action('wb_gamification_event', $type, $user, $ctx) but WBGamification never listens to that action. WBGam ingests events only through wb_gamification_register_action() (Registry binds the action's 'hook' to Engine::process via add_action), and ships no bn_* action manifest. No BuddyNext activity produces point rows in wb_gam_points unless a site author manually registers actions whose hook is the raw buddynext_* event. The spec's 'all mappings are WBGam rule configurations' assumes a wiring that does not exist on either side. _(includes/Bridges/GamificationBridge.php:141-150 (fire() -> do_action('wb_gamification_event')); grep of /Users/vapvarun/dev/repos/wb-gamification shows no add_action('wb_gamification_event',...) and no bn_ action manifests; ingestion path in /Users/vapvarun/dev/repos/wb-gamification/src/Engine/Registry.php:104-132 (hook+user_callback model))_
- **[high/expectation-vs-reality/confirmed-in-code]** Template hand-rolls ranking SQL instead of using WBGam's own LeaderboardEngine / LeaderboardController REST API and wb_gam_leaderboard_cache snapshot table. This bypasses the single source of truth, ignores the 5-minute cron snapshot (full GROUP BY scans on every pageview at scale), and duplicates ranking logic that already exists and is correct in WBGam. _(templates/gamification/leaderboard.php (raw $wpdb->get_results SUM/GROUP BY queries) vs /Users/vapvarun/dev/repos/wb-gamification/src/Engine/LeaderboardEngine.php (wb_gam_leaderboard_cache, 5-min snapshot cron) and /Users/vapvarun/dev/repos/wb-gamification/src/API/LeaderboardController.php (GET /wb-gamification/v1/leaderboard + /leaderboard/me))_
- **[high/expectation-vs-reality/confirmed-in-code]** Leaderboard opt-out / privacy is not honored. WBGam stores wb_gam_member_prefs.leaderboard_opt_out and its own LeaderboardEngine filters opted-out users; the BuddyNext template queries points directly and never checks this preference, so opted-out members would be publicly ranked. _(/Users/vapvarun/dev/repos/wb-gamification/src/Engine/Installer.php:177-186 (wb_gam_member_prefs.leaderboard_opt_out) and LeaderboardEngine.php header note 'users with leaderboard_opt_out = 1 are excluded'; no reference to member_prefs or opt_out in templates/gamification/leaderboard.php)_
- **[medium/plan-vs-reality/confirmed-in-code]** Spec-promised UI injection into Profile (points/level/badges via buddynext_profile_extra_data), Member Directory (points badge on cards), and Space pages (leaderboard sidebar widget) is not implemented. GamificationBridge only fires events and adds zero filters/UI hooks; the profile_extra_data filter is used only by JetonomyBridge. _(docs/specs/features/12-wbgamification-bridge.md 'UI Integration' section; includes/Bridges/GamificationBridge.php (no add_filter for profile_extra_data / member card / space tabs); grep shows buddynext_profile_extra_data consumed only by includes/Bridges/JetonomyBridge.php)_
- **[medium/both/confirmed-in-code]** In-template placeholders left as static fakes contradicting the master plan's 'no partial implementations' standard: rank-change delta is hard-coded to 0 ('No change' for everyone), the rank-window <select> is disabled at 'Top 10', and every Follow CTA renders with is_following=false (no follow-state hydration, no wired action — the gamification store.js only changes the period query arg). _(templates/gamification/leaderboard.php ($rank_changes set to 0 with comment 'WBGamification does not currently expose a rank_snapshot API'; <select id=bn-lb-window ... disabled>; $is_following = false comment 'bridge to BuddyPress follow API can hydrate later'); assets/js/gamification/store.js (only setFilter -> period query arg))_
- **[medium/expectation-vs-reality/confirmed-in-code]** Category filters reference event_type values that will never match WBGam data even after the table-name bug is fixed. Template filters 'connectors' on event_type IN ('bn_followed','bn_connected') and the points-breakdown label map keys on bn_post_created/bn_comment/bn_reaction/etc., but WBGam stores the column as action_id and the bridge action slugs are not registered as WBGam action ids, so these IN-clauses match nothing. _(templates/gamification/leaderboard.php ($cat_sql event_type IN ('bn_followed','bn_connected'); $event_labels keyed bn_post_created/bn_comment/bn_reaction) vs WBGam wb_gam_points.action_id column and absent bn_ action registration in /Users/vapvarun/dev/repos/wb-gamification)_
- **[low/plan-vs-reality/confirmed-in-code]** Bridge listener notification types use the bn.* namespace (bn.badge_awarded, bn.level_up) while the spec names them wb.badge_earned / wb.level_up. Cosmetic/contract drift, but means any notification-type-specific rendering keyed to the spec names would miss. _(includes/Bridges/GamificationBridgeListener.php:53,79 (type 'bn.badge_awarded' / 'bn.level_up') vs docs/specs/features/12-wbgamification-bridge.md ('wb.badge_earned', 'wb.level_up'))_

### 🟡 Post Composer & Posts (free) — partial

**Headline:** Robust post backend (CRUD, polls, mentions, content-warnings, scheduling, privacy gates) but the composer UI under-wires it: no content-warning, schedule, link, or working edit-post controls reach the user.

**Plan source:** docs/MASTER_DEVELOPMENT_PLAN.md (Post Types table ~L644-665, BLOCK 6 content warnings ~L247-252, BLOCK MC unified composer ~L1407-1425, scheduled-publish cron ~L1626); docs/v2 Plans/v2/post-detail.html (post-detail/thread mockup — composer is the reply composer, not the home composer)

**Plan promised:** The master plan specifies a post layer with types text/link/poll/activity/photo/video, REST CRUD (create/get/update/delete/pin), poll options (2-5), content warnings (nsfw/spoilers/violence/language), @mention parsing, link auto-unfurl, scheduled posts, edit-post tracking edited_at, and per-profile/per-space pinning. A single shared composer partial (BLOCK MC) must serve both the home feed and spaces with Photo/Poll/Link tools, privacy selector, media preview, and pass space_id to the REST body. There is no dedicated post-composer mockup; post-detail.html shows only the reply composer for a thread.

**Owner expects:**
- Compose and publish text, photo, poll, and link posts from one composer on feed and inside spaces
- Choose post privacy (public / followers / only-me) and post to a space
- Attach images with inline preview and per-item removal
- Create a poll with 2-5 options that members vote on inline
- Edit own posts and delete own posts; pin a post to profile/space
- Drafts not silently lost (auto-save / restore) like mature platforms
- Mentions notify mentioned users; links show a preview card
- Schedule a post to publish later if advertised
- Apply a content warning so sensitive posts are blurred

**What we have:** Backend is solid. PostService (includes/Feed/PostService.php) handles create/get/update/delete/pin/unpin against bn_posts with cache-backed reads, poll-option insertion (insert_poll_options, 2-5 enforced), @mention parsing firing buddynext_user_mentioned, OG link-meta auto-fetch via fetch_og_meta, content_warning + content_warning_type persistence, scheduled_at column, is_announcement gating to manage_options, and SafeguardService new-member pending-review gate. PostController (includes/Feed/PostController.php) exposes POST /posts, GET/PUT/DELETE /posts/{id}, POST|DELETE /posts/{id}/pin under buddynext/v1, with four privacy/visibility gates on GET (block list, secret-space membership, followers-only, private) and accepts content_warning, content_warning_type, scheduled_at, options, media_ids, link_url/link_meta. ComposerDraftController (includes/Feed/ComposerDraftController.php) provides POST/GET/DELETE /me/drafts in usermeta for opt-in cloud draft sync. UX: templates/partials/composer.php is the unified composer (avatar, textarea, Image/Poll/Event tools, privacy chip, draft status, media preview, poll inputs) wired to the buddynext/post-composer Interactivity store in assets/js/feed/store.js, whose submit() POSTs to /posts with content/privacy/type/media_ids/options. Drafts auto-save to localStorage (debounced) with optional cloud sync to /me/drafts. Scheduled publish is wired server-side: includes/Core/CronService.php::handle_publish_scheduled() publishes due status='scheduled' rows, scheduled via CronScheduler.

**Gaps:**
- **[high/both/confirmed-in-code]** Edit-post UX is a dead redirect. store.js editPost() (L897-901) navigates to '/activity/?edit='+postId, but PageRouter.php has no route honoring an ?edit= param for posts (its only 'edit' case returns profile/edit.php for member profiles). PUT /posts/{id} works at the API level, but no edit composer/template is wired to it, so 'Edit post' (plan Key Features) is not deliverable through the UI. _(assets/js/feed/store.js editPost L897-901; includes/Core/PageRouter.php L795-823 (no post-edit route); PostController.php update_post L225-252 (API exists))_
- **[medium/both/confirmed-in-code]** Composer submit() never sends content_warning or content_warning_type. The backend (PostService/PostController) fully supports content warnings (BLOCK 6, marked complete) but the composer UI in templates/partials/composer.php has no content-warning control and store.js submit() body only includes content/privacy/type/media_ids/options. A member cannot apply a content warning when posting. _(assets/js/feed/store.js submit() L1445-1473 (no content_warning); templates/partials/composer.php (no warning control); PostController.php L106-107 accepts the params)_
- **[medium/both/confirmed-in-code]** Scheduled posts have no composer UI. Server cron handler (CronService::handle_publish_scheduled) and the scheduled_at column/REST param exist, but the composer offers no date/time scheduling control and submit() never sends scheduled_at. The 'Event' modal sets link_meta.event_at, not the post's scheduled_at. So 'Scheduled posts' (plan Key Features) is backend-only, not user-reachable. _(assets/js/feed/store.js submit() L1445-1502 omits scheduled_at; templates/partials/composer.php has no schedule control; CronService.php L267 handle_publish_scheduled exists; plan docs/MASTER_DEVELOPMENT_PLAN.md L662)_
- **[medium/both/confirmed-in-code]** Link post type has no dedicated composer affordance. openLink() action exists in store.js (L1411) but no Link tool button is rendered in templates/partials/composer.php (only Image, Poll, Event + Pro injection slot). OG unfurl runs server-side only when link_url is posted, and submit() never sets type='link' or sends link_url, so the planned 'link' post type with auto-unfurl is not reachable from the free composer UI. _(assets/js/feed/store.js openLink L1411-1415 (orphaned); templates/partials/composer.php L201-246 tool row has no link button; submit() L1445-1473 omits link_url)_
- **[low/both/confirmed-in-code]** Rich text / formatting is not supported in the composer. The composer is a plain textarea (templates/partials/composer.php L143-148) and PostController sanitizes with wp_kses_post but the UI provides no formatting controls (code, quote, bold) that the v2 post-detail mockup's reply composer implies (Image/Code/Link/Mention tools) and that BuddyBoss/mature platforms offer. Plan lists 'Rich text' for text posts. _(templates/partials/composer.php L143-148 plain textarea; docs/v2 Plans/v2/post-detail.html L547-554 reply tools; plan L647 'Rich text')_
- **[low/expectation-vs-reality/needs-live-verification]** Single-source composer instance assumption in JS. pickMedia() and discardDraft() query document-global selectors (document.querySelector('.bn-composer__file-input'), '[data-wp-interactive="buddynext/post-composer"] .bn-composer__prompt') rather than the event's own composer, so if two composers render on one page (e.g., feed + a space panel) media upload and draft-discard can target the wrong/first composer. Needs live verification that only one composer ever renders per page. _(assets/js/feed/store.js pickMedia L1303-1313, discardDraft L1430; module-level _mediaState L1054 is global)_

### 🟡 Comments (free) — partial

**Headline:** Comments core is fully shipped and UX-wired (CRUD, react, reply, edit, pin, report, notifications), but @mentions are unparsed, client rendering is plain-text only, and the create path skips the spec's privacy-inheritance check.

**Plan source:** docs/specs/features/08-reactions-comments.md (locked spec, "Comments" section); docs/MASTER_DEVELOPMENT_PLAN.md Phase 7 (lines 759-775)

**Plan promised:** The locked spec promises a shared comment layer on any object type (posts, media via bridge, DM) backed by bn_comments. Comments: threading "two levels max (post -> comment -> reply, no deeper)", rich text with @mentions and emoji, edit-own with "edited" marker, soft delete with placeholder, react on comments, report to moderation queue, moderator pin on space posts, cursor-based pagination, privacy inherited from parent ("can't comment on content you can't see"), and comment-created notifications to the post owner.

**Owner expects:**
- Logged-in members can comment on feed posts and reply to comments, with author name + avatar and relative timestamps
- Threaded replies that stay readable (BuddyPress/BuddyBoss parity: at least one nesting level)
- Edit and delete your own comment; moderators/admins can remove any
- @mention a user in a comment and have them notified (BuddyBoss parity)
- Comment counts on the post stay accurate
- Report an abusive comment to a moderation queue
- Post owner is notified when their post is commented on
- Soft delete preserves thread shape without leaking the author
- Comments respect the parent's privacy: you cannot comment on content you cannot see

**What we have:** Full CRUD service at includes/Comments/CommentService.php: create/get/update/delete (soft-delete blanks content + is_deleted=1), edit() alias, pin()/unpin() (manage_options only, stored as bn_pinned_comment_{type}_{id} option), and an N-deep threaded list() with a MAX_REPLY_DEPTH=5 fold-back cap plus a restrict-gate that hides comments from owner-restricted users on the READ path. REST at includes/Comments/CommentController.php exposes POST/GET /comments, PUT/DELETE /comments/{id}, POST/DELETE /comments/{id}/pin under buddynext/v1; list_comments enriches each node with author_name, avatar, like_count, viewer_liked, can_edit/can_delete/can_pin, is_pinned, anonymizes deleted comments. DB table bn_comments created in includes/Core/Installer.php (lines 592-607) with the spec columns. UX is genuinely wired in assets/js/feed/store.js: buildCommentNode renders comment + nested replies, with React (6-emoji picker -> /reactions/toggle), Reply, Edit (PUT), Delete, Pin (POST/DELETE .../pin), and Report (POST /reports with object_type=comment). Comment form template at templates/parts/post-comment-form.php and list at templates/parts/post-comments-list.php. Notifications: includes/Notifications/NotificationListener.php on_comment_created (line 273) notifies the post owner; comment_count maintained on bn_posts in create()/delete() and recounted via includes/Core/CronService.php handle_recount_stats. WPMediaVerse bridge fires buddynext_comment_created for media (includes/Bridges/WPMediaVerseBridge.php:513).

**Gaps:**
- **[high/both/confirmed-in-code]** @mentions in comments are not parsed. Mention parsing + buddynext_user_mentioned only fires in includes/Feed/PostService.php (lines 169-184); the comment create path in CommentService::create() does no mention extraction and the JS renders comment.content via para.textContent (plain text). So an @mention in a comment is neither linked nor notified. Spec explicitly promised 'Rich text with @mentions'. _(includes/Comments/CommentService.php (create, no mention parse); includes/Feed/PostService.php:169-184; assets/js/feed/store.js:101 (para.textContent = comment.content))_
- **[high/both/confirmed-in-code]** Privacy inheritance not enforced on the comment create path. Spec: 'Inherits parent object's privacy. Can't comment on content you can't see.' CommentService::create() inserts on any object_type/object_id with only auth + non-empty content; it does not check the parent post's visibility/privacy or space membership. The restrict-gate in list() only hides comments on the READ side. A user could POST /comments to an object they cannot view. _(includes/Comments/CommentService.php:48-120 (create, no visibility check); includes/Comments/CommentController.php:170-209 (create permission_callback = require_auth only))_
- **[medium/plan-vs-reality/confirmed-in-code]** Rich text / emoji rendering not delivered on the client. Server stores content via wp_kses_post (HTML allowed) but the feed store renders comment.content with textContent, so any formatting or emoji-as-markup is shown as literal text. Spec promised 'Rich text ... and emoji'. (Unicode emoji typed inline would still display, but no rich-text formatting survives render.) _(assets/js/feed/store.js:94-102; includes/Comments/CommentService.php:49 (wp_kses_post on input))_
- **[medium/expectation-vs-reality/confirmed-in-code]** Reply pagination not implemented for large threads. list() fetches the ENTIRE descendant set for an object in one query (CommentService.php:402-412) and the code comment itself flags that viral threads (>1000 comments) 'would need per-branch pagination instead — that's a separate sprint.' Only top-level comments are paginated. Production risk on busy posts. _(includes/Comments/CommentService.php:395-412 (single descendant query, O(thread length)))_
- **[medium/plan-vs-reality/confirmed-in-code]** Pin is gated on manage_options (global admin), not space moderator. Spec: 'Moderator can pin a comment on space posts.' CommentService::pin()/unpin() and CommentController::require_moderator() both check user_can(manage_options) only — a space moderator who is not a WP admin cannot pin. Also the pin is stored object-global with no space-scope check. _(includes/Comments/CommentService.php:277,303; includes/Comments/CommentController.php:437-444)_
- **[medium/expectation-vs-reality/needs-live-verification]** No admin screen for comment moderation. Reports route comments into the moderation queue (store.js report btn -> POST /reports object_type=comment; includes/Moderation/ModerationService.php:47-121 accepts 'comment'), but there is no dedicated comments admin list to browse/bulk-delete comments independent of a report. Owner parity expectation (BuddyBoss has an Activity/Comment admin list). Needs live verification of whether the moderation admin surface renders comment reports actionably. _(includes/Moderation/ModerationService.php:47-156; no comments-specific admin file under includes/Comments/)_
- **[low/plan-vs-reality/confirmed-in-code]** Threading depth deviates from spec. Spec says 'two levels max (post -> comment -> reply, no deeper nesting)'. Code implements MAX_REPLY_DEPTH=5 with Discord-style fold-back (CommentService.php:321, list() attach closure; mirrored COMMENT_MAX_DEPTH=5 in store.js:24). This exceeds the locked spec rather than violating owner expectation, but it is an undocumented divergence from the 'Locked' spec. _(includes/Comments/CommentService.php:321,424-463; assets/js/feed/store.js:24)_
- **[low/plan-vs-reality/confirmed-in-code]** Cursor-based pagination promised by spec; offset/page pagination shipped instead. list_for_object() uses LIMIT/OFFSET with page numbers (CommentService.php:516-552) and the REST args expose page/per_page, not a cursor. Functionally adequate but a plan-vs-reality mismatch; offset pagination drifts on high-traffic threads. _(includes/Comments/CommentService.php:516-552; includes/Comments/CommentController.php:81-91)_

### 🟡 Polls (free) — partial

**Headline:** Core inline poll create/vote/results flow is fully shipped and wired end-to-end; gaps are a UI-capped 5th option, a dead 'Allow polls' admin toggle, and unimplemented expiry (end_date column exists but unused).

**Plan source:** docs/MASTER_DEVELOPMENT_PLAN.md (Phase 3 Feed — Post Types table line 649, DB Tables line 668, completion checklist lines 633-636, 768)

**Plan promised:** No dedicated poll spec doc exists. The master plan treats polls as one of the feed post types: a single table row reading "poll | 2–5 options, vote inline" (line 649), backed by bn_poll_options and bn_poll_votes tables (line 668). The completion checklist marks PollService, PollController, the my-vote endpoint, delete-cascade to bn_poll_votes, and the composer Poll button as done. Intent is a lightweight inline poll: create a poll with a handful of options from the composer, vote/switch/un-vote inline, see live percentage results. No expiry, multi-select, or admin moderation was ever scoped.

**Owner expects:**
- Members can create a poll from the composer with at least 2 and up to the documented 5 options
- Members vote inline, see live percentage bars and total vote count, and can change or retract their vote
- Vote integrity: one vote per user per poll enforced server-side
- Results visible to non-voters / logged-out visitors (public read)
- A site-wide on/off switch the owner can actually use to disable polls
- Poll closing / expiry so a poll stops accepting votes after a deadline (parity with BuddyBoss/BuddyPress poll add-ons)
- Poll data survives post deletion cleanly (no orphaned votes)

**What we have:** Fully wired end-to-end vote path. REST: PollController (includes/Feed/PollController.php) registers POST /posts/{id}/vote, GET /posts/{id}/poll, GET /posts/{id}/my-vote under buddynext/v1, registered in includes/REST/Router.php:69. Service: PollService (includes/Feed/PollService.php) handles vote/switch/toggle-off with vote_count denormalisation, results(), user_vote(), and fires do_action('buddynext_poll_voted'). Creation: PostService::create() validates 2–5 options and inserts via insert_poll_options() (includes/Feed/PostService.php:81-92,510-545); delete() cascades to bn_poll_votes + bn_poll_options (lines 304-305). DB: bn_poll_options (with an unused end_date column) and bn_poll_votes with UNIQUE KEY one_vote_per_user (includes/Core/Installer.php:449-469). UX: composer panel with Poll toggle button and 4 option inputs (templates/partials/composer.php:174-199, store.js togglePoll/submit at 1406-1469); voting UI with percentage fill bars, total votes, voted-state, Interactivity API bindings (templates/parts/post-body.php:214-267); post-card.php:233-254,338-341,473-475 passes pollNonce + reactive context; store.js votePoll action POSTs and updates state (assets/js/feed/store.js:1008-1036). Public results read works (permission_callback __return_true). Admin: a "Allow polls" checkbox registered in includes/Admin/Settings.php:49,693-696.

**Gaps:**
- **[high/expectation-vs-reality/confirmed-in-code]** The 'Allow polls' admin toggle (buddynext_allow_polls) is registered and rendered but never read or enforced anywhere — PostService::create() and the composer do not check it. Disabling polls in settings has no effect. _(grep of buddynext_allow_polls returns only Settings.php:49,693-696; no read in includes/Feed/PostService.php create() or templates/partials/composer.php)_
- **[medium/plan-vs-reality/confirmed-in-code]** Composer renders exactly 4 fixed option inputs (Option 1-4) with no 'add option' control, so the 5th option the backend explicitly allows (PostService caps at 5) is unreachable from the UI. Plan says 2–5; UI delivers 2–4. _(templates/partials/composer.php:187-198 (only 4 .bn-composer__poll-option inputs, no add button); store.js:1459 collects all .bn-composer__poll-option nodes; PostService.php:92 enforces max 5)_
- **[medium/expectation-vs-reality/confirmed-in-code]** Poll expiry/closing is not implemented. The bn_poll_options.end_date column exists in schema but is never written (insert_poll_options omits it) or read; PollService::vote() has no deadline check, so polls accept votes forever. _(Installer.php:455 defines end_date; grep shows no other read/write of poll end_date in includes/ or assets/; PostService.php:515-545 insert_poll_options writes only option_text/display_order)_
- **[low/expectation-vs-reality/confirmed-in-code]** No multi-select polls — single vote per user is hard-enforced by UNIQUE KEY one_vote_per_user and PollService toggle logic, with no allow_multiple option. This matches the master plan (single inline vote) but is below BuddyBoss feature parity a broad owner may expect. _(Installer.php:467 UNIQUE KEY one_vote_per_user; PollService.php:37-130 single-vote switch/toggle logic; no allow_multiple anywhere)_
- **[low/expectation-vs-reality/confirmed-in-code]** No admin moderation or results-inspection screen for polls; the only admin surface is the (non-functional) on/off toggle. Owners cannot view, close, or remove a specific poll's results from wp-admin. _(includes/Admin/Settings.php:693-696 is the sole admin poll reference; no poll admin screen in includes/Admin/)_

### 🟡 Shares / Reposts (free) — partial

**Headline:** Repost is shipped end-to-end (REST, table, modal, feed card, notification) but is one-way: no unshare in the UI, no profile shares tab, and quote is a text-only permalink dump rather than a structured quoted card.

**Plan source:** /Users/vapvarun/dev/repos/buddynext/docs/MASTER_DEVELOPMENT_PLAN.md (no dedicated Shares spec; share is mentioned only as a checklist item: line 633 ShareService, line 634 ShareController, line 668 bn_shares table, line 1108 "Shared posts | BuddyNext | share | done", and as a wired action in the post-card store at lines 1288/1401/1403). Quality bar from "The Standard We Are Building To" (lines 97-108).

**Plan promised:** The master plan treats Shares/Reposts as one of the core feed primitives but never gives it a dedicated journey/spec. It promises a ShareService + ShareController + bn_shares table, a "share" post type that surfaces in the home feed, and that the Share action button on every post card is wired to the correct REST endpoint with zero non-functional buttons (lines 1288, 1401, 1403). The implied standard is BuddyPress/BuddyBoss-parity reposting that is fully shippable, not a stub.

**Owner expects:**
- Member can repost any post they can see to their own feed; the repost renders as a card embedding the original post (author, content, media)
- Repost respects the original post's privacy/visibility
- Share count on the source post increments and is visible
- Post owner is notified when their post is shared
- Member can UNDO a repost (unshare) from the UI, and the button reflects shared/not-shared state
- Optional quote-repost: add a comment/note that displays with the embedded original
- Shares are discoverable on the sharer's profile (an activity/shares tab) — parity with BuddyPress activity reposts
- Re-shares of a re-share resolve to the original (no infinite nesting)
- Block-editor activity feed honors the same share action as the main feed

**What we have:** Full vertical slice is present and wired. REST: /Users/vapvarun/dev/repos/buddynext/includes/Feed/ShareController.php registers POST/DELETE /posts/{id}/share and GET /me/shares (auth-required), registered at includes/REST/Router.php:72. Service: includes/Feed/ShareService.php inserts into bn_shares (dedup via already_shared WP_Error), creates a type='share' bn_posts row inheriting the original's privacy, increments share_count via PostService::adjust_share_count, and fires buddynext_post_created + buddynext_post_shared. DB: bn_shares table defined in includes/Core/Installer.php:427 (id, user_id, post_id, content, created_at, indexed on user/post). Notification: includes/Notifications/NotificationListener.php:42 hooks buddynext_post_shared and creates a bn.post_shared notification to the post owner (with self-share and blocked-user guards). UX: templates/parts/post-actions.php (lines 158-169) renders the Share button (suppressed on type='share' cards), data-wp-on--click=actions.openShare; templates/partials/share-modal.php provides Repost/Quote/Copy-link CTAs; the buddynext/share-modal store (assets/js/feed/store.js:1852-1947) implements repost (POST), quote (prefills composer with permalink text), and copyLink. post-card.php (lines 102-115) re-queries and embeds the shared original. A repostFromCard optimistic fallback also exists (feed/store.js:849).

**Gaps:**
- **[high/expectation-vs-reality/confirmed-in-code]** No unshare/undo in the UI. The DELETE /posts/{id}/share endpoint exists in ShareController but no JS code path calls it — grep for unshare/DELETE-share in assets/js/feed/store.js and assets/js/spaces/store.js returns nothing. The Share button is one-way (repost only); shareShared state is set optimistically but there is no toggle to remove a repost. BuddyPress/BuddyBoss let users delete their reposts. _(/Users/vapvarun/dev/repos/buddynext/includes/Feed/ShareController.php (DELETE registered), assets/js/feed/store.js (no unshare caller), templates/parts/post-actions.php)_
- **[medium/expectation-vs-reality/confirmed-in-code]** GET /me/shares (share history) has no UX consumer. No template or profile store reads it; there is no profile 'Shares' tab. Shares are only discoverable as cards inline in the home feed, not collected on the sharer's profile the way BuddyPress activity reposts are. _(/Users/vapvarun/dev/repos/buddynext/includes/Feed/ShareController.php my_shares; grep of templates/ and assets/js/profile/ found no me/shares consumer)_
- **[medium/both/confirmed-in-code]** Quote-repost is text-only, not a structured quote. share-modal store quote() merely appends the permalink string into the composer textarea (assets/js/feed/store.js ~1906-1922); it does not create a share row with a note nor embed the quoted card. Also the modal's repost() POSTs with no content body, so the bn_shares.content / bn_posts.content quote-note field — which the service and schema support — is never populated from the primary UX path. _(/Users/vapvarun/dev/repos/buddynext/assets/js/feed/store.js (quote() and repost() actions), includes/Feed/ShareService.php (content param supported but unused by UX))_
- **[low/expectation-vs-reality/confirmed-in-code]** Re-share of a re-share is not flattened. ShareService::share stores shared_post_id = the post being shared, with no resolution to an original when that post is itself type='share'. A repost of a repost would embed a share card inside a share card. post-card.php only renders one level of shared_post. Needs runtime check to confirm visual outcome but the data path does not de-reference nested shares. _(/Users/vapvarun/dev/repos/buddynext/includes/Feed/ShareService.php (no flatten of shared_post_id), templates/partials/post-card.php:102-115 (single-level embed))_
- **[low/plan-vs-reality/needs-live-verification]** Block-editor activity feed (templates/blocks/activity-feed.php) is flagged in the plan (lines 1294/1400) as having a display-only post card not yet converted to the interactive shared partial; the Share action there may be non-functional. The block header comment claims actions are provided by the shared partial but the plan lists the conversion as still open. Requires live verification of which path the block actually renders. _(/Users/vapvarun/dev/repos/buddynext/docs/MASTER_DEVELOPMENT_PLAN.md:1400, templates/blocks/activity-feed.php)_

### 🟢 Member Profiles (free) — shipped

**Headline:** Member Profiles is genuinely shipped and mature — full custom-table model, data-driven field engine, server-side visibility gating, repeater fields, admin CRUD UI, and Interactivity-API edit form — but generic required-field enforcement is missing (silent save) and avatar crop is absent vs BuddyPress parity.

**Plan source:** docs/journeys/profile-fields.md (flow walk) + docs/MASTER_DEVELOPMENT_PLAN.md Phase 4 "Profiles + Member Directory + Search" / Profile Field Architecture + "The Standard We Are Building To". v2 mockup docs/v2 Plans/v2/user-profile.html referenced for view surface.

**Plan promised:** The plan called for a custom-table profile system (bn_profile_groups / bn_profile_fields / bn_profile_values) supporting flat AND repeater groups (Work Experience, Education), a data-driven field-type engine where adding a niche field type is "pure data, never code", per-field/per-group/per-entry visibility (public/followers/connections/private) enforced server-side by viewer relationship, a profile completion score, avatar + cover photo upload (focal point), profile-slug routing, member-type badges, a tabbed admin Profile Fields manager, and a full REST surface. The journey doc explicitly flagged POST group/field-create endpoints as a TODO "not in the manifest."

**Owner expects:**
- Admin can define profile field groups and fields (multiple types) from an admin screen without touching code
- Members can edit their profile and fill those fields, with validation and required-field enforcement
- Per-field privacy (public / followers-or-members / connections / private) honored everywhere a value is shown
- Avatar and cover photo upload with crop/positioning, plus sensible default avatars
- Profile view page showing identity, bio, fields, social counts, and follow/connect actions
- Repeater fields (work history, education) with multiple entries
- Profile completion indicator to nudge onboarding
- Searchable/directory-filterable fields without leaking private data
- Clean profile URLs (slugs)

**What we have:** Full custom-table implementation. ProfileService.php (1600 lines) does get_fields/get_groups, create/update/delete/reorder for groups and fields, save_profile with flat+repeater handling, get_profile with viewer-relationship visibility gating (group+field+entry, most-restrictive-wins, cache-keyed per relationship at lines 539-644), get_completion_score (lines 755-850), privacy-safe search-mirror to bn_field_{key} usermeta (sync_search_mirror lines 1373-1391, only mirrors when effective visibility resolves public), visibility clamp so members can only tighten (clamp_visibility lines 1311-1327), and avatar URL storage. FieldType.php (847 lines) is a complete data-driven engine: 14 built-in types (text/textarea/url/email/phone/number/date/boolean/select/radio/multiselect/color/file) with input render, display render, sanitize/validate, and searchable mirror, plus add-on filters buddynext_field_types / buddynext_field_render_input / buddynext_field_render_display / buddynext_field_sanitize. ProfileController.php (1300+ lines) registers the full REST surface incl. POST /profile-groups and POST /profile-fields (lines 226, 277 — contradicting the journey's "not in manifest" note), GET/PUT/DELETE/reorder, /me/profile GET+PUT, /me|users/{id}/avatar + /cover (real wp_handle_upload with type/size validation + cover focal point, handle_avatar_upload/handle_cover_upload lines 1115-1267), profile-slug get/set/check. Admin screen ProfileFieldsManager.php (1449 lines) is a real tabbed CRUD UI driven by FieldType::types(), supporting flat + repeater group types. Templates: profile/view.php (composer delegating to profile-hero/stats-strip/tab-bar/tab-panel parts, member-type badge, social graph state) and profile/edit.php wired through the Interactivity API (data-wp-interactive="buddynext/profile", actions.saveProfile, assets/js/profile/store.js) using FieldType::render_input for every field. AvatarService.php provides SVG-initials default avatars via the avatar filter.

**Gaps:**
- **[high/both/confirmed-in-code]** Required-field enforcement is incomplete. ProfileController::validate_profile_payload (lines 657-696) only validates display_name, a hardcoded list of URL fields, and audience enums. It does NOT iterate bn_profile_fields to enforce is_required on arbitrary custom fields. save_profile silently `continue`s past any field whose value fails (or is empty). The journey doc's stated edge case ('Mark years_of_experience required, submit empty -> Expected 422/400') will NOT return an error for a generic custom required field. _(includes/Profile/ProfileController.php:657-696 (validate_profile_payload), includes/Profile/ProfileService.php:419-465 (silent continue on save))_
- **[medium/expectation-vs-reality/needs-live-verification]** No server-side avatar/cover crop or resize. handle_avatar_upload/handle_cover_upload (ProfileController.php 1115-1267) validate type+size and run wp_handle_upload but store the raw uploaded URL; cover supports a focal_x/focal_y point only. BuddyPress/BuddyBoss offer interactive crop on avatar upload. Whether a JS-side cropper exists in the editor is not confirmed from PHP. _(includes/Profile/ProfileController.php:1115-1267 (handle_cover_upload/handle_avatar_upload), includes/Profile/AvatarService.php (no resize/crop methods))_
- **[medium/expectation-vs-reality/confirmed-in-code]** Search/directory filtering of custom fields relies on the bn_field_{key} usermeta mirror, which is written ONLY for flat + is_searchable + free-text-capable fields whose effective visibility is public (sync_search_mirror, ProfileService.php:1373-1391). number/date/boolean/color/file and any non-public field are intentionally not directory-filterable. An owner expecting to filter members by, e.g., a numeric 'years of experience' or a followers-only field will find it absent. Correct privacy behavior, but a functional limitation vs BuddyPress xprofile filters. _(includes/Profile/ProfileService.php:1373-1391 (sync_search_mirror), includes/Profile/FieldType.php:200-205 (is_text_searchable excludes number/date/boolean/color/file))_
- **[low/plan-vs-reality/confirmed-in-code]** Plan-vs-reality (resolved positively): the journey doc states POST /profile-groups and POST /profile-fields are 'not in the current manifest' and instructs WP-CLI inserts. The endpoints ARE registered (admin-gated) in ProfileController.php. The journey doc is stale, not the code. _(includes/Profile/ProfileController.php:216-312 (POST /profile-fields line 226, POST /profile-groups line 277) vs docs/journeys/profile-fields.md:46,71,246)_
- **[low/expectation-vs-reality/confirmed-in-code]** Profile completion score counts only flat-group fields (get_completion_score query WHERE g.type='flat', ProfileService.php:772) and weights every field equally with no concept of which fields are 'profile-essential'. Repeater fields (work history/education) never contribute. This is a documented design choice but a niche owner expecting work-history to count toward completion will be surprised. _(includes/Profile/ProfileService.php:755-850)_
- **[low/expectation-vs-reality/confirmed-in-code]** profile/view.php runs ~8 direct unbatched $wpdb COUNT/EXISTS queries per pageview for follower/following/connection/post counts and viewer relationship state (lines 49-73) in addition to ProfileService::get_profile. Functionally correct and cached at the get_profile layer, but the per-view social-graph probes are not cached. Scale concern on busy member pages. _(templates/profile/view.php:48-84)_

### 🟡 Member Types (free) — partial

**Headline:** Member Types is shipped and fully wired (tables, service, 7 REST routes, admin CRUD + per-member assignment, directory filter, profile/card badges) — but the promised profile-field-group scoping (type_restriction) has no read path, and self_select has a backend gate with no front-end UI.

**Plan source:** docs/MASTER_DEVELOPMENT_PLAN.md, section "12g — Member Types (grouping / segmentation of members)" (lines 398-559). Dedicated, detailed spec — no separate journey/mockup doc found; v2 prototype not checked but the master plan section is exhaustive (schema, scale design, REST table, admin tab, frontend integration, hooks, file list).

**Plan promised:** Admin-defined member type labels (Student, Alumni, etc.), one type per user in Free tier, stored in two custom tables (bn_member_types definitions + bn_member_type_assignments join) plus a write-through usermeta read cache for 100k-member-safe WP_User_Query filtering. Types render as colored badges on profile header, member cards, and as routable directory filter tabs (/members/{slug}/). Plan promised 7 REST routes, a Members > Member Types admin tab with full CRUD + per-member assignment dropdown + self_select toggle, four lifecycle hooks, and that profile field groups can be scoped to a specific type via a type_restriction column. All plan checkboxes are marked [x] done.

**Owner expects:**
- Admin can create/edit/delete named member types with a color badge and assign a type to any member (BuddyPress/BuddyBoss parity).
- Each type produces a filterable directory view at a clean URL, and types show as badges on member cards and profiles.
- Per-member assignment from the admin user-edit screen, not just bulk DB.
- Performance that holds at large member counts (indexed directory filtering).
- If self-assignment is offered, members have an actual front-end control to pick their type.
- If field groups can be scoped to a type, the restriction is actually enforced when rendering profile fields.
- Lifecycle hooks so other features/integrations react to type changes.

**What we have:** Feature is built and wired end-to-end at both API and UX layers. Schema: both tables created in includes/Core/Installer.php (lines 853-880) matching the plan, plus type_restriction column on bn_profile_groups (line 649). Service: includes/MemberTypes/MemberTypeService.php — full CRUD, assign/remove with single-type enforcement, write-through usermeta (bn_member_type) + CacheService caching, all 4 hooks (buddynext_member_type_assigned/removed/created/deleted), SVG sanitization, slug-rewrite cache rebuild. REST: includes/MemberTypes/MemberTypeController.php registers all 7 planned routes; wired via includes/REST/Router.php:83. Service bound in includes/Core/Plugin.php:656. Admin: includes/Admin/Members/MemberTypesManager.php — types tab (CRUD form with color pickers, self_select toggle, sort_order, badge preview), admin_post handlers for save/delete/assign, and a per-member assignment field rendered on buddynext_after_edit_member_form; tab registered in includes/Admin/Members.php (lines 53, 734, 745-746); admin JS at assets/js/admin/member-types.js. Routing: includes/Core/PageRouter.php adds %bn_member_type% rewrite tag (906), /members/{slug}/ rule (1043), sanitizes query var (1243-1245), member_type_url() builder (1517). Templates: directory filter via WP_User_Query meta_query on bn_member_type (templates/directory/members.php:186-195), sidebar pill tabs with counts (415-444), profile hero badge with color/text_color (templates/parts/profile-hero.php:222-228), member card type badge (templates/parts/member-card.php:188). REST member directory also filters by type (includes/Profile/MemberDirectoryService.php:218-220).

**Gaps:**
- **[high/both/confirmed-in-code]** Plan promised 'Profile field groups can be scoped to a specific type.' The type_restriction column exists in the schema and is cleared on type-delete in MemberTypeService::delete(), but NO read path consumes it — a project-wide grep finds type_restriction only in Installer.php, MemberTypeService.php, and the plan doc. No profile-field rendering code filters field groups by the current user's member type. The data model ships but the behavior does not. _(grep across /Users/vapvarun/dev/repos/buddynext returns type_restriction only in includes/Core/Installer.php, includes/MemberTypes/MemberTypeService.php, docs/MASTER_DEVELOPMENT_PLAN.md — no consumer in includes/Profile or templates.)_
- **[medium/expectation-vs-reality/confirmed-in-code]** self_select is enforced server-side (MemberTypeController::set_user_type allows self-assign when type self_select=1) but there is NO front-end UI for a member to self-assign a type — no template references self_select/self-assign. A site owner who enables 'Allow members to self-assign' gets a toggle that does nothing on the member side; assignment is API-only. _(grep for self_select/self-assign in /Users/vapvarun/dev/repos/buddynext/templates returns nothing; gate exists at includes/MemberTypes/MemberTypeController.php:302.)_
- **[low/plan-vs-reality/confirmed-in-code]** Plan file list placed the REST controller at includes/REST/Controllers/MemberTypeController.php and the admin manager at includes/Admin/Members/MemberTypesManager.php. The controller actually lives at includes/MemberTypes/MemberTypeController.php (manager path matches). Cosmetic path divergence from the plan; no functional impact since Router.php imports the correct namespace. _(find shows includes/MemberTypes/MemberTypeController.php; plan line 533 lists includes/REST/Controllers/MemberTypeController.php; Router.php:34 imports BuddyNext\MemberTypes\MemberTypeController.)_
- **[low/expectation-vs-reality/confirmed-in-code]** Single-type-per-user enforcement in assign_type() does DELETE-then-INSERT on bn_member_type_assignments without a transaction. Under a crash/concurrent write between the two statements a user could end up with zero or (briefly) duplicate rows; uq_user_type protects against duplicate identical pairs but not against the gap. Likely low-frequency, but not the race-safe guarantee the plan's 'scale design' narrative implies. _(includes/MemberTypes/MemberTypeService.php:418-432 — $wpdb->delete then $wpdb->insert, no transaction wrapper.)_

### 🟡 Follows (free) — partial

**Headline:** Follows backend and primary UX surfaces are production-grade and fully wired; the only real gap is two Gutenberg block templates (member-card, profile-header) whose follow buttons render but are inert because they use an unbound data-action instead of the Interactivity directive.

**Plan source:** docs/journeys/social-graph.md (dedicated journey doc); quality bar from docs/MASTER_DEVELOPMENT_PLAN.md "The Standard We Are Building To"

**Plan promised:** The social-graph journey doc specifies a one-directional follow system backed by wp_bn_follows: toggle follow/unfollow via REST, public follower/following lists, friends-of-friends follow-suggestions, self-follow rejection, and a block guard preventing follows between blocked parties. It fires buddynext_user_followed / buddynext_user_unfollowed and requires login for write endpoints. The journey treats follows as part of a larger social graph alongside connections and blocks. The master plan adds the cross-cutting standard that every UI must match the mockups exactly and no feature ships partial — buttons must actually work, not just render.

**Owner expects:**
- Follow/unfollow toggle that works from every place a user appears: profile header, member directory cards, sidebar widgets
- Accurate follower/following counts and browsable follower/following lists on profiles
- Private-account follow requests with an approve/reject inbox (parity with modern community platforms)
- Block boundaries enforced — blocked users cannot follow and are hidden from follower/following lists
- Follow suggestions to drive discovery for new/low-connection users
- Follow buttons rendered server-side must be functionally wired, not inert placeholders

**What we have:** Backend is complete and robust. FollowService (includes/SocialGraph/FollowService.php) implements follow/unfollow with INSERT IGNORE, self-follow rejection, a bidirectional block guard (lines 76-95), private-account pending status via bn_account_private user_meta, approve_follow_request/reject_follow_request, pending_followers inbox queries, cached followers/following/counts, friends-of-friends suggestions(), and fires buddynext_user_followed, buddynext_follower_gained, buddynext_user_followed_first_time, buddynext_follow_requested, buddynext_follow_request_approved/rejected. FollowController (includes/SocialGraph/FollowController.php) exposes POST/DELETE /users/{id}/follow, GET /users/{id}/followers, GET /users/{id}/following (both block-filtered via filter_blocked), GET /follow-suggestions, GET /me/follow-requests, POST /me/follow-requests/{id}/approve and /reject; writes require auth. DB schema in includes/Core/Installer.php (lines 351-359) has bn_follows with status ENUM('approved','pending') and a pending_inbox index. UX: assets/js/social/follow-store.js wires the follow button and follow-request approve/reject via the Interactivity API; templates/profile/followers.php renders the followers list plus a pending-request inbox; templates/profile/following.php renders following; templates/partials/follow-button.php and templates/blocks/follow-button.php are correctly bound (data-wp-on--click="actions.toggleFollow"); the member directory (templates/parts/member-card.php inside data-wp-interactive="buddynext/members", members/store.js) and the "People to Follow" sidebar widget (templates/partials/sidebar.php) are wired.

**Gaps:**
- **[high/both/confirmed-in-code]** The Gutenberg block templates templates/blocks/member-card.php (line 78) and templates/blocks/profile-header.php (line 99) render their Follow button with only data-action="bn-toggle-follow" plus data-user-id/data-nonce — there is NO data-wp-interactive root and NO data-wp-on--click directive. No JS in assets/js/ listens for [data-action="bn-toggle-follow"] (grep across assets/js confirms the only data-action handler is nav-manager's bn-open-add-tab). These two block-rendered follow buttons are inert: clicking them does nothing. The correctly-wired sibling templates/blocks/follow-button.php proves the intended pattern, making this an inconsistency, not an architecture gap. _(templates/blocks/member-card.php:78, templates/blocks/profile-header.php:94-106, assets/js/social/follow-store.js:104 (uses data-wp-on--click not data-action), grep of assets/js for bn-toggle-follow returns no handler)_
- **[low/plan-vs-reality/needs-live-verification]** The GET /follow-suggestions REST endpoint (FollowController::get_suggestions, backed by FollowService::suggestions friends-of-friends) has no confirmed client consumer. The 'People to Follow' sidebar widget (templates/partials/sidebar.php:48) calls a separate widget method suggested_follows(), not the REST endpoint or FollowService::suggestions(). The friends-of-friends algorithm may therefore be reachable only via direct API call, not surfaced in any shipped UI. Whether another consumer hydrates it could not be proven by reading code. _(includes/SocialGraph/FollowController.php:300-305, templates/partials/sidebar.php:48 (uses suggested_follows widget method), no template/JS references /follow-suggestions in grep)_
- **[low/expectation-vs-reality/confirmed-in-code]** member-card.php static follow button styling relies on CSS selector .bn-md-card__follow[data-state="following"] (assets/css/bn-members.css:825) but the static template templates/blocks/member-card.php never emits a data-state attribute (only the JS-built directory cards set it via data-wp-bind--data-state). So even visually the block card's following state will not render its intended treatment. Secondary to the wiring gap above. _(assets/css/bn-members.css:825, templates/blocks/member-card.php:73-83 (no data-state attribute))_

### 🟢 Blocking & Muting (free) — shipped

**Headline:** Blocking & Muting is shipped and genuinely wired end-to-end (service, REST, broad block enforcement, action buttons, confirm modal, and member-facing manage/undo lists); the real gaps are semantic (mute only suppresses notifications, not feed posts), an undocumented bonus 'restrict' type, no admin moderation view, and unverified DM-send enforcement under a block.

**Plan source:** docs/journeys/social-graph.md (Part 3: Block/Unblock + the "Mute vs block" edge case). docs/MASTER_DEVELOPMENT_PLAN.md ("The Standard We Are Building To"; Phase 2 Social Graph marked Verified).

**Plan promised:** The journey doc scopes Blocking & Muting as part of the Social Graph: a single bn_blocks table holding (blocker_id, blocked_id, type) pairs. Block is a hard, bidirectional cut (prevents follow/connect, profile/activity hidden, notifications suppressed); mute is a one-way silence where the muted user "still appears in feeds but their notifications are suppressed." The plan promises REST toggle endpoints (POST/DELETE block, POST/DELETE mute) plus GET /me/blocked and /me/muted lists, all requiring is_user_logged_in, with PrivacyService consuming the block graph. The doc does NOT mention "restrict" at all.

**Owner expects:**
- A member can block another member from the profile and from member listings, with a confirmation step and a clear success state
- Blocks are bidirectional: blocked user cannot see/follow/connect/message the blocker, and disappears from feeds, directory, search, and notifications for both parties
- A member can mute another member to reduce their noise without notifying them
- A member-facing screen to review and undo their blocked/muted lists (parity with BuddyBoss 'Blocked Members' settings tab)
- Block prevents the blocked user from re-initiating contact across all surfaces (DMs, mentions, connection requests, group/space interactions)
- No information leak that reveals to a blocked/muted user that they were blocked/muted
- Admin visibility/control over block relationships for moderation/abuse handling

**What we have:** BlockService (includes/SocialGraph/BlockService.php) implements block/unblock, mute/unmute, restrict/unrestrict against the bn_blocks table (schema in includes/Core/Installer.php:375-383: PRIMARY KEY (blocker_id, blocked_id), type ENUM('block','mute','restrict')), with object-cache reads (10-min TTL), bidirectional cache invalidation, self-action WP_Error guards, and a centralized is_user_online() presence gate. Block fires buddynext_block/buddynext_unblock; restrict fires buddynext_user_restricted/unrestricted; mute fires no action. BlockController (includes/SocialGraph/BlockController.php) registers POST/DELETE for /users/{id}/block, /mute, /restrict plus GET /me/blocked, /me/muted, /me/restricted, all gated by require_auth (401 if logged out). Block ENFORCEMENT is real and broad: bidirectional exclusion in PostController.php:162-167 (single-post 404), MemberDirectoryService.php:165-176, SearchService.php:188-190, SpaceMemberService.php:924-926, Pro bridges (JetonomyBridgeListener.php:110, WPMediaVerseBridge.php:320), and NotificationListener.php (block bidirectional, mute+restrict one-way at line 673). Restrict is enforced in comments (CommentService.php:353-381), reactions (ReactionService.php:384-406), and presence. UX is wired: profile/store.js (block/mute/restrict fetch calls + confirm modal + redirect on block), members/store.js (block/mute), templates/parts/member-block-modal.php + templates/partials/block-confirm-modal.php. Member-facing MANAGEMENT exists: templates/profile/edit.php:543-632 server-renders blocked/restricted/muted lists with Unblock/Unrestrict/Unmute buttons, and templates/parts/notifications-sidecard-muted.php renders a muted-people widget; both undo via assets/js/social/relation-remove.js, a generic document-level DELETE handler reading data-relation + data-user-id. includes/Blocks/ holds only BlockRegistrar.php (Gutenberg blocks — unrelated to user-blocking).

**Gaps:**
- **[high/expectation-vs-reality/needs-live-verification]** Whether block fully severs DIRECT MESSAGING (the blocked user cannot open/continue a DM thread with the blocker) is not provable from the Free repo alone — messaging is a Pro/bridge surface. templates/messages/thread.php and requests.php reference 'block' but the enforcement path for DM send between a blocked pair was not confirmed in code here. Needs live verification on a seeded site with two accounts in a block relationship. _(templates/messages/thread.php, templates/messages/requests.php (block referenced; send-side enforcement not confirmed in Free repo))_
- **[medium/expectation-vs-reality/confirmed-in-code]** Mute does NOT hide muted users' posts from the activity feed. Mute is enforced only in NotificationListener.php:673; FeedService.php has no muted_users/is_muted filter (grep of includes/Feed/*.php returns zero mute references). This matches the journey doc's explicit statement ('muted user still appears in feeds') so it is plan-consistent, but it diverges from BuddyBoss/Instagram mute semantics where muted content is removed from the feed — a site owner expecting 'mute = stop seeing their posts' gets only notification suppression. _(includes/Feed/FeedService.php (no mute filter), includes/Notifications/NotificationListener.php:654-673, docs/journeys/social-graph.md:152)_
- **[low/expectation-vs-reality/confirmed-in-code]** No admin-side visibility or control over block/mute relationships. grep of includes/Admin shows block references only in unrelated screens (Spaces, Settings, NavManager); there is no admin screen listing or moderating bn_blocks rows. Mature platforms give moderators insight into block graphs for abuse handling. Neither the journey doc nor master plan promised this, so it is purely an owner-expectation gap. _(includes/Admin/* (grep: no bn_blocks management screen), includes/Core/Installer.php:375)_
- **[low/plan-vs-reality/confirmed-in-code]** The 'restrict' relationship type (Instagram-style soft block) is fully built — service, REST routes, comments/reactions/presence/notification enforcement, profile-edit management UI — but appears NOWHERE in docs/journeys/social-graph.md, which only covers block and mute. This is shipped-beyond-plan: real and wired, but undocumented in the journey, so the plan understates what exists. _(includes/SocialGraph/BlockService.php:185-315, includes/SocialGraph/BlockController.php:67-112, docs/journeys/social-graph.md (no 'restrict' mention))_
- **[low/plan-vs-reality/confirmed-in-code]** The GET list endpoints /me/blocked, /me/muted, /me/restricted (BlockController.php:84-112) have no JavaScript/template consumer (grep of assets+templates for 'me/blocked|me/muted|me/restricted' returns nothing). The member-facing management lists are instead server-rendered directly via BlockService->blocked_users()/muted_users()/restricted_users() in profile/edit.php, so the UX works, but the three GET REST routes are effectively dead/redundant surface for the bundled UI (still usable by headless/Pro clients). _(includes/SocialGraph/BlockController.php:186-203,243-248; templates/profile/edit.php:547-549 (server-side calls instead); grep assets/js + templates: no me/blocked|me/muted consumer)_

### 🟡 Email System (free) — partial

**Headline:** Email pipeline is genuinely shipped and wired (immediate async send, prefs, cron digests, unsubscribe, 21 seeded templates, admin editor) but has a real high-severity token mismatch: the admin editor advertises/resets to tokens the runtime renderer never resolves, so edited templates leak literal {{placeholders}}.

**Plan source:** docs/journeys/notifications-email.md (dedicated journey doc); quality bar from docs/MASTER_DEVELOPMENT_PLAN.md "The Standard We Are Building To" (lines 97-108) and Phase 6 "Notifications + Email" marked Verified.

**Plan promised:** The journey doc promises a transactional email pipeline driven by notifications: NotificationService::create() fires buddynext_notification_created, EmailDispatchListener checks per-user bn_notification_prefs and dispatches via EmailSender for email_freq=immediate; daily/weekly route to a digest queue + cron; off suppresses. EmailSender renders a bn_email_templates row, sends via wp_mail, logs to bn_email_log, and emits HMAC-signed unsubscribe URLs handled on init. Filters buddynext_email_payload / _should_send / _send_at are the extension points. The master plan holds the bar at "complete or not started", premium UX, WPCS/PHPStan clean.

**Owner expects:**
- Lifecycle/transactional emails actually deliver for core events (follow, connection, mention, comment, reaction, space invite/join, moderation, welcome, email verification)
- Per-user email preferences with immediate / daily digest / weekly digest / off — and those settings actually change what is sent
- Working daily and weekly digest batching on a real cron, not just stub handlers
- One-click unsubscribe from emails that updates preferences
- Admin can edit subject/body/enable-disable each email template and send a test
- Tokens shown in the admin editor must be the tokens that actually resolve at send time (no literal {{placeholder}} leaking into inboxes)
- Sender name/address controls and an HTML email wrapper/branding consistent across all mail
- Send logging / deliverability visibility

**What we have:** Pipeline is real and wired. EmailDispatchListener (includes/Notifications/EmailDispatchListener.php) registers on buddynext_notification_created, buddynext_send_notification_email, buddynext_queue_email_digest, and an init unsubscribe handler; it is booted in includes/Core/Plugin.php:235. EmailSender (includes/Notifications/EmailSender.php) honors email_freq off/daily/weekly/immediate, dispatches immediate sends async via Action Scheduler (as_enqueue_async_action) with a synchronous fallback, renders a bn_email_templates row, calls wp_mail, applies the buddynext_email_payload filter (with send=>false suppression for Pro), logs to bn_email_log, and builds/verifies HMAC unsubscribe URLs (hash_hmac sha256 + hash_equals). Digest cron handlers exist and ARE scheduled: includes/Core/CronScheduler.php:80-81,174 wires buddynext_daily_digest / buddynext_weekly_digest to CronService::handle_daily_digest/handle_weekly_digest (includes/Core/CronService.php), which de-dupe via bn_email_log.digest_date and send via wp_mail. Note: CronService digests read a global email_freq pref (get_digest_user_ids, CronService.php:333) and the EmailDispatchListener digest queue writes per-user user_meta buddynext_digest_queue_{freq} (EmailDispatchListener.php:109-121) that CronService never reads — two parallel digest mechanisms. Installer (includes/Core/Installer.php:69-218) seeds 21 templates into bn_email_templates with INSERT IGNORE using only {{user_name}}/{{site_name}}/{{site_url}}/{{unsubscribe_url}} tokens. Admin editor EmailEditor (includes/Admin/EmailEditor.php) is bound in Plugin.php:665, gives a split-pane UI to edit subject/preview/body, enable/disable, reset-to-default, and send-test (admin_post_ handlers, nonce + manage_options guarded). DB tables bn_email_templates, bn_email_log, bn_notification_prefs all exist (Installer.php:545+).

**Gaps:**
- **[high/both/confirmed-in-code]** Token mismatch between the admin Email Template Editor catalogue and the runtime renderer. EmailEditor::get_catalogue() (includes/Admin/EmailEditor.php:91-252) advertises and seeds-on-reset rich tokens like {{follower_name}}, {{recipient_name}}, {{follower_bio}}, {{profile_url}}, {{follow_back_url}}, {{mentioner_name}}, {{context_excerpt}}. But EmailSender::render() (includes/Notifications/EmailSender.php:172-194) only resolves {{site_name}}, {{site_url}}, {{user_name}}, {{actor_name}}, {{notification_message}}, {{unsubscribe_url}} plus scalar keys from $data. The $data delivered for events (e.g. follow in NotificationListener.php:70-79 / NotificationService.php:138-167) contains only sender_id/object_id/type/group_key — none of the catalogue tokens. Result: if an admin edits or resets a template to catalogue defaults, sent emails leak literal {{follower_name}}, {{recipient_name}}, {{profile_url}} etc. The factory-seeded Installer templates avoid this because they use {{user_name}}, so the bug only surfaces after admin edit/reset. _(includes/Admin/EmailEditor.php:97-100 (catalogue tokens) vs includes/Notifications/EmailSender.php:172-194 (resolved tokens); includes/Notifications/NotificationListener.php:70-79 and includes/Notifications/NotificationService.php:138-167 (actual $data payload))_
- **[medium/plan-vs-reality/confirmed-in-code]** Two divergent digest mechanisms that do not connect. EmailDispatchListener::on_queue_email_digest stores queued notifications in per-user user_meta key buddynext_digest_queue_{freq} (EmailDispatchListener.php:109-121), but CronService digest handlers build their recipient list from a global email_freq pref query (CronService::get_digest_user_ids, CronService.php:333-341) and re-query notifications directly, never reading that user_meta queue. The queued meta is written and never consumed. Whether digests reach a daily/weekly-pref user depends on the global pref row, not on the per-type queue the listener fills. _(includes/Notifications/EmailDispatchListener.php:109-121 vs includes/Core/CronService.php:333-341 and CronService.php:59-86)_
- **[medium/expectation-vs-reality/confirmed-in-code]** No sender (From name/address) configuration and no shared branded HTML wrapper. EmailSender wraps the body in a bare <html><body>...</body></html> (EmailSender.php:125) with no header/footer/logo template; the editor Preview pane shows a branded frame (EmailEditor.php:915-933) but that chrome is preview-only and not applied to actual outgoing mail. From: address relies on wp_mail defaults. Mature platforms (BuddyPress/BuddyBoss) ship a consistent email template wrapper and sender controls. _(includes/Notifications/EmailSender.php:122-155 (no wrapper, no From header); includes/Admin/EmailEditor.php:906-933 (branded frame is preview-only))_
- **[low/plan-vs-reality/needs-live-verification]** Editor catalogue covers 21 template slugs but is not fully aligned with what runtime listeners actually trigger, so admin-facing template list may not 1:1 match dispatched mail. e.g. follow uses type bn.new_follower (NotificationListener.php:74) but the editor catalogue follow entry tokens assume data the dispatcher never supplies (see token gap). Recommend live verification that every catalogue slug has a real trigger path and vice-versa. _(includes/Admin/EmailEditor.php:91-252 (catalogue) vs includes/Notifications/NotificationListener.php (trigger types) and includes/Core/Installer.php:84-211 (21 seeded types))_
- **[low/expectation-vs-reality/confirmed-in-code]** Deliverability/log visibility is minimal. bn_email_log records only user_id/type/digest_date/sent_at (Installer.php schema) and there is no admin surface reading it; failures from wp_mail are not captured (return value of wp_mail in send_now is ignored, EmailSender.php:150-157). Owners get no in-product view of what was sent or what bounced. _(includes/Notifications/EmailSender.php:150-157 (wp_mail return ignored), includes/Core/Installer.php bn_email_log schema; no admin reader found for bn_email_log in includes/Admin/)_

### 🟡 Moderation (free) — partial

**Headline:** Moderation is broadly shipped end-to-end (service + full REST + routed front-end queue + report modals + safeguards + audit log), but suspension does not actually block posting and the appeal-approve path never lifts the suspension — the two enforcement promises the journey makes are unmet.

**Plan source:** docs/journeys/moderation-report.md (dedicated journey doc); docs/MASTER_DEVELOPMENT_PLAN.md ("The Standard We Are Building To", lines 97-108)

**Plan promised:** The journey doc specifies a full member-to-admin moderation lifecycle: members report content (POST /reports, unique-per-reporter), admins/space-moderators review a paginated queue (GET /reports/queue) and dismiss/escalate/resolve/remove reports, issue and reverse strikes, suspend/unsuspend users (with the explicit promise that suspended users get a 403 when posting — Step 14), warn, shadow-ban, and handle a user appeal flow (submit → approve/deny → suspension lifted). Every action writes an immutable bn_mod_log row and fires documented actions/filters. The master plan adds the production bar: complete features only, UX matching mockups, no partial implementations.

**Owner expects:**
- Members can flag posts/comments/users from the UI, with one report per user per object
- A moderator queue that lists pending/escalated reports with dismiss / resolve / remove-content actions reachable from the UI, not just REST
- Take-down of reported content (soft-remove so it disappears from public view but is auditable/restorable)
- Progressive enforcement: warn, strike (with thresholds), suspend (temporary + permanent), shadow-ban
- Suspension must actually block the offender from posting/acting while suspended, not merely hide their content after the fact
- An appeal path so suspended users can contest and admins can reverse
- Per-space moderation so space owners/mods handle their own reports without site-admin access
- Automated safeguards: banned words, blocked domains, rate limiting, new-member hold-for-review
- An audit log of every moderation action
- Admin notification when the queue backs up

**What we have:** Service + REST + UX are all present. ModerationService (includes/Moderation/ModerationService.php) implements report/dismiss/escalate/resolve/remove_content, issue_strike/reverse_strike/get_strikes/get_active_strike_count, suspend_user/unsuspend_user/is_suspended/get_suspension, warn/log_warning, shadow_ban/remove_shadow_ban/is_shadow_banned, submit_appeal/create_appeal/resolve_appeal/decide_appeal, plus get_queue with space-scoped filtering (get_moderated_space_ids). ModerationController (includes/Moderation/ModerationController.php) registers the full REST surface under buddynext/v1: /reports (POST/GET), /reports/queue, /reports/{id}/dismiss|escalate|resolve|remove, /users/{id}/strikes (+/{sid}/reverse), /users/{id}/suspend (POST/DELETE) and /suspension, /users/{id}/warn, /users/{id}/shadow-ban (POST/DELETE), /appeals (POST/GET) +/{id}/approve|deny|resolve, /me/appeals, /spaces/{id}/bans, /posts/{id}/content-warning. Permission callbacks gate require_auth / require_admin (manage_options) / require_queue_access (admin OR space owner/moderator) / require_space_owner_or_admin. ModerationListener (includes/Moderation/ModerationListener.php) wires notifications+emails for every event, auto-suspend/warn at configurable strike thresholds, soft-removes content on buddynext_content_removed (bn_posts status='deleted', bn_comments is_deleted=1), purges shadow-banned users from bn_search_index, and runs a daily WP-Cron queue-threshold alert email. SafeguardService (includes/Moderation/SafeguardService.php) enforces banned words, blocked domains, per-minute rate limit, and a new-member hold-for-review gate before any post insert, with buddynext_safeguard_check and buddynext_moderation_auto_actions filters for Pro. UX is real: front-end moderation hub page routed at /moderation via includes/Core/PageRouter.php (template templates/moderation/queue.php, 717 lines) driven by the Interactivity store assets/js/moderation/store.js (dismiss/removeContent/warn/strike/suspend wired to the real routes); report submission wired from templates/partials/report-modal.php and templates/parts/member-report-modal.php through assets/js/feed/store.js (fetch restUrl + '/reports'). FeedService (includes/Feed/FeedService.php) excludes suspended and shadow-banned authors' posts from feeds/queries via a SQL fragment; ProfileService (includes/Profile/ProfileService.php:527) returns a minimal profile for suspended users to non-admins.

**Gaps:**
- **[high/both/confirmed-in-code]** Suspension is NOT enforced on the posting path. The journey (Step 14) promises a suspended user gets 403 when calling POST /posts, but PostController::create_post (includes/Feed/PostController.php:94-128) and PostService::create (includes/Feed/PostService.php:70-117) never call ModerationService::is_suspended(); SafeguardService::check only covers banned words/domains/rate-limit/new-member gate (includes/Moderation/SafeguardService.php:40-76). A suspended user's POST /posts therefore succeeds (201); their content is only hidden after the fact by FeedService. There is no global rest_pre_dispatch suspension gate (grep found is_suspended only in ProfileService, MemberEditForm, ModerationService). Suspension is effectively a soft content-hide, not an action block. _(includes/Feed/PostController.php:94, includes/Feed/PostService.php:70, includes/Moderation/SafeguardService.php:40, docs/journeys/moderation-report.md (Step 14))_
- **[high/plan-vs-reality/confirmed-in-code]** Appeal API has duplicated, divergent paths that don't match the journey contract. The journey uses POST /me/appeals with a suspension_id body and expects the suspension to be lifted on approve. But create_own_appeal (ModerationController.php:1264) only accepts a message and calls create_appeal() which inserts a bn_appeals row with NO suspension_id link. Approving via decide_appeal()/resolve_appeal() updates appeal status but never lifts the linked suspension. Only the separate POST /appeals route (submit_appeal, requires suspension_id) links a suspension — and even its approve path does not call unsuspend. So 'approve appeal => suspension lifted' (journey Step 17-18) is not implemented anywhere. _(includes/Moderation/ModerationController.php:1264; includes/Moderation/ModerationService.php:1284 (create_appeal), :894 (resolve_appeal), :1333 (decide_appeal) — none call unsuspend/unsuspend_user)_
- **[medium/expectation-vs-reality/confirmed-in-code]** buddynext_appeal_submitted fires with two conflicting signatures. submit_appeal() fires it as (appeal_id, user_id, 'suspension', suspension_id) (ModerationService.php:880) while create_appeal() fires it as (user_id, insert_id) (ModerationService.php:1315). ModerationListener::on_appeal_submitted (ModerationListener.php:329) is registered with 2 args and treats arg1 as user_id, arg2 as appeal_id — correct for create_appeal() but inverted for submit_appeal(), so appeals from the suspension-linked path notify admins with swapped user/appeal IDs. _(includes/Moderation/ModerationService.php:880 vs :1315; includes/Moderation/ModerationListener.php:329-363)_
- **[medium/expectation-vs-reality/confirmed-in-code]** No moderator capability model beyond manage_options for user-level actions. Strikes, suspend, warn, shadow-ban all require_admin (manage_options) — only the report queue and space bans honor space owner/moderator roles. A site owner expecting delegated human moderators (a standard BuddyBoss/BuddyPress expectation) cannot let a non-admin moderator issue strikes or suspensions site-wide; they would need full admin. Whether this is intended scoping for Free vs Pro is not stated in the journey. _(includes/Moderation/ModerationController.php:151-405 (require_admin on strikes/suspend/warn/shadow-ban); :1089 require_queue_access is the only role-aware callback for user actions)_
- **[low/expectation-vs-reality/confirmed-in-code]** buddynext_user_shadow_banned fires with two conflicting arities. ModerationService::shadow_ban() fires (user_id, actor_id, reason) (line 715) and set_shadow_ban() fires (user_id) only (line 1025). The REST shadow-ban route calls set_shadow_ban() (single arg). ModerationListener::on_user_shadow_banned is registered with 1 arg (ModerationListener.php:35) so it works for the REST path, but any consumer expecting actor_id/reason gets nothing on the REST-driven path. Duplicate suspend/shadow methods (suspend_user vs suspend, shadow_ban vs set_shadow_ban) are a maintenance hazard. _(includes/Moderation/ModerationService.php:701,1013 (two shadow-ban paths); ModerationController.php:1176 (uses set_shadow_ban); ModerationListener.php:35)_
- **[low/plan-vs-reality/confirmed-in-code]** Journey route mismatches vs shipped routes (doc drift). The journey lists PUT /reports/{id}/escalate and /resolve, but the controller registers them under WP_REST_Server::EDITABLE which accepts PUT — OK. However the journey lists POST /reports/{id}/dismiss and POST /users/{id}/strikes/{sid}/reverse which match. The journey's GET /appeals 'list pending' and PUT /appeals/{id}/approve|deny all exist. Net: routes largely match, but the journey's claim that buddynext_user_warned/shadow_banned/appeal hooks were 'pending in HOOKS.md' is stale — they are implemented. This is documentation rot, not a code gap. _(docs/journeys/moderation-report.md (Known limitations, lines 329-332) vs includes/Moderation/ModerationService.php (warn/shadow/appeal hooks all fire))_

### 🟢 Authentication & Verification (free) — shipped

**Headline:** Auth + verification is shipped end-to-end (services, 7 REST routes, wired Interactivity templates, schema, cron) — but the advertised verification GATE is never enforced, resend is un-throttled, and token TTL (48h) contradicts every '24h' string.

**Plan source:** docs/journeys/auth-verification.md (dedicated journey doc) + docs/MASTER_DEVELOPMENT_PLAN.md "The Standard We Are Building To"

**Plan promised:** The journey doc describes an email-verification handshake: VerificationService::create_token() inserts a 64-char token into bn_verify_tokens with a ~24h expiry and fires buddynext_send_verification_email; VerificationListener hooks user_register and sends the email; the verify link (a ?bn_verify=TOKEN page URL, explicitly not a REST endpoint per the doc) calls verify(), sets usermeta buddynext_email_verified=1, deletes the token, and fires buddynext_user_verified / buddynext_email_verified. Resend regenerates a token. The doc also lists an edge case to confirm: whether posting is gated behind verification (buddynext_can('post')). It expected bn_email_log to record the dispatch. The master plan demands production-grade, fully-wired UX (templates match mockups, no partial implementations).

**Owner expects:**
- Custom login + registration that creates a usable account and signs the user in
- Email verification on registration with a clickable link, success/error/expired states, and a resend path
- Idempotent verification (already-verified users handled gracefully) and expired/invalid token rejection
- Self-service account management: change password (re-auth required), change email (confirm-then-swap to the new address), sign out everywhere
- Abuse protection: rate-limiting/throttle on resend so the endpoint cannot be used to spam an inbox
- An optional enforcement gate so unverified users can be restricted (post/comment) until verified — parity with BuddyPress/BuddyBoss require-verification
- Reliable delivery + logging of verification mail so owners can debug 'I never got the email'

**What we have:** Fully built and runtime-wired. VerificationService (includes/Auth/VerificationService.php) creates a 64-char hex token in bn_verify_tokens, fires buddynext_send_verification_email with the full ?bn_verify=TOKEN URL, verifies (sets usermeta buddynext_email_verified=1, deletes token, fires buddynext_user_verified), and has is_verified()/resend(). VerificationListener (includes/Auth/VerificationListener.php) is registered in Core/Plugin.php:188; it hooks user_register, the ?bn_verify and ?bn_verify_email init handlers, and sends mail via EmailSender (logs to bn_email_log) with a wp_mail fallback. AuthController (includes/Auth/AuthController.php) ships REST under buddynext/v1: /auth/login, /auth/register (issues token + auto-signin), /auth/verify/resend, /auth/verify/status, /auth/change-password, /auth/change-email (confirm-then-swap via transient token to the candidate address), /auth/sign-out-everywhere; registered in includes/REST/Router.php:61. UX is wired: templates/auth/verify.php (success/error/pending/email_changed/email_change_failed states) + assets/js/auth/verify-store.js (Interactivity store posting to resend with X-WP-Nonce), plus login/signup templates and stores. Schema in includes/Core/Installer.php:568 (UNIQUE KEY on token, 64-char). Cron prune in includes/Core/CronService.php:141. profile-hero.php:230 reads the meta for a verified badge.

**Gaps:**
- **[high/both/confirmed-in-code]** No enforcement gate. is_verified() exists but is never called to restrict any action — grep shows no buddynext_can('post')/comment check or any guard consulting buddynext_email_verified. FeatureRegistry.php:174 advertises 'gate certain actions on verified status' and the journey edge case asks to confirm a 403 for unverified posting, but verification is purely informational: the only consumer of the meta is a verified badge in profile-hero.php:230. A site owner who enables 'Require email verification' (Settings.php:627) gets the email flow but unverified users are NOT actually blocked from posting/joining. _(includes/Core/FeatureRegistry.php:174 + includes/Admin/Settings.php:627 promise gating; grep across includes/ and templates/ finds is_verified() called only inside AuthController status/resend, never as an action guard; only meta consumer is templates/parts/profile-hero.php:230 (badge))_
- **[medium/expectation-vs-reality/confirmed-in-code]** No rate-limiting / throttle on POST /auth/verify/resend. The endpoint only requires an authenticated session (require_auth) and resend() deletes+recreates a token on every call with no cooldown or transient guard, so a logged-in unverified user can trigger unlimited verification emails to their address. Mature platforms throttle this. No rate_limit/throttle/cooldown code exists in includes/Auth/. _(includes/Auth/AuthController.php:462-476 (resend_verification, no throttle) + includes/Auth/VerificationService.php:157-180 (resend deletes/recreates unconditionally); grep for rate_limit/throttle/cooldown in includes/Auth returns nothing)_
- **[medium/expectation-vs-reality/confirmed-in-code]** Registration auto-signs-in the user (wp_set_auth_cookie) immediately on POST /auth/register even when buddynext_email_verify is ON — the account is fully active before email is confirmed. Combined with the missing enforcement gate, 'Require email verification' provides no actual access restriction; it only changes the post-register redirect to the verify page. Whether this matches intended product behavior needs product confirmation. _(includes/Auth/AuthController.php:422-434 (create_token then wp_set_auth_cookie unconditionally; redirect-only difference when email_verify is set))_
- **[low/both/confirmed-in-code]** Token expiry is inconsistent: VerificationService::create_token() sets expires_at to +48 hours (VerificationService.php:44), but every user-facing string and docstring says 24 hours (template verify.php:189/270, email body VerificationListener.php:179, CronService.php:133 comment, and the journey doc's expectation of +24h). Functionally tokens live twice as long as advertised. _(includes/Auth/VerificationService.php:44 (+48 hours) vs templates/auth/verify.php:189 and includes/Auth/VerificationListener.php:179 (24 hours))_
- **[low/plan-vs-reality/confirmed-in-code]** Verification email is only logged to bn_email_log when EmailSender is available; the wp_mail fallback paths (no email_sender service, or no/disabled template) send mail but write no bn_email_log row. The journey expects a bn_email_log row for type=email_verify. So delivery logging — an owner's primary debugging tool — is conditional, not guaranteed. _(includes/Auth/VerificationListener.php:135-191 (EmailSender path returns early; both wp_mail fallbacks call wp_mail directly with no bn_email_log insert) vs includes/Notifications/EmailSender.php:271 which is the only verify-path writer)_

### 🟡 Onboarding (free) — partial

**Headline:** Both wizards and the invite tool are genuinely built and runtime-wired, but the bulk-invite half is hollow: invite links don't pre-fill or bypass the gate, status never leaves 'pending', and the admin can't see who registered.

**Plan source:** docs/specs/features/10-onboarding-setup-wizard.md (locked spec); docs/MASTER_DEVELOPMENT_PLAN.md Phase 11 + BLOCK 14; docs/v2 Plans/v2/onboarding.html (member wizard mockup, CSS/copy only)

**Plan promised:** The plan defines two separate wizards plus a bulk-invite tool. (1) Admin Setup Wizard: a one-time first-run wizard (spec says 6 steps: branding, registration, default notifications, space categories, addons, done) configuring the site, state in wp_options. (2) Member Onboarding Wizard: a once-per-user post-registration wizard (spec steps: profile, interests->space-category mapping, suggested spaces, suggested people), skippable, redirecting to the feed and firing buddynext_onboarding_completed which cancels nudge emails and awards WBGamification points/badge. (3) Nudge emails at +24h/+72h via Action Scheduler, cancelled on completion. (4) Admin bulk invite: CSV upload, personalised invite email with unique signup link that pre-fills email and bypasses the invite-only gate, per-row status pending/registered/bounced, resend per row or bulk, 7-day expiry, bn_invites table.

**Owner expects:**
- A first-run admin setup wizard that configures branding, registration mode, pages, and starter content so the site is usable without hunting through settings (BuddyBoss/BuddyPress parity)
- A guided new-member onboarding flow (profile, find spaces, follow people) that runs once after signup and is skippable
- Nudge/reminder emails to recover members who abandon onboarding, auto-cancelled once they finish
- A bulk invite tool: upload a list, send branded invite emails with working signup links, and SEE which invitees actually registered vs are still pending vs bounced
- Invite links that actually do something on the registration form (pre-fill email, satisfy an invite-only gate)
- Completion rewards/hooks wired to gamification when that plugin is active, per the plan's stated standard

**What we have:** Both wizards and the invite tool are real and wired at runtime, not stubs. ADMIN SETUP WIZARD: includes/Onboarding/SetupWizard.php is an 8-step wizard (branding, registration, profile-field presets, default notifications, space categories, community-page creation, addons review, done), registered via Plugin.php:129 (setup_wizard->init()) with a first-run redirect to ?page=buddynext-setup when buddynext_setup_complete!=1 (Plugin.php:140-141); it provisions profile-field groups, creates WP pages with shortcodes, seeds space categories, and saves brand/reg options. MEMBER WIZARD: 4 steps (Profile, Spaces, People, Notifications) — interests step deliberately removed (templates/onboarding/index.php:117-122). Fully reactive via Interactivity store assets/js/onboarding/store.js (@buddynext/onboarding); routed by PageRouter (case 'onboarding' -> onboarding/index.php, PageRouter.php:877-878), gated so completed users redirect to feed (PageRouter.php:203-208), and AuthController.php:428 redirects new users to onboarding_url() after registration. REST in OnboardingController.php (POST /me/onboarding/step|skip|complete); complete() joins spaces + follows users then fires finish(). OnboardingService.php fires buddynext_onboarding_completed once. NUDGE EMAILS: OnboardingListener.php schedules bn_onboarding_nudge_24h/72h via wp_schedule_single_event on user_register, cancels both on completion, both handlers share one bn.onboarding_nudge template (seeded Installer.php:206). INVITES: InviteService.php (create/resend/import_from_csv, 7-day expiry, secure token, sends bn.bulk_invite template), REST InviteController.php (POST /invites/import-csv, admin-only), admin tab InviteManager.php (CSV upload + pending table + resend). bn_invites table (Installer.php:789) has status ENUM pending/registered/bounced.

**Gaps:**
- **[high/both/confirmed-in-code]** Invite status never transitions past 'pending'. InviteService::mark_registered() and mark_bounced() have ZERO callers anywhere in includes/ (grep returns only the function definitions and a docblock). The bn_invites.status ENUM supports registered/bounced but nothing ever sets them, so the admin can never see who actually signed up. _(includes/Onboarding/InviteService.php:103-115 (mark_registered/mark_bounced defined); grep across includes/ shows no caller; AuthController has no invite handling)_
- **[high/both/confirmed-in-code]** Invite links are non-functional on the registration flow. The plan requires the invite token to pre-fill the email field and bypass the invite-only gate. AuthController.php has no bn_invite token handling at all, the signup template (templates/auth/signup.php) hardcodes email to '' with no token/prefill wiring, and get_by_token() has no caller. Invitees land on a generic signup form with nothing pre-filled and no gate bypass. _(grep for bn_invite/get_by_token/InviteService in includes/Auth/ returns nothing; templates/auth/signup.php:34 email=>''; InviteService.php:79 get_by_token has no caller)_
- **[medium/plan-vs-reality/confirmed-in-code]** Admin invite table omits the status column the plan explicitly requires ('Admin sees invite status per row: pending / registered / bounced'). InviteManager::render_invites_tab() lists only Email, First Name, Sent, Expires, Resend — no Status column — and only queries get_pending(), so registered/bounced invites are invisible. No bulk resend either (only per-row). _(includes/Admin/Members/InviteManager.php:209-245 (table headers + rows, no status), :138 get_pending() only)_
- **[medium/plan-vs-reality/confirmed-in-code]** Member wizard 'interests -> space-category mapping' step (spec step 2, plan step 'Pick interests') was removed and never replaced with the mapping behavior. Suggested spaces (step 2 now) are ordered purely by member_count, and suggested people by follower_count — not filtered by interests / mutual interest as the spec requires ('Suggested spaces filtered by interests', 'mutual-interest members'). This is a documented deliberate simplification in the template. _(templates/onboarding/index.php:117-122 (interests removed), :63 spaces ORDER BY member_count, :78-88 people ORDER BY follower_count)_
- **[medium/plan-vs-reality/confirmed-in-code]** Plan claims Action Scheduler for nudge emails and bulk invite batch sending; implementation uses raw wp_schedule_single_event for nudges and synchronous per-row wp_mail in the CSV import loop (no batching/queue). On large CSV uploads (limit 500) this sends all emails inline during the admin request, risking timeouts. _(includes/Onboarding/OnboardingListener.php:43-44 (wp_schedule_single_event, no AS); includes/Onboarding/InviteService.php:60-68 send_invite_email called inline in create(); grep for as_schedule/ActionScheduler in Onboarding returns nothing)_
- **[low/plan-vs-reality/needs-live-verification]** No gamification/WBGam reward on onboarding completion. Spec says completion 'awards WBGam points + Welcome badge'. In the Free repo the only subscriber to buddynext_onboarding_completed is OnboardingListener (nudge cancel). No GamificationBridge hook found in includes/. Per project memory BN routes gamification through Bridges; that wiring may live in the Pro repo or be intentionally Pro-only, so absence in Free is not necessarily a defect. _(grep buddynext_onboarding_completed across includes/ returns only SetupWizard/OnboardingListener/OnboardingService; no Bridges subscriber in free repo)_
- **[low/plan-vs-reality/confirmed-in-code]** Nudge emails use a single shared template for both +24h and +72h, contradicting the spec's two distinct messages (+24h 'Complete your profile', +72h 'Join your first space'). Both cron handlers call buddynext_service('email_sender')->send(...,'bn.onboarding_nudge',...) with identical args, and the +72h nudge is not conditioned on whether a space was joined (spec asks it be cancelled if a space is joined). _(includes/Onboarding/OnboardingListener.php:33-34,79-86 (both hooks -> handle_onboarding_nudge with same bn.onboarding_nudge template))_
- **[low/expectation-vs-reality/confirmed-in-code]** Member wizard step persistence is best-effort/fire-and-forget and the step dispatcher only saves step 1. OnboardingService::save_step() only persists profile data for step==1; nextStep() in store.js POSTs /me/onboarding/step with .catch(()=>{}) swallowing failures. Combined with the step gate using bn_onboarding_step, a mid-wizard reload could resume at a step whose join/follow actions already ran. Low risk (joins/follows are idempotent via own endpoints) but worth a live check. _(includes/Onboarding/OnboardingService.php:73-84 (only step1 saved); assets/js/onboarding/store.js:112-118 (catch swallows))_

### 🟡 Search (free) — partial

**Headline:** Search ships a working FULLTEXT index, public REST endpoint, and a full v2 results UI — but the UI and the REST/service layer are two divergent code paths with different exclusion logic, the promised admin reindex endpoint is absent, and hashtags/post-titles/member-fields are indexed inconsistently with the journey doc.

**Plan source:** docs/journeys/search.md (detailed REST/index journey) and docs/v2 Plans/v2/search-results.html (UI mockup, title "BuddyNext — Search"); quality bar from docs/MASTER_DEVELOPMENT_PLAN.md "The Standard We Are Building To".

**Plan promised:** The journey doc promises a unified FULLTEXT search over a bn_search_index table covering posts, users, spaces and hashtags, exposed at GET /buddynext/v1/search with q/type/per_page/page, returning a posts-keyed result set; viewer-aware exclusions for blocks (bn_blocks), suspensions (bn_user_suspensions) and shadow bans; index maintenance driven by lifecycle hooks (buddynext_post_created etc.) via SearchIndexListener; an admin reindex endpoint POST /buddynext/v1/search/index/{type} gated by manage_options (SearchController::reindex()); and the buddynext_search_query_args / buddynext_search_results / buddynext_search_performed extension hooks. The v2 mockup specifies a grouped results page (Members/Posts/Spaces/Hashtags/Media tabs) with date + sort filters, highlight, recent searches and a "/" focus shortcut.

**Owner expects:**
- Global search box reachable from anywhere that finds members, posts/activity, and groups/spaces (BuddyPress/BuddyBoss parity)
- Results respect privacy: private/secret content and blocked/suspended/banned users excluded for the viewer
- Relevance-ranked results with pagination and per-type filtering
- A way to (re)build the index after import/migration so existing content becomes searchable
- Reasonable result freshness as content is created/edited/deleted
- Autocomplete/typeahead or at least a fast results page; recent searches a plus
- Hashtag and media discovery consistent with what the activity feed exposes

**What we have:** Two parallel search implementations exist. (1) REST/service layer: includes/Search/SearchController.php registers GET /buddynext/v1/search (public, __return_true) with q/type/per_page/page; empty q returns 400; no type => SearchService::grouped_search() returns {grouped:true, results:{types:[{type,results,total}]}}; with type => flat {items,total}. includes/Search/SearchService.php does FULLTEXT MATCH...AGAINST on wp_bn_search_index (BOOLEAN MODE with prefix '*'), LIKE fallback when ft_search index absent, viewer block exclusion via bn_blocks, suspension exclusion via bn_user_suspensions, shadow-ban via usermeta bn_shadow_banned, and applies buddynext_search_query_args / buddynext_search_results (driver short-circuit) / buddynext_search_performed. (2) UI layer: templates/search/results.php (routed at /activity/search/ via includes/Core/PageRouter.php case 'search' => 'search/results.php', rewrite '^.../search/?$') renders the full v2 mockup with parts (templates/parts/search-*.php), date/sort filters, hashtag + media tabs, highlight, recent-searches + "/" shortcut in assets/js/search/store.js. The DB table bn_search_index is created with FULLTEXT ft_search(title,content) in includes/Core/Installer.php (lines 48, 686-703). Index maintenance: includes/Search/SearchIndexListener.php hooks buddynext_post_created/deleted and space create/update/delete, dispatching via Action Scheduler or inline; full reindex via buddynext_reindex_all handler (batches posts/users/spaces) scheduled on activation (Installer line 60). Users indexed by ProfileService::index_user() (display_name as title only).

**Gaps:**
- **[high/both/confirmed-in-code]** The admin reindex REST endpoint POST /buddynext/v1/search/index/{type} and SearchController::reindex() promised in the journey do not exist. SearchController only registers /search and /members. Reindex is only reachable via plugin activation (SearchService::schedule_reindex_all) or firing the buddynext_reindex_all action directly — no admin UI or REST trigger. A site owner who imports/migrates content has no supported way to rebuild the index. _(includes/Search/SearchController.php (only /search and /members registered); grep for 'search/index' and 'function reindex' returns no controller method; includes/Search/SearchService.php:496 schedule_reindex_all)_
- **[high/expectation-vs-reality/confirmed-in-code]** The user-facing search page (templates/search/results.php) bypasses SearchService entirely and runs its own raw SQL with a DIFFERENT exclusion model. SearchService excludes suspensions via bn_user_suspensions table; the template excludes via usermeta keys ('bn_suspended','bn_shadow_banned'). Two divergent definitions of 'suspended' mean the REST API and the rendered page can return different results for the same query. Also the template's block filter ignores bn_blocks.type='restrict' asymmetry that SearchService handles. _(templates/search/results.php:94-115 (usermeta bn_suspended/bn_shadow_banned + bn_blocks) vs includes/Search/SearchService.php:199-208 (bn_user_suspensions table + usermeta bn_shadow_banned))_
- **[medium/plan-vs-reality/confirmed-in-code]** Hashtags are not in bn_search_index, so the REST /search endpoint cannot return hashtag results despite the journey listing type=hashtags as a walked surface. SearchService::grouped_search discovers types from DISTINCT object_type in bn_search_index (only post/user/space ever indexed). Hashtag search works ONLY in the template, which queries bn_hashtags directly. REST type=hashtags yields nothing. _(includes/Search/SearchIndexListener.php indexes only post/user/space; templates/search/results.php:182-199 queries bn_hashtags separately; journey docs/journeys/search.md line 195 lists GET /search?type=hashtags)_
- **[medium/plan-vs-reality/confirmed-in-code]** REST response shape differs from the journey contract. Journey repeatedly expects a 'posts' array key (lines 62,70) and a flat type-filtered list; actual no-type response is {grouped:true, results:{types:[{type,results,total}]}} and type-filtered is {items,total}. Any client coded to the documented shape breaks. _(includes/Search/SearchController.php:164-179; docs/journeys/search.md:62,70,152)_
- **[medium/expectation-vs-reality/confirmed-in-code]** User index rows are only refreshed on display_name (title) and updated_at via ON DUPLICATE KEY UPDATE; username, bio/profile fields, and visibility are not (re)indexed. Members are searchable by display name only — narrower than BuddyPress member search (xprofile fields). Could surface as 'search can't find a member by username' though not provably broken without data. _(includes/Profile/ProfileService.php:859-881 (index_user inserts display_name as title, content '', ON DUPLICATE updates only title+updated_at))_
- **[medium/expectation-vs-reality/needs-live-verification]** Index freshness for posts depends on whether the buddynext_post_created hook reliably fires and (in production) Action Scheduler processes the queued job; edits to posts have no buddynext_post_updated indexing hook (only created/deleted), so editing a post's content does not re-index it. New-content searchability and edit-staleness should be confirmed live. _(includes/Search/SearchIndexListener.php:34-39 hooks post_created/post_deleted but no post_updated; async path via Action Scheduler in dispatch())_
- **[low/plan-vs-reality/confirmed-in-code]** Posts are indexed with an empty title (''), so FULLTEXT matching for posts relies entirely on the content column. The journey (Step 2/verification queries) expects a populated title. Minor relevance/ranking impact but contradicts documented index contents. _(includes/Search/SearchIndexListener.php:168-175 (index('post', id, '', content, ...)); docs/journeys/search.md:37-49)_
- **[low/expectation-vs-reality/confirmed-in-code]** No typeahead/autocomplete for global search. assets/js/search/store.js only handles follow/join toggles, date/sort navigation, '/' focus and a localStorage recent-searches list; there is no live suggestions call to /search. Results appear only on full page navigation to /activity/search/. BuddyBoss offers instant search dropdown. Whether this matters depends on owner expectation; it is a parity gap, not a defect. _(assets/js/search/store.js (no fetch to restSearchUrl for suggestions); PageRouter.php:596 exposes restSearchUrl but template form does a plain GET submit)_

### 🟡 Outbound Webhooks (free) — partial

**Headline:** Backend and admin CRUD shipped per BLOCK 7, but the UI only exposes 5 of 14 dispatchable events (one of which is never fired), never reveals the auto-generated signing secret, and surfaces no delivery-log viewer — leaving signed deliveries unverifiable and undebuggable from the admin.

**Plan source:** docs/MASTER_DEVELOPMENT_PLAN.md — BLOCK 7 "OutboundWebhookService" (lines 256-264), table additions (181-182), Settings tab listing (293), and Phase wiring notes (1628-1629, 1663, 1689-1694). No dedicated webhook spec doc found.

**Plan promised:** BLOCK 7 promised a new OutboundWebhookService that lets admins register/list/delete external webhook endpoints, dispatches HMAC-SHA256-signed payloads to matching active endpoints on community events, logs every delivery, auto-disables an endpoint after 3 consecutive failures, and runs a 5-minute WP-Cron retry job for failures in the last 24h. A REST controller exposes list/register/delete/log/test-ping. Dispatch must hook "all 13 spec events" (member registered/verified/suspended, post created/deleted, space joined/left, connection accepted, user followed, reaction added, comment created, ability granted/revoked). A Webhooks settings tab is part of the 10-tab admin Settings page.

**Owner expects:**
- Register multiple endpoints (BuddyPress/BuddyBoss + Zapier/Make parity); Free capping at 1 is acceptable only if clearly an upsell, not a silent failure
- Per-endpoint signing secret that the admin can actually retrieve, to verify HMAC signatures on the receiving side
- A selectable catalogue covering all events the platform actually fires, so each endpoint can subscribe to the events it cares about
- Visible delivery log / status per endpoint so the owner can debug failures (the log REST route exists; needs a UI)
- Test-ping button to validate an endpoint before going live
- Automatic retry + auto-disable on repeated failure with notification, so a dead endpoint doesn't silently drop events
- SSRF-safe URL validation (https-only at minimum) so a webhook can't be pointed at internal hosts

**What we have:** Backend is real and complete. includes/Outbound/OutboundWebhookService.php: register() (https-only + FILTER_VALIDATE_URL + buddynext_outbound_webhook_limit filter defaulting to 1), list_all(), delete(), get_log() (paginated), dispatch() (fan-out to active endpoints, empty events array = all), send_test_ping(), retry_failed() (24h window, cron buddynext_webhook_retry on buddynext_5min), deliver() (JSON envelope {event,timestamp,data}, HMAC-SHA256 in X-BuddyNext-Signature, logs to bn_outbound_webhook_log), maybe_deactivate_on_failure() (3 consecutive failures -> is_active=0). REST controller includes/Outbound/OutboundWebhookController.php registers GET/POST /webhooks, DELETE /webhooks/{id}, GET /webhooks/{id}/log, POST /webhooks/{id}/test, all behind manage_options; auto-generates a 40-char secret when omitted and returns it in the 201 body. Listener includes/Outbound/OutboundWebhookListener.php hooks 14 events (the 13 planned plus member.verified via buddynext_user_verified). Admin UX IS wired: includes/Admin/Settings.php render_tab_webhooks()/render_webhook_endpoints() renders the secret field, endpoint table (Endpoint/Events/Status/Created/Actions), event-checkbox add form, and Send-test/Remove buttons; assets/js/admin/settings.js initWebhookManager() POSTs to /webhooks, calls /{id}/test, and DELETEs /{id}. Tables bn_outbound_webhooks and bn_outbound_webhook_log are in the installer (plan lines 181-182). Note: AccessWebhookController.php in the same dir is a separate INBOUND endpoint (POST /webhook/access) and unrelated to outbound dispatch.

**Gaps:**
- **[high/expectation-vs-reality/confirmed-in-code]** Auto-generated signing secret is unrecoverable from the UI. POST /webhooks returns the generated secret in the 201 body, but settings.js add handler (assets/js/admin/settings.js lines 111-122) ignores res.body.secret and just reloads the page; the endpoint table (Settings.php lines 1232-1290) has no secret column. An admin who leaves the secret field blank can never obtain the secret needed to verify X-BuddyNext-Signature on the receiving side, making signed delivery unverifiable. _(assets/js/admin/settings.js lines 111-122; includes/Admin/Settings.php lines 1232-1290; OutboundWebhookController.php create_webhook() lines 178-200)_
- **[medium/both/confirmed-in-code]** Admin add-form event catalogue is a hardcoded list of 5 (post.created, comment.created, user.followed, space.joined, notification.sent) while the service actually dispatches 14 event slugs. Events such as member.registered, member.verified, user.suspended, connection.accepted, reaction.added, ability granted/revoked cannot be selected per-endpoint from the UI. Worse, notification.sent is offered but is never dispatched anywhere (no listener fires it), so subscribing to it yields zero deliveries. _(includes/Admin/Settings.php lines 1293-1299 (catalogue); includes/Outbound/OutboundWebhookListener.php lines 30-45 (14 hooked slugs, none named notification.sent); grep found no notification.sent dispatch in includes/)_
- **[medium/expectation-vs-reality/confirmed-in-code]** Delivery log has a REST route (GET /webhooks/{id}/log) and service method (get_log) but no admin UI surfaces it. The endpoint table shows only Active/Disabled status; there is no per-endpoint log viewer, last-response code, or failure detail in Settings.php. Owners cannot debug failed deliveries from the admin without calling the REST API directly. _(includes/Outbound/OutboundWebhookController.php get_log() lines 228-234; includes/Admin/Settings.php render_webhook_endpoints() lines 1201-1340 (no log rendering); assets/js/admin/settings.js (no log fetch))_
- **[low/expectation-vs-reality/confirmed-in-code]** Auto-disable after 3 failures is silent — no admin notification/email is sent when an endpoint is deactivated. maybe_deactivate_on_failure() flips is_active to 0 with no do_action/notification. An owner only discovers a dead integration by noticing the Disabled badge on next visit. (Plan did not require a notice, but mature-platform parity expects one.) _(includes/Outbound/OutboundWebhookService.php maybe_deactivate_on_failure() lines 481-511 (no do_action/email))_
- **[low/expectation-vs-reality/confirmed-in-code]** URL validation is https-only + FILTER_VALIDATE_URL but performs no SSRF guard: an endpoint can be registered against an internal/loopback host (https://127.0.0.1, https://169.254.169.254, https://internal-host) and the server will POST signed event payloads to it. No private-IP / metadata-endpoint blocklist. For a single-admin manage_options feature the blast radius is limited, but it is below mature-platform hardening expectations. _(includes/Outbound/OutboundWebhookService.php register() lines 78-92 (only str_starts_with https + FILTER_VALIDATE_URL))_
- **[low/plan-vs-reality/confirmed-in-code]** Retry strategy logs a new error row on each cron retry without bounding retries per original delivery; combined with the 3-consecutive-failure auto-disable, an endpoint that flaps will be deactivated quickly, and retry_failed() re-fires every error row from the last 24h on every 5-min tick, so a transient outage can generate duplicate deliveries to a recovered endpoint. Behavior matches plan literally but may surprise owners (no idempotency/dedup key beyond the envelope timestamp). _(includes/Outbound/OutboundWebhookService.php retry_failed() lines 348-385 and deliver() lines 404-474 (each retry inserts a fresh log row; selection is all status='error' rows in 24h, no retry counter))_

### 🟡 Realtime Updates (free) — partial

**Headline:** Free realtime is one polished surface (notification badge polling) plus a Pro-only transport seam; three of the four polling behaviors the spec promised for Free (DM 5s, feed 60s, presence 2min) are not implemented in this repo.

**Plan source:** docs/specs/features/P3-realtime-websocket.md (dedicated, "Locked"); referenced from docs/MASTER_DEVELOPMENT_PLAN.md lines 55, 977, 1007

**Plan promised:** The plan treats true WebSocket realtime as a Pro feature (Ratchet/Soketi swapped in via the buddynext_realtime_transport filter). The Free tier is explicitly the "polling-based real-time approximation" that gracefully degrades: notification bell poll every 30s, DM conversations poll every 5s, feed new-posts bar poll every 60s, online presence poll every 2min. Free ships the transport seam (interface + no-op PollingTransport + factory/filter) so Pro can override. The spec lists "Gaps / Open Questions: None — fully locked."

**Owner expects:**
- Notification badge updates without a page reload (parity with BuddyPress heartbeat-driven counts)
- New direct messages appear in an open conversation without manual refresh
- A 'new posts' indicator on the activity feed so the viewer knows fresh content arrived
- Online/last-active presence that stays reasonably current while a member browses
- Polling that backs off when the tab is hidden and does not hammer the server
- Graceful behavior with no Pro/WebSocket layer present — Free should still feel live-ish, not static

**What we have:** Free realtime is split between a Pro-handoff seam and one real polling loop. The seam: includes/Realtime/RealtimeTransport.php (interface), includes/Realtime/PollingTransport.php (no-op push), includes/Realtime/TransportFactory.php (apply_filters('buddynext_realtime_transport')). TransportFactory::current() is bound into the container at includes/Core/Plugin.php:691 as 'realtime' but is never resolved or called by any Free code (no ->push() callers found outside the Realtime dir) — it is purely a forward seam for Pro. The only fully working Free polling surface is notifications: assets/js/notifications/store.js:422-595 implements a 30s cold / 5s hot cadence poll against the real GET /buddynext/v1/me/notifications/unread-count route (includes/Notifications/NotificationController.php:48-56, backed by NotificationService::unread_count at includes/Notifications/NotificationService.php:285), pauses on document.hidden, re-polls on visibilitychange, paints the mobile/header badge, and exposes a bn:notification:new window event seam plus optional sound. DM realtime is NOT in this repo: assets/js/messages/store.js is a 9-line stub delegating all messaging to WPMediaVerse (mvs/messaging). The feed new-posts bar (assets/js/feed/store.js:2374-2423) only adds an event listener for the Pro-fired bn:realtime:post-new CustomEvent and is an explicit no-op when Pro is absent — there is no Free polling producer for it. Online presence exists server-side (is_online flag and ?online=1 filter via includes/Profile/MemberDirectoryService.php / MemberDirectoryController.php, SessionTracker at includes/Engagement/SessionTracker.php), surfaced in assets/js/members/store.js:172 (data-presence), but is only refreshed on a directory filter/search action — no 2min client poll. No admin screen governs realtime (the 'realtime' => 'zap' hit in includes/Admin/AdminHub.php:278 is just a Lucide icon slug).

**Gaps:**
- **[high/both/confirmed-in-code]** Spec promises Free DM conversations poll every 5s, but BuddyNext ships no DM polling — assets/js/messages/store.js is a 9-line stub that hands all messaging to WPMediaVerse. The 5s DM live-update behavior is not implemented in this repo and depends entirely on an external plugin. _(assets/js/messages/store.js (lines 1-9, intentionally empty); P3-realtime-websocket.md line 19)_
- **[high/both/confirmed-in-code]** Spec promises a Free feed new-posts bar polling every 60s, but the feed pill (assets/js/feed/store.js:2374-2423) only listens for the Pro-dispatched bn:realtime:post-new event and is documented as a no-op when no realtime layer is active. No Free-side 60s poll produces that event, so the 'new posts' indicator never appears on Free. _(assets/js/feed/store.js:2366-2423 (comment 'No-op when no realtime layer is active (the event never fires)'))_
- **[medium/both/confirmed-in-code]** Spec promises Free online presence polling every 2min, but there is no client-side presence poll. Presence (is_online) is computed server-side and only refreshes when the member directory is re-fetched on a filter/search action; an idle directory view goes stale until reloaded. _(assets/js/members/store.js (no setInterval; presence only updated in renderItem at line 172 on fetch at line 344); includes/Engagement/SessionTracker.php; P3-realtime-websocket.md line 21)_
- **[medium/expectation-vs-reality/confirmed-in-code]** Notification polling is the one surface that fully meets the bar, and it slightly exceeds the plan (adds a 5s hot cadence for 60s after user action, visibilitychange re-poll, optional sound) — but it is the ONLY live Free realtime surface. A site owner expecting 'live-ish' behavior across DMs, feed, and presence on Free gets only a live notification badge. _(assets/js/notifications/store.js:422-595; includes/Notifications/NotificationController.php:48-56)_
- **[low/plan-vs-reality/confirmed-in-code]** The TransportFactory/PollingTransport/RealtimeTransport server-side abstraction is dead weight on Free: bound into the container at Plugin.php:691 but never resolved and ->push() is never called by any Free code. It exists only as a Pro filter seam. This is intentional per design, but means the 'Realtime' includes/ directory contributes nothing to Free runtime behavior — the only working Free realtime (notification badge) is implemented independently in JS, not through this transport. _(includes/Core/Plugin.php:691; grep for ->push( and TransportFactory::current shows no Free callers outside includes/Realtime/)_

### 🟡 Privacy Framework (free) — partial

**Headline:** Field-level field privacy is fully shipped and wired; the social-graph privacy preferences (who_can_follow/connect, profile_visibility) and GDPR data-rights are scaffolded but orphaned — preferences have no UI/save/enforcement path, the can_view filter is missing, and data export/deletion is settings-toggles with no backend.

**Plan source:** docs/specs/features/member-fields-search-privacy.yaml (dedicated field-level privacy spec); docs/MASTER_DEVELOPMENT_PLAN.md lines 592 (PrivacyService listed "done"), 619-620 (buddynext_can_view filter), 293/1018 (admin "Privacy & Data" tab), 97-108 ("The Standard We Are Building To": no partial implementations).

**Plan promised:** Two distinct privacy concepts were planned. (1) A field-level privacy engine (member-fields-search-privacy.yaml): admin sets each field's default visibility from a 4-value enum [public, followers, connections, private], a member may only TIGHTEN it, effective visibility = most restrictive of group/field/entry, and a privacy-aware search mirror (bn_field_{key}) is written only when effective visibility is public. (2) A social-graph PrivacyService (master plan line 592) controlling who_can_follow / who_can_connect / profile_visibility, plus a pluggable buddynext_can_view filter (line 619) and an admin "Privacy & Data" tab covering indexing, retention, data export, and account deletion (lines 293, 1018).

**Owner expects:**
- Members can set who sees their profile / posts (public, followers/members, connections, private) and that choice is actually enforced when a stranger visits — BuddyPress/BuddyBoss parity
- Per-profile-field visibility controls (e.g. hide email/phone from non-connections) that hold on the profile-view page and in the directory/search
- Members can control who may follow or send connection requests, and the platform refuses disallowed follow/connect attempts
- A working GDPR path: member-initiated data export (download my data) and account deletion/anonymisation, plus WP core personal-data exporter/eraser integration
- Admin defaults for new-account privacy and site-wide indexing that members can override
- Blocking always overrides any privacy allowance

**What we have:** Field-level privacy engine is fully shipped and wired end-to-end. includes/Profile/FieldType.php is the type engine; includes/Profile/ProfileService.php implements clamp_visibility() (member may only tighten, line 1311), effective_visibility() (most-restrictive-wins, line 1337), sync_search_mirror()/rebuild_field_mirror() writing bn_field_{key} only when effective visibility is public (line 1373). save_profile() accepts {field_key}__visibility and group_key[n][_visibility] and clamps them (lines 396-461). get_profile() enforces group/field/entry visibility for non-owners (lines 614-641). templates/profile/edit.php renders a per-field privacy <select> (bn_privacy_select closure, line 152; flat at 472-478, repeater at 414-421) and templates/profile/view.php gates the whole profile via buddynext_service('privacy')->can_view_profile() (line 34). The social-graph layer exists as includes/SocialGraph/PrivacyService.php (registered in Core/Plugin.php line 623) with can_follow/can_connect/can_view_profile/can_view_activity; can_view_activity is consumed by includes/Feed/FeedService.php line 521 and can_view_profile by view.php. Admin "Privacy & Data" tab is rendered in includes/Admin/Settings.php render_tab_privacy() (line 1018): indexing select, cookie-consent toggle, data-retention days, and toggles for data export / account deletion / anonymise-on-delete. The edit-profile Privacy section exposes bn_account_private, bn_privacy_see_email, bn_privacy_dm, bn_privacy_mention, bn_privacy_show_in_directory, bn_privacy_search_indexable (edit.php lines 198-223; saved in ProfileController.php lines 572-594; toggled via store.js togglePref line 702).

**Gaps:**
- **[high/both/confirmed-in-code]** PrivacyService's three core preference keys (who_can_follow, who_can_connect, profile_visibility) have NO UI to set them and NO save path. The edit-profile form (edit.php) and ProfileController save list (lines 572-594) only persist bn_account_private + bn_privacy_* keys; the strings who_can_follow/who_can_connect/profile_visibility appear nowhere outside PrivacyService.php itself. They therefore always resolve to the hardcoded defaults (everyone/everyone/public). _(includes/SocialGraph/PrivacyService.php lines 37-39 vs grep of includes/ + templates/ finding no other write of these keys; ProfileController.php lines 572-594)_
- **[high/expectation-vs-reality/confirmed-in-code]** profile_visibility is read by view.php->can_view_profile() to decide 'This profile is private', but since no UI ever writes profile_visibility, it is permanently 'public'. The whole-profile privacy gate is effectively inert — a member cannot make their profile followers/connections/private even though the resolution logic exists. _(templates/profile/view.php line 34 calls can_view_profile(); PrivacyService.php line 185 reads profile_visibility; no writer exists in ProfileController.php / edit.php)_
- **[high/both/confirmed-in-code]** GDPR/data-rights is admin-settings-only with no backend. The 'Allow members to export their data' / 'Allow members to delete their account' / 'Anonymise on delete' toggles (Settings.php lines 1061-1080) and the danger-zone delete-account button (edit.php line 870, profile-edit-danger-zone.php) have no REST route, no JS action handler, and no wp_register_personal_data_exporter/eraser registration anywhere in includes/. The retention-days option also has no purge cron consuming it. _(grep of includes/ finds register_personal_data / wp_privacy / personal_data_export = 0 hits; allow_data_export/allow_account_deletion referenced only in Settings.php; no /me/delete or /me/export route in ProfileController.php; no deleteAccount/exportData action in assets/js/)_
- **[medium/expectation-vs-reality/confirmed-in-code]** PrivacyService::can_follow() and can_connect() are never invoked by the follow/connect REST controllers. FollowController::follow() (FollowController.php line 116) and ConnectionController (line 101) check blocks only, not the privacy preference. So who_can_follow=nobody / who_can_connect restrictions would not be enforced even if they could be set. _(includes/SocialGraph/FollowController.php lines 116-129 (blocks check only); includes/SocialGraph/ConnectionController.php line 101; grep shows can_follow/can_connect only defined in PrivacyService.php, no callers)_
- **[medium/plan-vs-reality/confirmed-in-code]** The buddynext_can_view extensibility filter promised in the master plan (line 619, 'lets addons extend visibility rules') is not implemented. PrivacyService::can_view_profile/can_view_activity return raw booleans with no apply_filters hook, so addons cannot extend visibility rules. _(docs/MASTER_DEVELOPMENT_PLAN.md line 619; includes/SocialGraph/PrivacyService.php lines 176-235 contain no apply_filters('buddynext_can_view', ...))_
- **[low/expectation-vs-reality/confirmed-in-code]** Naming/vocabulary mismatch between the two privacy subsystems risks divergence: the shipped audience enums use [everyone, members, connections, nobody] (ProfileController.php line 683) for see_email/dm/mention, while the field-level engine and PrivacyService use [public, followers, connections, private] (yaml line 57). Two parallel visibility vocabularies coexist with no unifying resolver. _(includes/Profile/ProfileController.php line 683 (everyone/members/connections/nobody) vs docs/specs/features/member-fields-search-privacy.yaml line 57 (public/followers/connections/private))_

### 🟡 Private Messaging (planned) (free) — partial

**Headline:** Messaging ships as a WPMediaVerse UI bridge exactly as the plan scoped it (no BN tables, no BN message REST), but BN's own thread/requests templates are a second, divergent server-side implementation with a confirmed broken deep-link param and a loopback-HTTP anti-pattern; standalone BN Free has no messaging.

**Plan source:** docs/MASTER_DEVELOPMENT_PLAN.md (Phase 9 "Direct Messaging (UI Bridge)", lines 815-836; integration model lines 850-855, 1006-1020); mockups docs/v2 Plans/v2/dm-list.html and dm-thread.html; spec referenced at docs/specs/features/07-direct-messaging.md (not read, referenced only)

**Plan promised:** The plan deliberately scopes messaging as a UI BRIDGE, not an owned feature: "WPMediaVerse owns the engine. BuddyNext builds the UI that consumes WPMediaVerse REST API" and "No BuddyNext tables for DM. All data lives in WPMediaVerse tables." BuddyNext Free is to ship the conversation list, chat thread, and message-requests templates matching the mockups; suppress WPMediaVerse's own chat chrome when both are active; gate DMs on bn_blocks; route new-DM events into the BuddyNext notification system; and surface an unread DM badge in nav plus DM buttons on profile/directory. The v2 mockups depict a rich DM UX (typing indicators, read receipts, online presence, message reactions, replies, voice notes, calls, attachments, pinned/archive/mute, group threads).

**Owner expects:**
- 1-to-1 private conversations: compose, send, receive, thread history
- Inbox/conversation list with unread state and per-conversation unread counts
- Unread DM badge in the global nav so users notice new messages
- Start-a-DM entry points from member profiles and the member directory
- Block enforcement: a blocked user cannot DM you
- New-message notifications (in-app bell + email per prefs)
- Message requests / spam gate for non-connections (parity with BuddyBoss)
- Deep-linkable conversation URLs that actually open the right thread
- Privacy control: who can message me
- A coherent single messaging UI (not two divergent implementations)

**What we have:** Messaging is wired as a delegated bridge, matching the plan's intent, but with internal inconsistencies. Routing: includes/Core/PageRouter.php registers full rewrite rules for /messages/, /messages/requests/, /messages/{id}/ (register_messages_rules ~line 1098), resolves them to templates/messages/list.php, requests.php, thread.php (resolve_hub_template ~line 844), and exposes messages_url()/conversation_url(). No BuddyNext DB tables for messages exist (includes/Core/Installer.php CREATE TABLE block 337-723 has bn_connections/bn_blocks/bn_notifications etc. but NO bn_messages/bn_conversations) — consistent with the plan. No MessageController in the REST layer (includes/REST/Router.php imports/registers no messaging controller; assets/js/messages/store.js is an intentionally-empty stub) — also per plan. The base /messages/ route (templates/messages/list.php) is a thin shell: it detects WPMediaVerse and either shows a dependency notice or fires do_action('buddynext_render_messages'), which includes/Bridges/WPMediaVerseBridge.php::render_messages() answers by enqueuing MVS assets and rendering MVS's own Interactivity-API two-pane chat (chat-list.php + chat-conversation.php). The bridge also wires mvs_buddynext_active, mvs_can_send_message block-gating against bn_blocks (check_block), and routes mvs_message_sent into bn_notifications as bn.new_message plus fires buddynext_dm_sent/buddynext_dm_received. DM entry points exist: templates/profile/connections.php (Message button ~line 206) and templates/directory/members.php pass a messages_base URL. By contrast templates/messages/thread.php and requests.php are a SEPARATE, parallel server-side implementation that fetches MVS REST (mvs/v1/me/conversations, mvs/v1/conversations/{id}/messages) via wp_remote_get and render their own markup — they do not use the delegated MVS Interactivity store.

**Gaps:**
- **[high/expectation-vs-reality/confirmed-in-code]** Deep-link to a conversation is broken: PageRouter maps /messages/{id}/ to the query var bn_conv_id (register_messages_rules, includes/Core/PageRouter.php ~line 1108), but templates/messages/thread.php line 37 reads $conv_id = absint($_GET['conversation'] ?? 0). Nothing copies bn_conv_id into $_GET['conversation'] (grep across includes/ and templates/ finds the only read is that one line), so a clean-URL deep link yields conv_id=0 and no thread is fetched/rendered. _(includes/Core/PageRouter.php (bn_conv_id, lines ~1108 and ~844); templates/messages/thread.php line 37)_
- **[high/expectation-vs-reality/confirmed-in-code]** Without the WPMediaVerse plugin active, BuddyNext Free has zero messaging: list.php shows a 'Dependency required' notice (templates/messages/list.php lines 42-53) and thread.php/requests.php gate on class_exists('WPMediaVerse\\Core\\Plugin'). This is the documented design (plan line 836 'All data lives in WPMediaVerse tables'), but a site owner evaluating BuddyNext Free standalone gets no private messaging at all — a parity gap vs BuddyPress/BuddyBoss which ship messaging in-core. _(templates/messages/list.php lines 29-53; templates/messages/thread.php line 31; docs/MASTER_DEVELOPMENT_PLAN.md line 836)_
- **[medium/both/confirmed-in-code]** Two divergent messaging UIs ship simultaneously. The base /messages/ route delegates the entire chat to WPMediaVerse's Interactivity store (templates/messages/list.php -> buddynext_render_messages -> WPMediaVerseBridge::render_messages), but /messages/{id}/ and /messages/requests/ are a hand-rolled server-side REST-consuming implementation (templates/messages/thread.php, requests.php). They don't share state or markup, so navigating from the delegated list into a thread crosses into an unrelated UI — fragile and a maintenance/UX hazard. _(templates/messages/list.php lines 55-67; includes/Bridges/WPMediaVerseBridge.php render_messages() lines 147-176; templates/messages/thread.php lines 62-128)_
- **[medium/expectation-vs-reality/confirmed-in-code]** No dedicated unread-DM badge in the global nav. templates/partials/nav.php computes only bn_unread_notifs (lines 58-137) and the badge data-wp-text binds to that single notifications count; there is no separate per-feature unread DM pill as the master plan describes (line 332/833) and the mockups emphasize. New DMs do create bn.new_message notifications via the bridge, so they fold into the generic bell, but a distinct Messages-link unread count is not wired here. _(templates/partials/nav.php lines 58-137; docs/MASTER_DEVELOPMENT_PLAN.md lines 332,833)_
- **[medium/plan-vs-reality/needs-live-verification]** Rich DM UX promised by the mockups (typing indicators, read receipts, online presence, message reactions, replies, voice notes, calls, attachments, pinned/archive/mute, group threads per dm-list.html/dm-thread.html) is entirely owned by WPMediaVerse at runtime; BuddyNext code does not implement it. Whether each of these renders correctly inside BuddyNext's hub shell depends on the live WPMediaVerse build and the MVS Interactivity store — cannot be confirmed from the BuddyNext repo alone. _(docs/v2 Plans/v2/dm-list.html, dm-thread.html (feature labels); includes/Bridges/WPMediaVerseBridge.php (delegates to MVS partials, does not implement these))_
- **[medium/expectation-vs-reality/confirmed-in-code]** thread.php/requests.php perform synchronous server-side wp_remote_get loopback calls to the site's own mvs/v1 REST API on every page render, forwarding the raw Cookie header (templates/messages/thread.php lines 72-113, requests.php line ~40). This is an anti-pattern (internal HTTP loopback per request, 5s timeout, cookie passthrough) that can hang or fail under loopback-blocked hosting; the delegated list.php path avoids it by using the client-side store. Functional impact depends on host loopback config. _(templates/messages/thread.php lines 72-113; templates/messages/requests.php line ~40)_

### 🟢 Activity Feed (free) — shipped

**Headline:** Activity Feed is fully shipped end-to-end (REST + service + templates + Interactivity-wired UX); remaining gaps are an absent admin activity-management/announcement screen and some SQL duplicated in templates vs FeedService.

**Plan source:** docs/journeys/activity-feed.md (detailed journey + REST surface); docs/v2 Plans/v2/home-feed.html and explore-feed.html (mockups); docs/MASTER_DEVELOPMENT_PLAN.md (Phase 3 Activity Feed marked "Verified 2026-03-23", plus "The Standard We Are Building To").

**Plan promised:** The journey doc specifies a complete activity feed: create text/poll/link/photo/file posts, poll voting, emoji reactions (toggle), comments (create/soft-delete), shares, bookmarks, and four feed surfaces (home, explore, profile, space) with cursor pagination, secret-space gating, anonymous explore access, and an empty home-feed fallback. It enumerates a precise REST surface under buddynext/v1. The v2 mockups add: composer, 4 home filter tabs (For you / Following / Spaces / Network) with counts, reaction summary chips, inline byline Follow, trending tags + suggested spaces sidebars, an explore masonry grid with content-type filter chips and search. The master plan demands production-grade, mockup-exact UX with no partial implementations.

**Owner expects:**
- Members can compose posts (text, photo/media, link, poll) and see them appear in feeds
- Personalized home feed (own + followed + joined spaces + followed hashtags) with chronological cursor pagination and infinite scroll
- Public explore feed accessible to guests
- Per-post engagement: emoji reactions (toggle), threaded comments with soft-delete, shares, bookmarks
- Poll voting with one-vote-per-user enforcement and live percentages
- Privacy/visibility honored (public/followers/connections/space_members/private) and secret-space gating
- Suspended / shadow-banned authors excluded from feeds
- Empty states rather than errors for new users
- A way for admins to moderate/remove reported posts and to post site-wide announcements

**What we have:** Full vertical slice is shipped and UX-wired. REST: includes/Feed/FeedController.php registers /feed/home, /feed/counts, /feed/explore, /users/{id}/feed, /spaces/{id}/feed, plus pre-rendered HTML pagination endpoints /feed/home/page and /feed/explore/page, and POST /feed/announcements/{id}/dismiss. Service layer includes/Feed/FeedService.php implements home_feed (with 4-filter source blending in home_source_clause: for-you/following/spaces/network), home_feed_counts (24h window), profile_feed (private-account gate via privacy service), space_feed, explore_feed, cursor encode/decode/paginate, suspended+shadow-ban exclusion (excluded_users_where), site-wide announcement prepend, and Pro extension hooks (buddynext_feed_query_args, buddynext_feed_order_by, buddynext_feed_items, buddynext_post_impression). Companion services exist: PostService.php (create/get/hydrate; announcement creation admin-gated at line 97), PollService, BookmarkService, ShareService, FeedCache (page-1 caching). Templates: templates/feed/home.php (composer + Home/Explore tabs + 4 filter tabs with Interactivity store buddynext/feed-tabs + skeleton + error + announcement banner + infinite-scroll trigger + per-filter empty states), templates/feed/explore.php (search, content-type filter chips, grid, trending tags, cursor pagination), single-post.php, bookmarks.php. templates/partials/post-card.php composes parts/post-* and is fully wired to the Interactivity API (data-wp-interactive="buddynext/post-card"). JS store assets/js/feed/store.js (2633 lines) implements stores post-card, post-composer, announcement, feed-tabs, feed, share-modal with actions toggleReactionPicker/pickReaction (POST /reactions/toggle with optimistic revert), toggleBookmark, openShare, loadComments, submitComment, votePoll, dismissAnnouncement. Moderation services exist (includes/Moderation/ModerationController.php, ModerationService.php).

**Gaps:**
- **[medium/expectation-vs-reality/confirmed-in-code]** No dedicated wp-admin screen to moderate/manage activity posts. AdminHub registers submenus for Members, Spaces, Email, Nav, Integrations, Settings (includes/Admin/*.php add_submenu_page calls) but none for the activity feed / bn_posts. Admins moderate only via front-end report flow + includes/Moderation/. Mature platforms (BuddyPress/BuddyBoss) ship a back-end Activity management table; owners expect one. _(includes/Admin/AdminHub.php, includes/Admin/Settings.php, includes/Admin/Spaces.php (no posts/activity submenu); includes/Moderation/ModerationController.php)_
- **[medium/both/needs-live-verification]** Explore template runs its own inline wpdb query and trending-tag query (templates/feed/explore.php:67-97) rather than routing through FeedService::explore_feed, so explore does NOT inherit the suspended/shadow-banned exclusion that FeedService::excluded_users_where applies. Needs runtime confirmation of whether the inline explore query independently filters banned authors. _(templates/feed/explore.php:28-97 (inline grid query); includes/Feed/FeedService.php:91-101 excluded_users_where vs explore_feed at :762)_
- **[low/both/confirmed-in-code]** Site-wide announcements have a render path (FeedService::active_announcement + home.php banner) and a dismiss REST endpoint, and PostService::create() admin-gates type='announcement' (line 97), but there is no dedicated admin UI to compose/schedule/expire an announcement (site_pin_expires_at). Creation relies on the generic composer with type=announcement, which is not surfaced as an obvious admin affordance. Owners expect a clear admin announcement composer. _(includes/Feed/PostService.php:97,148; includes/Feed/FeedService.php active_announcement(); includes/Feed/FeedController.php dismiss_announcement(); no announcement screen under includes/Admin/)_
- **[low/plan-vs-reality/confirmed-in-code]** Home-feed SQL is duplicated: templates/feed/home.php inlines the full for-you source query (lines 181-242) instead of calling FeedService::home_feed for the default tab (it only delegates to the service for non-default filters, line 144). Two copies of the same source-blend logic can drift; the journey/master-plan single-source-of-truth intent is partially violated. Functionally works today. _(templates/feed/home.php:144-242 vs includes/Feed/FeedService.php home_source_clause()/home_feed_uncached())_
- **[low/plan-vs-reality/confirmed-in-code]** v2 home mockup shows a streak widget (welcome-streak, lines 574-585) and an 'Ask AI for help' composer tool (data-tone=ai, line 391). Neither is in the Free home template; these are Pro/gamification surfaces (gamification routes through the GamificationBridge per project memory). Free parity is fine but the mockup's full surface is not all in Free. _(docs/v2 Plans/v2/home-feed.html:391,574-585; templates/feed/home.php (no streak/AI tool))_

### 🟢 Reactions (free) — shipped

**Headline:** Reactions is shipped and genuinely wired end-to-end (service, REST, post + comment + DM UX, notifications, gamification hook); the real gaps are the spec's "admin-configurable emoji set" (no config UI) and a picker that hardcodes the six types instead of consuming the filterable list.

**Plan source:** docs/specs/features/08-reactions-comments.md (linked from docs/MASTER_DEVELOPMENT_PLAN.md lines 37, 759-772). Status "Locked", "Gaps / Open Questions: None — fully locked".

**Plan promised:** The spec promises a shared reaction layer across all content surfaces (feed posts, comments, DM messages, WPMediaVerse media, Jetonomy discussion cards) backed by a single bn_reactions table keyed unique per user+object. Default emoji set is the canonical six (like/love/haha/wow/sad/angry), described as an "admin-configurable emoji set". One reaction per user per object with replace-on-change semantics, per-emoji grouped counts, a "who reacted" list on hover/tap, counts cached on the parent bn_posts row and recomputed async on drift, and reaction-on-post notifications to the post owner plus WBGamification points for receiving reactions.

**Owner expects:**
- React/unreact on feed posts and comments with one-tap toggle and replace-on-change semantics (BuddyPress/BuddyBoss parity)
- Multiple reaction types (the six emoji) selectable from a hover/click picker, not just a binary like
- Per-emoji grouped counts plus a 'who reacted' list/popover showing avatars and names
- Post owner gets notified when their content is reacted to
- Reaction state persists, is cached, and counts stay accurate (recount/drift repair)
- Gamification integration so authors earn credit for engagement received
- Reaction set surface is consistent across all content types the platform supports (posts, comments, DMs, media)

**What we have:** Data/service layer is complete and solid. includes/Reactions/ReactionService.php implements react/unreact/toggle/toggle_reaction, get_counts (per-emoji), count, get_user_emoji/get_user_reaction, has_reacted, and get_reactors (newest-first, capped at 100, with a block/restrict gate). It fires buddynext_reaction_added, buddynext_reaction_removed, and buddynext_post_reaction_received, increments/decrements bn_posts.reaction_count, and caches via wp_cache (group buddynext_reactions, 300s TTL). reaction_types() exposes the filterable buddynext_reaction_types (Pro extension point). REST: includes/Reactions/ReactionController.php registers POST buddynext/v1/reactions/toggle (auth), GET /reactions (public count+state), GET /reactions/list (hydrated reactors with display_name+avatar_url); routes wired in includes/REST/Router.php:79. DB: includes/Core/Installer.php:582 creates bn_reactions with composite PK (user_id,object_type,object_id), object_reactions index, emoji default 'like', created_at. UX is wired: templates/parts/post-actions.php renders a 6-emoji picker bound to the Interactivity store (data-wp-on--click=actions.setReaction); assets/js/feed/store.js and spaces/hashtags stores POST to /reactions/toggle and build comment-level pickers (object_type 'comment'); templates/parts/post-reaction-summary.php renders grouped top-reaction chips; templates/parts/dm-message.php renders message reactions. Notifications: includes/Notifications/NotificationListener.php:222 on_reaction_added notifies the post owner (grouped, post-only). Gamification routes via buddynext_post_reaction_received. Admin (includes/Admin/Settings.php) exposes an emoji-picker enable toggle and a "reaction on post" notification toggle.

**Gaps:**
- **[medium/plan-vs-reality/confirmed-in-code]** Spec promises an 'admin-configurable emoji set' but there is no admin UI to configure which reaction types are enabled. The only reaction-related admin controls are buddynext_enable_emoji_picker (composer/comment emoji picker, unrelated) and buddynext_notif_default_reaction. The reaction type list is fixed in ReactionService::REACTION_TYPES and only mutable in code via the buddynext_reaction_types filter — no settings screen feeds it. _(includes/Admin/Settings.php (grep: only buddynext_enable_emoji_picker + buddynext_notif_default_reaction; no reaction-set config); includes/Reactions/ReactionService.php:175-202 (hardcoded REACTION_TYPES, filter is the only extension point))_
- **[medium/both/confirmed-in-code]** The post-card reaction picker hardcodes the six emoji labels in the template instead of iterating ReactionService::reaction_types(). The filterable buddynext_reaction_types extension point exists at the data layer but the picker UI does not consume it, so a Pro-registered 7th reaction would be accepted/stored by the service but never appear in the picker. API-level extensibility is present; UX-level extensibility is not wired. _(templates/parts/post-actions.php:115-133 (static $reaction_labels array of 6, foreach renders them); grep shows no template/JS consumer of buddynext_reaction_types — only ReactionService.php and assets/emoji/README.md reference it)_
- **[medium/expectation-vs-reality/needs-live-verification]** The 'who reacted' list endpoint (GET /reactions/list) and get_reactors() exist and are hydrated with avatars/names, but I found no JS that calls /reactions/list to render a hover/tap reactor popover. post-reaction-summary.php renders grouped count chips only. The popover UX wiring could not be confirmed in code — endpoint presence does not prove the popover is built. _(includes/Reactions/ReactionController.php:81-108,167-193 (list endpoint + hydration present); grep for 'reactions/list'/'reactor'/'whoReacted' across templates+assets/js returned only post-reaction-summary.php and feed/store.js mentions, with no fetch to /reactions/list found in the store reaction code paths read)_
- **[low/expectation-vs-reality/confirmed-in-code]** Reaction notifications and the cached bn_posts.reaction_count increment only apply when object_type === 'post'. Comments, DM messages, and bridged media/discussion reactions get no owner notification and no cached parent count (counts for those are computed live via count()/get_counts()). This matches spec for posts but means comment-reaction notifications are absent — BuddyBoss notifies on comment reactions. _(includes/Reactions/ReactionService.php:63-71,83-107,146-154 (post-only count + post_reaction_received); includes/Notifications/NotificationListener.php:222-225 (returns early unless object_type === 'post'))_
- **[low/plan-vs-reality/needs-live-verification]** Spec lists WPMediaVerse media (mvs_media) and Jetonomy discussion cards (jt_discussion) as reaction surfaces via bridges. The generic service supports any object_type, but the bridge wiring (mvs_owns_reactions disable, jt_discussion surface reactions) lives outside includes/Reactions/ and was not inspected here; presence of the generic API does not confirm the bridges are wired and rendering. _(docs/specs/features/08-reactions-comments.md (Addon Behavior section); includes/Reactions/ is object_type-agnostic but contains no bridge code — bridges would be in includes/Bridges/ (not read in this audit))_

### 🟢 Hashtags (free) — shipped

**Headline:** Hashtags is fully shipped and UX-wired (extraction, autolinking, per-tag page with sort tabs, follow, trending widget/block, #-typeahead, async indexing, cron reconcile); remaining gaps are documentation-vs-code REST contract mismatches and a couple of low-severity parity items.

**Plan source:** docs/journeys/hashtags.md (dedicated journey doc); quality bar from docs/MASTER_DEVELOPMENT_PLAN.md "The Standard We Are Building To"

**Plan promised:** The journey doc promises: hashtag extraction from post content into bn_hashtags + bn_post_hashtags (idempotent upsert, lowercase slug normalization, banned-word filtering), follow/unfollow into bn_hashtag_follows with follower_count maintenance, a trending list, a hashtag-detail-plus-feed endpoint, and a list/search endpoint. It documents a REST surface of GET /hashtags (list with ?q=), GET /hashtags/trending, GET /hashtags/{slug} (detail + post feed), and POST /hashtags/{slug}/follow (toggle). It also names extension hooks buddynext_index_hashtags (bridge entry) and buddynext_hashtag_related_discussions (Jetonomy extension point, no Free consumer).

**Owner expects:**
- Typing #tag in a post auto-creates/links the tag and the tag becomes a clickable link in the rendered post
- A per-hashtag page listing posts that use the tag, with sane privacy scoping (no private/followers-only posts leaking)
- A trending hashtags surface (widget/sidebar/block) that reflects recent activity, not just lifetime totals
- Follow/unfollow a hashtag and have followed-tag content surface in the home feed
- Hashtag autocomplete/typeahead while composing
- Admin moderation control over disallowed tags
- Counts (post_count, follower_count) stay correct as posts are added/deleted and reconcile via a scheduled job

**What we have:** Fully built and UX-wired end to end. Service: includes/Hashtags/HashtagService.php (extract, register, sync with stale-link removal + post_count recompute, follow/unfollow with follower_count delta, is_following, autocomplete, get_by_slug, get_feed with public+published scoping, get_trending using a rolling 24h window on bn_post_hashtags.created_at). Async indexing: includes/Hashtags/HashtagListener.php hooks buddynext_post_created and buddynext_index_hashtags, dispatches via Action Scheduler (as_enqueue_async_action) with inline do_action_ref_array fallback, fires buddynext_hashtag_used per tag for gamification. REST: includes/Hashtags/HashtagController.php registers /hashtags/trending, /hashtags/autocomplete, POST+DELETE /hashtags/{slug}/follow, and GET /hashtags/{slug}; wired in includes/REST/Router.php:81. Listener wired in includes/Core/Plugin.php:195. DB: bn_hashtags, bn_post_hashtags (KEY hashtag_feed), bn_hashtag_follows created in includes/Core/Installer.php:611-636. Page route: includes/Core/PageRouter.php registers /activity/hashtag/{slug}/ (rewrite ~line 938) -> templates/hashtags/feed.php with Latest/Top/Following sort tabs (direct bn_posts queries) plus hashtag-hero/sidebar/posts-list parts under templates/parts/hashtag-*.php. Autolinking: buddynext_format_content() in buddynext.php:268-343 turns #tag into <a class="bn-hashtag" href="/activity/hashtag/{slug}/">, rendered via templates/parts/post-body.php. JS: assets/js/hashtags/store.js (follow toggle, optimistic UI) and assets/js/feed/store.js (#-typeahead hitting /hashtags/autocomplete, line ~2295). Trending widget includes/Widgets/TrendingHashtagsWidget.php + block bn-trending-hashtags. Counts reconciled by includes/Core/CronService.php handle_trending_hashtags and includes/Core/CounterService.php recount methods. Followed-tag content surfaces in home feed via includes/Feed/FeedService.php:342-345. Admin: banned-hashtags textarea in includes/Admin/Settings.php (option buddynext_banned_hashtags).

**Gaps:**
- **[medium/plan-vs-reality/confirmed-in-code]** Documented REST surface diverges from shipped routes. The journey doc walks GET /hashtags (list with ?q= search) and treats GET /hashtags/{slug} as returning the hashtag detail PLUS a post feed. In code there is no plain GET /hashtags list route (only /hashtags/autocomplete serves prefix search), and HashtagController::get_by_slug returns ONLY the hydrated hashtag object, not the posts array. The post feed lives behind the page template (templates/hashtags/feed.php) / FeedService, not the documented /hashtags/{slug} REST call. A consumer following the doc's REST contract literally would get a different shape than promised. _(includes/Hashtags/HashtagController.php:113-213 (only get_by_slug, no posts, no list route); docs/journeys/hashtags.md lines 143-159 and 206-213)_
- **[medium/expectation-vs-reality/needs-live-verification]** Post deletion / count decrement on delete is not provably wired here. sync() recomputes post_count for affected tags when a post is re-synced or its tags change, and CronService.handle_trending_hashtags reconciles counts on schedule, but I did not confirm a delete hook that removes bn_post_hashtags rows (or decrements post_count) at the moment a post is deleted. The doc itself flags this edge case as 'verify actual behavior'. Cannot prove a leak from reading these files alone. _(includes/Hashtags/HashtagService.php:212-330 (sync recompute path); includes/Core/CronService.php:183-206 (scheduled reconcile); no post-deletion cleanup observed in includes/Hashtags/)_
- **[low/plan-vs-reality/confirmed-in-code]** Trending semantics differ from the journey doc's stated/queried model. The doc's verification queries and step 11 order trending by lifetime post_count (ORDER BY post_count DESC). The shipped get_trending() instead ranks by recent_count over a rolling 24h window (INNER JOIN bn_post_hashtags WHERE created_at >= NOW()-24h), so tags with no activity in the last 24h never appear regardless of lifetime post_count. This is arguably better behavior but contradicts the documented expectation and the doc's own SQL. _(includes/Hashtags/HashtagService.php:520-553 (24h window) vs docs/journeys/hashtags.md lines 138-141 and 199-203)_
- **[low/plan-vs-reality/confirmed-in-code]** Follow endpoint is non-idempotent in its response, not a true toggle as the doc describes. The doc (steps 6/9/10) treats POST /hashtags/{slug}/follow as a toggle returning {following:false} on the second call. In code POST always calls follow() and returns {following:true}; unfollow is a separate DELETE verb. The JS store (assets/js/hashtags/store.js) does pick POST vs DELETE based on current aria-pressed state, so the actual UI works, but the documented toggle-via-repeated-POST behavior is not what the controller implements. _(includes/Hashtags/HashtagController.php:164-196 (POST=>follow always true, DELETE=>unfollow); docs/journeys/hashtags.md lines 117-124)_
- **[low/expectation-vs-reality/confirmed-in-code]** No admin management screen for hashtags (browse/merge/rename/delete/view counts). Only a banned-hashtags textarea exists. Neither the journey doc nor the master plan explicitly promised an admin grid, so this is an expectation-vs-reality item against mature-platform parity, not a plan miss. Banning is also enforced only at extract()/register() time, so already-stored tags are not retroactively purged. _(includes/Admin/Settings.php:69,841-844 (only buddynext_banned_hashtags option); no admin hashtag screen found under includes/Admin)_

### 🟡 Member Directory (free) — partial

**Headline:** Core directory shipped and well-wired (reactive grid, search, sorts, relation/type filters, viewer-aware exclusions, follow/connect/moderation, admin member-type CRUD); gaps are the unrendered Grid/List view toggle (dead JS), an unexposed location filter, no map view, and dual server/REST read paths with divergent sort vocabularies.

**Plan source:** docs/journeys/member-directory.md (journey/test script) + docs/v2 Plans/v2/member-directory.html (HTML mockup) + docs/MASTER_DEVELOPMENT_PLAN.md ("The Standard We Are Building To")

**Plan promised:** The journey doc frames the directory primarily around member-type CRUD and viewer-aware exclusions (blocked/shadow-banned/suspended users hidden), with listing routed through search and filterable by member_type, plus pagination. The v2 mockup specifies a richer UX: a member-type pill row, a filter strip (search + sort + online toggle), a 4-column responsive member grid with Follow/Connect actions, and a Grid/List/map view toggle. The master plan demands every UI match the mockup exactly with no partial implementations.

**Owner expects:**
- Browse all members in a paginated grid with avatar, name, bio, member-type badge
- Free-text search by name/handle/profile fields, privacy-aware (no leaking private field values)
- Filter by member type, and sort (newest, alphabetical, most active, online now)
- Relationship filters: All / Following / Connections
- Online-now indicator and an online-only filter
- Per-card actions: Follow/Connect with state machine, plus Mute/Block/Report
- Blocked, suspended, and shadow-banned users excluded viewer-aware
- Admin can create/edit/delete member types and assign them to users
- Reactive UX (no full reload) matching the mockup, with loading/empty/error states

**What we have:** Service: includes/Profile/MemberDirectoryService.php — list_members() does keyset (cursor) pagination with sorts newest/alphabetical/most_active/online; filters search (name/login + privacy-safe bn_field_* mirrors), location, member_type (via bn_member_type usermeta), space_id, connection_status, online_only; excludes self, suspended (bn_user_suspensions), shadow-banned (bn_shadow_banned usermeta), and bidirectional blocks (bn_blocks); computes follower_count and mutual_connection_count; 60s result cache. matching_user_ids() shares the search surface for the server render. REST: includes/Profile/MemberDirectoryController.php registers GET buddynext/v1/members (public callback) with search/sort/relation/member_type/location/online/cursor/per_page args; shape_item() returns display_name, handle, avatar_url, profile_url, messages_url, bio_excerpt, member_type, follow/connection state, mutual_count, is_online; 'following' relation resolved post-query. Registered in includes/REST/Router.php:74; service bound in includes/Core/Plugin.php:640. Template: templates/directory/members.php builds a server-rendered WP_User_Query path (with the same exclusions and search), composes parts/member-directory-{hero,tabs,filter-bar,grid}.php, member-card.php, block/report modals, empty/error/skeleton states, and online-now + by-type sidebar widgets. JS store: assets/js/members/store.js (833 lines) implements reactive fetch of /members, debounced search, sort/relation/online query params, follow/connect/accept/decline, mute/block/report, toasts, optimistic rollback; enqueued via includes/Core/AssetService.php:318. Admin: includes/Admin/Members/MemberTypesManager.php provides member-type CRUD + assignment via admin_post handlers, plus member-type REST endpoints referenced in the journey.

**Gaps:**
- **[medium/both/confirmed-in-code]** Grid/List view toggle is implemented in JS but no template renders it. store.js has setGridView/setListView/isListView/isGridPressed and targets '.bn-md-filters__view .bn-btn', but grep across templates/ for 'bn-md-filters__view', 'data-view', 'view-toggle' returns nothing; hero.php and grid.php both label view_mode 'Reserved for future grid/list view modes'. The v2 mockup (member-directory.html lines 252-253) shows an active Grid/List view toggle. Result: dead view-switch code with no UI to drive it. _(assets/js/members/store.js:435-436 (setGridView/setListView), :56 ('.bn-md-filters__view .bn-btn'); templates/parts/member-directory-hero.php:17; templates/parts/member-directory-grid.php:17; docs/v2 Plans/v2/member-directory.html:252)_
- **[medium/expectation-vs-reality/confirmed-in-code]** Two parallel directory read paths exist with divergent sort vocabularies and pagination models: the server template uses WP_User_Query with orderby registered|display_name|post_count and offset pagination, while the REST service uses cursor keyset pagination with sorts newest|alphabetical|most_active|online. The template maps post_count->most_active and has no 'online' sort/online-only on initial server render, so first paint and subsequent live fetches can differ. Maintenance/consistency risk rather than a hard break. _(templates/directory/members.php:63-65,136-211 (WP_User_Query, allowed_sort) vs includes/Profile/MemberDirectoryService.php:54-285 (cursor + online sort))_
- **[medium/expectation-vs-reality/needs-live-verification]** Member-type assignment delete is documented (journey REST surface lists DELETE /users/{id}/member-type and notes orphaned assignment rows have no CASCADE) — the journey itself flags orphan cleanup as an open question. Whether deleting a member type cleans up bn_member_type_assignments and the bn_member_type usermeta fast-read mirror (which the directory filters on) is not verifiable from the files read; stale usermeta would make deleted types still filterable. _(docs/journeys/member-directory.md:191 (orphan/no-CASCADE note); includes/Profile/MemberDirectoryService.php:217-222 (filter relies on bn_member_type usermeta))_
- **[low/expectation-vs-reality/confirmed-in-code]** Location filter is supported by the service (MemberDirectoryService location LIKE on bn_field_location) and accepted by the REST controller, but no UI exposes it. The filter-bar part has no location input (grep for 'location' in member-directory-filter-bar.php returns nothing). API-level present, UX-level unwired. _(includes/Profile/MemberDirectoryService.php:104-108,212-215; includes/Profile/MemberDirectoryController.php:77-81; templates/parts/member-directory-filter-bar.php (no location markup))_
- **[low/plan-vs-reality/confirmed-in-code]** Map view shown in the v2 mockup (view-toggle includes a map option, mockup references 'map' x3) is not implemented anywhere — no map view in store.js (only grid/list) and no template. Plan-vs-reality omission against the mockup. _(docs/v2 Plans/v2/member-directory.html (grep 'map' = 3 hits incl. view-toggle); assets/js/members/store.js readView only returns 'list'|'grid')_
- **[low/expectation-vs-reality/needs-live-verification]** REST /members endpoint is fully public (permission_callback => __return_true) and returns the same viewer-aware set, but member visibility/privacy gating beyond block/suspension/shadow-ban (e.g. site-level 'members must be logged in to browse' or per-profile visibility) is not enforced in the controller. Mature platforms (BuddyPress/BuddyBoss) offer a 'restrict directory to logged-in' option. Cannot confirm absence of a higher-level gate from the files read. _(includes/Profile/MemberDirectoryController.php:55 (permission_callback __return_true))_

### 🟢 Connections (free) — shipped

**Headline:** Connections is shipped end-to-end (service + REST + wired Interactivity UI); gaps are polish/parity edges — unwired connect-note, no requests-inbox list page, no admin screen, and unverified notification + DB-unique-key.

**Plan source:** /Users/vapvarun/dev/repos/buddynext/docs/journeys/social-graph.md (dedicated journey doc covering Follow + Connections + Block); quality bar from /Users/vapvarun/dev/repos/buddynext/docs/MASTER_DEVELOPMENT_PLAN.md "The Standard We Are Building To"

**Plan promised:** The social-graph journey doc specifies a mutual-connection (friendship) system on the bn_connections table with a pending -> accepted / declined lifecycle plus requester withdraw, an optional connect note, and symmetric status. It promises REST routes for connect (send/withdraw), accept, decline, listing accepted connections, and listing pending received requests, all requiring auth, with a UNIQUE pair key preventing duplicates and block-prevents-connect enforcement. The master plan demands production-ready code with UI matching the v2 mockups exactly (premium UX as baseline, not a follow-up).

**Owner expects:**
- Members can send, accept, decline, withdraw, and remove connection requests from real on-site UI (buttons on profiles/member directory), not just curl
- Connection state is reflected live on the button (Connect / Pending / Accept+Decline / Connected) without a page reload
- Blocked users cannot send connection requests; self-connect is rejected; duplicate requests are prevented
- A member can browse their own connections list and a pending-requests inbox
- Connection counts and mutual-connections surface on profiles and the member directory
- New connection requests generate a notification to the recipient
- Connection data feeds privacy/visibility (e.g. connections-only content) consistent with BuddyPress/BuddyBoss friendship parity

**What we have:** Service layer is complete in /Users/vapvarun/dev/repos/buddynext/includes/SocialGraph/ConnectionService.php: send_request (self-connect guard, existing-pair guard, 280-char note sanitize+cap, fires buddynext_connection_requested), accept_request, decline_request, withdraw_request, remove_connection, plus read helpers status(), connections(), pending_sent(), pending_received(), mutual_connections(), connection_degree(), connection_count() — all cache-backed (group buddynext_connections, 10min TTL, wp_cache_flush_group on write). REST controller /Users/vapvarun/dev/repos/buddynext/includes/SocialGraph/ConnectionController.php registers POST/DELETE /users/{id}/connect, POST /users/{id}/connect/accept, POST /users/{id}/connect/decline, GET /me/connections, GET /me/connection-requests; all gated by require_auth (is_user_logged_in, 401 otherwise); send_request enforces block-prevents-connect via buddynext_service('blocks')->is_blocking_either(). UX is wired: template /Users/vapvarun/dev/repos/buddynext/templates/blocks/connection-button.php renders the 4 states with data-wp-interactive="buddynext/connection-button" and data-wp-on--click bindings; the frontend store /Users/vapvarun/dev/repos/buddynext/assets/js/social/follow-store.js (line 194) implements sendRequest/withdrawRequest/acceptRequest/declineRequest/disconnect that fetch the exact REST routes with X-WP-Nonce and optimistic state. Connections list page /Users/vapvarun/dev/repos/buddynext/templates/profile/connections.php (direct JOIN query, paginated 12/page) and a removeConnection store in /Users/vapvarun/dev/repos/buddynext/assets/js/connections/store.js. Member directory /Users/vapvarun/dev/repos/buddynext/templates/directory/members.php uses connections() and mutual_connections() for a "connections" relation filter and mutual-count display.

**Gaps:**
- **[medium/expectation-vs-reality/needs-live-verification]** No dedicated 'connection requests inbox' page template was found (only profile/connections.php for accepted connections and the GET /me/connection-requests REST endpoint). The recipient can accept/decline inline via the per-profile connection-button, but a consolidated incoming-requests screen (BuddyPress 'Friendship Requests' parity) is not evidenced in templates/. The endpoint exists; the UX surface for browsing pending received requests as a list is unconfirmed. _(templates/profile/ contains connections.php/followers.php/following.php but no requests/inbox template; ConnectionController.php line 80-88 exposes /me/connection-requests but no template consumes it that was found)_
- **[medium/expectation-vs-reality/needs-live-verification]** Notification on a new connection request is not verifiable from the inspected files. The service fires the buddynext_connection_requested action (the correct extension point) but whether a notification listener is subscribed (and an email/in-app notice sent to the recipient) is outside ConnectionService/Controller; not confirmed in code here. _(/Users/vapvarun/dev/repos/buddynext/includes/SocialGraph/ConnectionService.php line 112 (do_action buddynext_connection_requested) — no consumer inspected in this audit)_
- **[low/expectation-vs-reality/confirmed-in-code]** The optional connect note is fully supported server-side (ConnectionService::send_request param + sanitize + 280 cap + passed into the buddynext_connection_requested hook, and ConnectionController reads request param 'note') but the frontend sendRequest() action in follow-store.js sends no body/note — it POSTs with only the nonce header. So the LinkedIn-style note the code was built for is unreachable from the shipped UI. _(/Users/vapvarun/dev/repos/buddynext/assets/js/social/follow-store.js line 204-220 (no note in fetch); /Users/vapvarun/dev/repos/buddynext/includes/SocialGraph/ConnectionController.php line 111; ConnectionService.php line 47-63)_
- **[low/expectation-vs-reality/needs-live-verification]** The buddynext/connection-button Interactivity store is defined twice: once in assets/js/blocks.js (line 230) and once in assets/js/social/follow-store.js (line 194). blocks.js is registered as the block editor script (serverSideRender) per BlockRegistrar.php, so on the front end only follow-store.js should run; but if both bundles ever load on the same request, the second store() registration for the same namespace is a duplicate-registration hazard. Could not prove both load together on the front end without runtime check. _(/Users/vapvarun/dev/repos/buddynext/assets/js/blocks.js line 230; /Users/vapvarun/dev/repos/buddynext/assets/js/social/follow-store.js line 194; /Users/vapvarun/dev/repos/buddynext/includes/Blocks/BlockRegistrar.php line 91-92)_
- **[low/expectation-vs-reality/confirmed-in-code]** No admin-side moderation/management screen for connections. grep of includes/Admin/ shows Settings, EmailEditor, NavManager, ProfileFieldsManager but nothing referencing the connections graph. A site owner cannot view, audit, or sever member connections from wp-admin. Mature platforms expose at least counts/management. This may be by design (member-managed only) but no admin surface exists in code. _(includes/Admin/ grep for 'connection' returns only unrelated files (Settings.php, EmailEditor.php, NavManager.php, Members/ProfileFieldsManager.php))_
- **[low/plan-vs-reality/needs-live-verification]** Journey doc lists GET /users/{id}/following, /users/{id}/followers, /follow-suggestions, /me/blocked, /me/muted as the broader 'REST surface walked', but for Connections specifically the doc's verification steps assume a UNIQUE KEY 'pair' on bn_connections to block duplicates, while the shipped ConnectionService::send_request enforces duplicate prevention in PHP via a pre-SELECT, not (verified) a DB unique constraint. If the schema lacks the UNIQUE key, concurrent requests could race a duplicate row. Schema file not inspected in this audit. _(docs/journeys/social-graph.md line 151 (claims UNIQUE KEY pair); ConnectionService.php line 68-87 (PHP-level existence check, no constraint reliance))_

### 🟢 Spaces (Communities) (free) — shipped

**Headline:** Spaces is shipped end-to-end (services, REST, routing, templates, Interactivity wiring, admin, schema); the only real gaps are sub-spaces being backend-only with no UX and missing admin category management.

**Plan source:** docs/MASTER_DEVELOPMENT_PLAN.md (Phase 5 — Spaces, plus "The Standard We Are Building To" and admin checklist lines 297, 715-725); docs/v2 Plans/v2/spaces-directory.html; docs/v2 Plans/v2/space-home.html. Spec referenced but not opened: docs/specs/features/03-spaces.md.

**Plan promised:** The plan promised three space types (Open=instant join, Private=request, Secret=invite-only), an Owner/Moderator/Member role hierarchy extensible via filter, sub-spaces nested one level deep via bn_spaces.parent_id, space categories, a spaces directory and a space-home surface (Feed/Members/Media/About tabs) matching the two HTML mockups exactly, space-scoped moderation/bans, per-space notification prefs, and a wp-admin Spaces management page (owner, type, member count, pending requests, archive/delete). Quality bar: no partial implementations, UI matches mockups, production-grade.

**Owner expects:**
- Create/edit/delete a community with name, slug, description, cover, avatar, type, category
- Three visibility types with correct join semantics (open=instant, private=request+approval, secret=invite-only and hidden from directory/search)
- Browse a spaces directory with search, category filter, type filter, sort, pagination
- A space home page with a scoped activity feed, member roster, media, and about/rules
- Join/leave/request flows that reflect state without a page reload
- Owner/moderator management: approve/decline requests, invite, change roles, remove, ban/unban, transfer ownership
- Per-space notification preferences
- An admin overview to find and remove problem spaces
- Manage the category taxonomy from the admin

**What we have:** Fully built end-to-end. Services: includes/Spaces/SpaceService.php (CRUD, get_by_slug, list/search, hydrate exposing avatar_url/cover_image_url/rules, type_label, 2-level parent_id depth guard), includes/Spaces/SpaceMemberService.php (join/request_join/invite/approve/decline/leave/ban/unban/remove/change_role, notification prefs, hard-ban table, block-aware roster, suspended-user exclusion via callers), includes/Spaces/SpaceCategoryController.php (REST GET/POST/DELETE categories, manage_options gated). REST: includes/REST/Router.php lines 76-77 registers both controllers under buddynext/v1; SpaceController.php exposes ~20 routes incl. members, pending-requests, join/leave, invite, approve/decline, ban/unban, role change, remove, transfer, notification-pref, permissions. Routing: includes/Core/PageRouter.php routes /spaces/ (directory), /spaces/{slug}/ (home), /members, /settings, /moderation, /admin, enqueues spaces assets and localizes URL base. Templates: templates/spaces/{directory,home,members,settings,moderation}.php plus templates/blocks/{space-directory,space-card,my-spaces}.php and ~10 space-settings-panel parts; home.php renders Feed/Members/Media/About (+Moderation) tabs and a space-scoped bn_posts feed with access gating for private/secret. UX wiring confirmed: templates carry data-wp-interactive="buddynext/spaces" + data-wp-on--click directives bound to assets/js/spaces/store.js and space-members/store.js; create-space modal (templates/partials/create-space-modal.php) posts name/slug/type/category_id; settings general panel has cover upload + rules fields. Admin: includes/Admin/Spaces.php (directory list with owner/type/member-count, type filter, search, delete via admin_post_bn_delete_space). Schema: includes/Core/Installer.php lines 473-516, 839 — bn_spaces (incl. parent_id, required_ability, accent_color, description_layout, rules), bn_space_members, bn_space_categories, bn_space_bans.

**Gaps:**
- **[medium/plan-vs-reality/confirmed-in-code]** Sub-spaces are backend-only with no UX path. bn_spaces.parent_id exists (Installer.php:479), SpaceService::create enforces a 2-level depth limit (SpaceService.php:152-167) and hydrate returns parent_id, but SpaceController::create_space (lines 439-512) never reads a parent_id param, the create-space modal has no parent field, and no template references parent_id. The plan (MASTER_DEVELOPMENT_PLAN.md:718-719, 'Sub-spaces One level deep') is therefore unreachable by any user. _(includes/Spaces/SpaceController.php:439-512 (no parent_id read); includes/Spaces/SpaceService.php:152-167; templates/partials/create-space-modal.php (name/slug/type/category_id only); docs/MASTER_DEVELOPMENT_PLAN.md:718-719)_
- **[medium/expectation-vs-reality/confirmed-in-code]** No wp-admin UI to manage space categories. SpaceCategoryController exposes REST create/delete (manage_options gated), but the admin Spaces screen (includes/Admin/Spaces.php) is a directory list + type filter + delete only; it registers no category-management tab/screen. An owner can only create categories via raw REST/CLI. Front-end create modal consumes categories but cannot create them. _(includes/Spaces/SpaceCategoryController.php:7-8,31-73; includes/Admin/Spaces.php:31-72 (only registers 'directory' tab, no category screen))_
- **[low/plan-vs-reality/needs-live-verification]** Admin Spaces table lacks the 'pending requests' and 'archive' columns/actions the plan calls for. Plan line 297 specifies 'table with owner, type, member count, pending requests, archive/delete actions'. The admin render shows owner, type, member count and a delete action, but grep of includes/Admin/Spaces.php shows no pending-request count column and no archive action (only hard delete via bn_delete_space). Needs the full render confirmed but archive/pending appear absent. _(docs/MASTER_DEVELOPMENT_PLAN.md:297; includes/Admin/Spaces.php:33,259-274 (owner/type/delete present; no archive/pending grep hits))_
- **[low/expectation-vs-reality/needs-live-verification]** space-card.php membership button uses a legacy non-Interactivity wiring path (data-action="bn-join-space", data-nonce, data-current-state) rather than the data-wp-on--click directives used by directory.php/home.php. This relies on a plain DOM listener in the spaces store (resolveSpaceId/resolveNonce fallbacks exist for exactly this), so it likely works, but it is an inconsistent dual wiring model that should be verified to actually fire and update state. _(templates/blocks/space-card.php:107-112 (data-action/data-nonce, no data-wp-on); assets/js/spaces/store.js:31-50 (DOM-walk fallbacks))_

### 🟢 Notifications (free) — shipped

**Headline:** Notifications is a genuinely shipped, end-to-end feature (triggers, aggregation, REST, prefs, digests, admin template editor, front-end Interactivity store) that meets BuddyPress-parity baseline; remaining gaps are a dead digest-queue path, doc drift, and mockup-only AI/paid/announcement surfaces that are correctly out of Free scope.

**Plan source:** docs/journeys/notifications-email.md (functional journey), docs/v2 Plans/v2/notifications.html (UX mockup), docs/MASTER_DEVELOPMENT_PLAN.md ("The Standard We Are Building To"). A deeper spec also exists at docs/specs/features/06-notifications-email.md and docs/specs/NOTIFICATION-MESSAGES.md.

**Plan promised:** The journey doc promised an event-driven notification pipeline: a trigger (e.g. follow) fires buddynext_notification_created, NotificationService inserts a bn_notifications row (with group_key/group_count aggregation), EmailDispatchListener checks per-type prefs in bn_notification_prefs and dispatches via EmailSender, which writes to bn_email_log and supports immediate/daily/weekly/off frequencies plus a should_send filter. REST surface for list/unread-count/mark-read/delete/prefs. The v2 mockup promised a richer UX: type tabs (Mentions/Replies/Reactions/People/Spaces/Paid), time-grouped sections (New/Earlier today/This week), per-row inline actions (Reply, Follow back, Accept invite), Quick-filter and Stats and Muted sidecards, and an AI-digest surface (AI rows/inbox are Pro/AI scope).

**Owner expects:**
- In-app notifications for the full event set: follows, connection requests/accepts, mentions, replies/comments, reactions/likes, shares, space invites/joins/approvals, new posts in spaces
- Unread badge with live count and mark-one / mark-all read
- Per-type email preferences with immediate/daily/weekly/off, and working daily+weekly digest emails
- One-click unsubscribe from emails without logging in
- Admin-editable, enable/disable-able email templates with test send
- Aggregation so N followers/likes collapse into one row (not notification spam)
- A dedicated notifications page plus a settings/preferences page, matching the rest of the platform's UX polish
- Auth-gated REST endpoints (401 for anonymous)

**What we have:** Fully built, end-to-end wired pipeline. Triggers: includes/Notifications/NotificationListener.php subscribes to ~14 events (buddynext_user_followed, follow_requested, connection_requested/accepted, reaction_added, comment_created, post_shared, user_mentioned, space_member_joined/invited, space_join_requested/approved, space new-post) and calls NotificationService::create() with group_key per type. Creation+aggregation+filters: includes/Notifications/NotificationService.php (24h group-merge via group_key/group_count, buddynext_notification_should_send and buddynext_notification_send_at filters, buddynext_notification_created action, cursor pagination, wp_cache for unread count). Email: includes/Notifications/EmailDispatchListener.php (hooks notification_created, async via Action Scheduler, HMAC unsubscribe handler on init) + EmailSender.php (pref check, immediate/daily/weekly/off, buddynext_email_payload filter, bn_email_log write, HMAC-signed unsubscribe URL). Digests: includes/Core/CronService.php handle_daily_digest()/handle_weekly_digest(), scheduled by includes/Core/CronScheduler.php (JOB_DAILY_DIGEST daily, JOB_WEEKLY_DIGEST weekly). Prefs: NotificationPrefService.php + NotificationPrefCatalogue.php (per-type defaults). REST: NotificationController.php registers GET /me/notifications, GET /me/notifications/unread-count, PUT|POST /me/notifications/read-all, PUT|POST /me/notifications/{id}/read, DELETE /me/notifications/{id}, GET|PUT /me/notification-prefs, GET|PUT /me/notification-channels, GET|POST /me/space-notification-prefs — all behind require_auth (401 anon). Message composition: NotificationMessageService.php enriches each row with message/url/icon/tone/label. Admin: includes/Admin/EmailEditor.php (edit/enable/disable/test/reset templates, AJAX admin_post handlers). Templates seeded by includes/Core/Installer.php (~20 email templates incl. moderation, gamification, digests, invites). Front-end: includes/Core/PageRouter.php routes /notifications/, /notifications/preferences/, /settings/notifications/; templates/notifications/index.php + prefs.php; assets/js/notifications/store.js (Interactivity API, optimistic mark-read, live badge) + prefs-store.js; ~12 template parts (notification-row, filter-bar, hero, empty, sidecard-muted/quick-filters/types/prefs/recent-actors) + blocks/bn-notification-bell. DB tables bn_notifications, bn_email_log, bn_notification_prefs, bn_email_templates all used.

**Gaps:**
- **[medium/expectation-vs-reality/needs-live-verification]** v2 mockup shows per-type filter TABS (Mentions/Replies/Reactions/People/Spaces/Paid) and time-grouped sections, but the list_notifications endpoint accepts only cursor + per_page — no type/tab/filter param. Tab filtering must therefore be client-side over the loaded page, or unimplemented. Template parts for tabs/filter-bar exist (templates/parts/notifications-filter-bar.php), and store.js handles popstate for filter tabs, so filtering likely runs client-side; cannot prove server-side absence is a UX gap without running the page. _(includes/Notifications/NotificationController.php:162-190 (only cursor/per_page params) vs docs/v2 Plans/v2/notifications.html:366-375 (tabs) and templates/parts/notifications-filter-bar.php)_
- **[medium/both/confirmed-in-code]** Admin-side broadcast/announcement-to-members notification (mockup 'Acme Studio posted a new announcement') has no creation trigger in Free. NotificationListener has no admin-announcement handler. A community owner expecting to push a site-wide announcement notification would not find it here (may be Pro broadcast scope). _(includes/Notifications/NotificationListener.php:31-54 (no announcement/broadcast hook) vs docs/v2 Plans/v2/notifications.html:580-595)_
- **[low/expectation-vs-reality/confirmed-in-code]** Digest queue is written but never consumed (dead path). EmailSender::send() fires buddynext_queue_email_digest for daily/weekly prefs, and EmailDispatchListener::on_queue_email_digest() appends to usermeta buddynext_digest_queue_{freq}. But CronService::handle_daily_digest/handle_weekly_digest never read that usermeta — they re-query bn_notifications directly. The queue meta accumulates forever and is never cleared or sent. Digests still work via the direct query, so this is wasted writes / divergent state rather than broken delivery. _(includes/Notifications/EmailDispatchListener.php:109-121 (writes usermeta) vs includes/Core/CronService.php:333-389 (get_digest_user_ids + fetch_unread_notifications query bn_notifications/bn_notification_prefs directly, no usermeta read))_
- **[low/plan-vs-reality/confirmed-in-code]** get_digest_user_ids() docblock claims it filters to the global preference row to avoid duplicate digests, but the SQL selects DISTINCT user_id WHERE email_freq = %s with no type='global' filter. Any per-type pref row set to daily/weekly enrolls the user in the digest. Combined with digest_already_sent() this won't double-send, but the user-selection criterion does not match the documented intent. _(includes/Core/CronService.php:328-350 (docblock says type='global', SQL has no such WHERE))_
- **[low/plan-vs-reality/confirmed-in-code]** REST route paths in the journey doc are stale: doc walks /buddynext/v1/notifications, /notifications/mark-read, DELETE /notifications/{id}, /notification-prefs; the controller actually registers them all under /me/ (/me/notifications, /me/notifications/read-all, /me/notifications/{id}/read, /me/notifications/{id}, /me/notification-prefs). Code is the richer/correct surface (also adds /me/notification-channels and /me/space-notification-prefs not in the journey). Doc-vs-code drift only. _(docs/journeys/notifications-email.md:247-254 vs includes/Notifications/NotificationController.php:37-153)_
- **[low/plan-vs-reality/confirmed-in-code]** v2 mockup 'AI digest' rows, AI inbox, and 'Paid'/MRR notification rows are not produced by any Free trigger (NotificationListener emits no bn.ai_* or paid type). This is expected — AI/monetization are Pro/AI scope — but a site owner reading the mockup may expect them. Free correctly omits them; flag as scope clarification, not a defect. _(includes/Notifications/NotificationListener.php:31-54 (no AI/paid triggers) vs docs/v2 Plans/v2/notifications.html:386-409,558-578)_

### 🟡 Email Broadcasts (pro) — partial

**Headline:** Broadcast scaffolding is fully built and wired, but the actual send path delivers nothing — recipients are marked "sent" while no campaign email leaves the server (missing bn.broadcast template + campaign content never passed to EmailSender).

**Plan source:** /Users/vapvarun/dev/repos/buddynext-pro/docs/journeys/broadcast-email.md (dedicated journey doc) + quality bar from /Users/vapvarun/dev/repos/buddynext/docs/MASTER_DEVELOPMENT_PLAN.md "The Standard We Are Building To" (lines 97-108)

**Plan promised:** The journey doc promises a full broadcast sub-system: create/list/dispatch campaigns from an admin screen (admin.php?page=buddynextpro-broadcasts) with name/subject/HTML-body/segment/schedule; a "Send Test" button; "Send Now" dispatch that queues one recipient row per segment user (status=queued); a 5-minute cron (buddynextpro_broadcast_send_pending) that sends each queued recipient via Free's EmailSender and marks them sent; an HMAC open-tracking pixel injected by dispatch_campaign (hedged in the doc); HMAC per-campaign unsubscribe; and a REST surface under buddynext-pro/v1/broadcasts plus an /email/unsubscribe endpoint. The master plan forbids partial implementations — features ship complete or not at all.

**Owner expects:**
- Composing a campaign (subject + HTML body) and having THAT content actually delivered to recipients' inboxes
- Audience segmentation (all users, by group/space, by tag, by join date, by activity)
- Schedule-for-later and send-now, with a draft state
- A working test-send to the admin before blasting the list
- Per-recipient send status the owner can trust (sent really means delivered)
- CAN-SPAM/GDPR compliant one-click unsubscribe link inside every broadcast, plus a global broadcast opt-out
- Basic engagement reporting (opens/clicks) — parity with BuddyBoss/Mailpoet-style broadcasts
- Batch/throttled sending so large lists don't time out or trip spam limits

**What we have:** Substantial backend is built and wired at runtime. DB: wp_bn_email_campaigns + wp_bn_campaign_recipients created in /Users/vapvarun/dev/repos/buddynext-pro/includes/Core/Installer.php (lines 375-404; recipients table has opened/clicked/bounced statuses and opened_at/clicked_at columns). Service: /Users/vapvarun/dev/repos/buddynext-pro/includes/Email/BroadcastService.php implements CRUD, dispatch_campaign() (queues recipients via INSERT IGNORE, marks campaign sending, fires buddynext_pro_broadcast_dispatched, schedules cron), and send_pending() cron worker. SegmentService.php resolves all_users/by_space/by_tag/by_activity_level/by_join_date/by_member_label. BroadcastUnsubscribe.php implements HMAC token gen/verify, an init-hook query-arg handler (?bn_unsub_campaign=), profile toggle, and per-campaign + global usermeta opt-out (read in send_pending lines 430-445). Admin screen exists: /Users/vapvarun/dev/repos/buddynext-pro/includes/Admin/BroadcastAdmin.php (list, create form, Send Now, Send Test, per-campaign recipient status breakdown) registered as a Growth tab. REST: /Users/vapvarun/dev/repos/buddynext-pro/includes/Email/Controllers/BroadcastController.php (GET/POST/GET{id}/PUT/DELETE/POST{id}/dispatch, all manage_options) and EmailUnsubscribeController.php (/me/email-preferences). All wired in /Users/vapvarun/dev/repos/buddynext-pro/includes/Core/Plugin.php (lines 157-168, 284-286).

**Gaps:**
- **[critical/both/confirmed-in-code]** Broadcasts do not deliver the campaign's content. send_pending() (BroadcastService.php line 450-457) calls EmailSender::send_now($user_id, 'bn.broadcast', ['campaign_id'=>..., 'type'=>'bn.broadcast']) but never passes the campaign subject/body_html. EmailSender::send_now() (buddynext/includes/Notifications/EmailSender.php lines 104-120, 245-256) loads its subject/body from a bn_email_templates row WHERE type='bn.broadcast'. No such template is seeded (buddynext/includes/Core/Installer.php lines 86-206 list every seeded type; 'bn.broadcast' is absent), so get_template() returns null and send_now() returns early at lines 106-108 — zero email is sent. Even if a 'bn.broadcast' template existed it would render that generic template, not the per-campaign content. Meanwhile send_pending() still marks each recipient status='sent' (lines 460-469), so the admin recipient view reports success for emails that were never dispatched. _(/Users/vapvarun/dev/repos/buddynext-pro/includes/Email/BroadcastService.php lines 447-469; /Users/vapvarun/dev/repos/buddynext/includes/Notifications/EmailSender.php lines 104-120,245-256; /Users/vapvarun/dev/repos/buddynext/includes/Core/Installer.php lines 86-206)_
- **[high/both/confirmed-in-code]** No per-campaign unsubscribe link is injected into sent broadcasts. BroadcastUnsubscribe::unsub_url() exists but is never called from the send path; no add_filter('buddynext_email_payload', ...) is registered anywhere in Pro (grep across buddynext-pro returns only a comment in BroadcastService.php line 448). The generic EmailSender {{unsubscribe_url}} token points at the transactional notification opt-out (type-based), not the broadcast campaign opt-out. CAN-SPAM/GDPR compliance the doc claims is therefore not met on actual broadcast sends. _(/Users/vapvarun/dev/repos/buddynext-pro/includes/Email/BroadcastUnsubscribe.php line 117; grep for 'buddynext_email_payload' add_filter in buddynext-pro returns none; /Users/vapvarun/dev/repos/buddynext/includes/Notifications/EmailSender.php lines 184,206-218)_
- **[high/expectation-vs-reality/confirmed-in-code]** Admin segment selector only sets a bare {type:...} with no parameters; by_space/by_tag/by_join_date/by_member_label all require space_ids/tags/dates/label_ids which the admin form never collects (the form shows the note 'Advanced segment options are configured via the REST API'). So from the admin UI, any segment other than all_users resolves to zero recipients (SegmentService by_space line 96-98, by_tag line 131-133, etc. return empty array when their key is absent). Owner-facing segmentation is effectively all_users-only via the only shipped UI. _(/Users/vapvarun/dev/repos/buddynext-pro/includes/Admin/BroadcastAdmin.php lines 132-133,356-364; /Users/vapvarun/dev/repos/buddynext-pro/includes/Email/SegmentService.php lines 89-98,126-133,237-246)_
- **[high/expectation-vs-reality/confirmed-in-code]** Scheduling is recorded but never auto-dispatched. create_campaign() sets status='scheduled' with scheduled_at, and BroadcastAdmin exposes a schedule field, but no cron/handler scans for scheduled campaigns whose scheduled_at has passed to call dispatch_campaign(). send_pending() only processes campaigns already in status='sending' (BroadcastService.php lines 405-414). A scheduled campaign sits forever until an admin manually clicks Send Now. _(/Users/vapvarun/dev/repos/buddynext-pro/includes/Email/BroadcastService.php lines 121-124,334-387,399-414 (no scheduled->sending promoter))_
- **[medium/both/confirmed-in-code]** No open/click tracking implemented. The journey doc (Part 4) and schema (opened/clicked statuses, opened_at/clicked_at columns) imply engagement tracking, but no pixel is injected and opened_at/clicked_at are never written anywhere in includes/Email/ (grep for track/pixel/opened_at/<img returns nothing). The admin Recipients view shows Opened/Clicked rows that will always read 0. _(/Users/vapvarun/dev/repos/buddynext-pro/includes/Email/ (no pixel/opened_at writer); /Users/vapvarun/dev/repos/buddynext-pro/includes/Admin/BroadcastAdmin.php lines 401-417; broadcast-email.md lines 84-93)_
- **[low/plan-vs-reality/confirmed-in-code]** Plan-vs-reality REST drift: the journey doc's 'REST surface walked' (lines 156-165) lists GET /buddynext-pro/v1/email/unsubscribe and DELETE returning {cancelled:true}. The actual unsubscribe is an init-hook query-arg handler (?bn_unsub_campaign=&bid=&uid=) in BroadcastUnsubscribe.php, not a REST route; the REST controller that exists is /me/email-preferences (EmailUnsubscribeController.php). The doc's WP-CLI test (lines 99-104) also calls generate_token(user_id, campaign_id) but the real signature is generate_token(campaign_id, user_id) — arg order reversed. _(/Users/vapvarun/dev/repos/buddynext-pro/includes/Email/BroadcastUnsubscribe.php lines 47-53,234; /Users/vapvarun/dev/repos/buddynext-pro/includes/Email/Controllers/EmailUnsubscribeController.php lines 44-90; broadcast-email.md lines 99-104,156-165)_

### 🟡 Drip / Welcome Sequences (pro) — partial

**Headline:** Fully scaffolded and admin-wired, but the core promise breaks: drip step emails almost certainly never send (no bn.drip_step template, step subject/body never reaches the mailer), and the delay model uses enrolled_at not the documented last_step_at.

**Plan source:** /Users/vapvarun/dev/repos/buddynext-pro/docs/journeys/drip-welcome.md (dedicated journey doc with happy-path, edge cases, REST surface, and "what this validates")

**Plan promised:** A Pro feature for multi-step welcome/drip email sequences. Admin creates a named sequence with a trigger (user_register, onboarding_completed, manual), adds ordered steps (delay_days, subject, body_html, optional template_slug), and enables it. New users are auto-enrolled via the user_register / buddynext_onboarding_completed hooks; manual enroll is available via REST. An hourly cron tick (buddynextpro_drip_tick) advances each active enrollment one step at a time once the step's day-delay has elapsed (the journey describes this delay as measured from last_step_at), sends the step email, and marks the enrollment completed after the final step. A full admin step editor and a 6-route REST surface are specified.

**Owner expects:**
- Create multi-step sequences with per-step subject/body and day delays, and have those emails actually arrive in the member's inbox
- Auto-enroll on registration (and ideally on other lifecycle events), with no duplicate enrollment
- Reliable scheduling: the right step sends at the right time, no resends, no skipped steps
- Per-member unsubscribe from a sequence and respect of global email/notification preferences
- Visibility into enrollment state (who is in which sequence, what step, completed/unsubscribed) and basic send tracking
- An editor with rich body content and the ability to test-send a step before going live

**What we have:** Backend services, REST, admin UI, and DB tables are all present and wired. DB tables wp_bn_drip_sequences and wp_bn_drip_enrollments are created in /Users/vapvarun/dev/repos/buddynext-pro/includes/Core/Installer.php (CREATE TABLE at lines ~409/421; steps LONGTEXT and last_step_at columns added via maybe_alter_tables lines 98-128). DripService (/Users/vapvarun/dev/repos/buddynext-pro/includes/Email/DripService.php) implements create_sequence, add_step, enable/disable, enroll_user, list/get, and process_due_enrollments (cron worker). DripEnrollmentService (same dir) registers user_register + buddynext_onboarding_completed listeners and the buddynextpro_drip_tick cron handler; cron is scheduled on activation in /Users/vapvarun/dev/repos/buddynext-pro/buddynext-pro.php (line 30). REST controller DripController (includes/Email/Controllers/DripController.php) exposes list/create/get/update(toggle)/steps/enroll under buddynext-pro/v1, registered in includes/Core/Plugin.php line 285. Admin UI DripAdmin (includes/Admin/DripAdmin.php) provides sequence list, create form, enable/disable, and a full step editor (add/move up-down/delete) plus an AdminHub 'growth>drip' tab; registered in Plugin.php line 169. Step send path delegates to Free's EmailSender::send_now (/Users/vapvarun/dev/repos/buddynext/includes/Notifications/EmailSender.php).

**Gaps:**
- **[critical/both/confirmed-in-code]** Drip step emails almost certainly never send on a default install. DripService::process_due_enrollments() calls $sender->send_now($user_id,'bn.drip_step',$data) (DripService.php line 317). EmailSender::send_now() fetches get_template('bn.drip_step') and returns early if null or disabled (EmailSender.php lines 105-112). No 'bn.drip_step' template is seeded in Free's Installer::seed_email_templates() (the seeded type list at Installer.php lines 74-206 does not include it) and nothing in Pro registers/seeds it or hooks buddynext_email_payload to inject the step content. Result: send_now no-ops while current_step still advances, so members get no emails and the enrollment silently completes. _(/Users/vapvarun/dev/repos/buddynext-pro/includes/Email/DripService.php:317; /Users/vapvarun/dev/repos/buddynext/includes/Notifications/EmailSender.php:104-112; /Users/vapvarun/dev/repos/buddynext/includes/Core/Installer.php:69-206)_
- **[critical/expectation-vs-reality/confirmed-in-code]** Even if a 'bn.drip_step' template existed, the per-step subject/body_html would be ignored unless the template body literally contains {{subject}}/{{body_html}} tokens. send_now renders subject/body from the TEMPLATE row, passing the step content only as data tokens (EmailSender.php lines 119-120, render() lines 187-191). The admin step editor collects subject/body per step (DripAdmin.php lines 527-534) but there is no mechanism that turns that into the actual email content. _(/Users/vapvarun/dev/repos/buddynext/includes/Notifications/EmailSender.php:119-120,187-191; /Users/vapvarun/dev/repos/buddynext-pro/includes/Admin/DripAdmin.php:527-534)_
- **[high/plan-vs-reality/confirmed-in-code]** Delay model contradicts the plan and the code's own docblock. Journey (drip-welcome.md lines 110-142) and DripService docblock (lines 264-266) describe delays measured from last_step_at, and the journey's simulation moves last_step_at backwards to trigger the next step. Actual code computes due_at = enrolled_at + delay_days*DAY (DripService.php lines 308-309) and never reads last_step_at in the delay calc (it is written at line 341 but unused). The documented test/simulation procedure would not advance steps. _(/Users/vapvarun/dev/repos/buddynext-pro/includes/Email/DripService.php:308-309,341; /Users/vapvarun/dev/repos/buddynext-pro/docs/journeys/drip-welcome.md:110-142)_
- **[medium/plan-vs-reality/confirmed-in-code]** Re-enrollment behavior contradicts the journey's stated edge case. Journey (line 159) claims a second user_register for the same user is skipped via UNIQUE KEY. Actual enroll_user RESETS progress (current_step=0, status=active, enrolled_at=now) when an enrollment already exists (DripService.php lines 156-176). Firing user_register again would restart the whole sequence and re-send step 1. _(/Users/vapvarun/dev/repos/buddynext-pro/includes/Email/DripService.php:156-176; /Users/vapvarun/dev/repos/buddynext-pro/docs/journeys/drip-welcome.md:159)_
- **[medium/expectation-vs-reality/confirmed-in-code]** No member-facing surface and no per-sequence unsubscribe wiring. DripEnrollmentService::unsubscribe() exists (DripEnrollmentService.php lines 124-140) but no REST route or UI calls it, and there is no member view of their enrollments and no enrollment/status listing for admins (DripController exposes no enrollments endpoint; DripAdmin shows only step counts, not enrollees). Mature platforms surface enrollment state and let members opt out of a specific sequence. _(/Users/vapvarun/dev/repos/buddynext-pro/includes/Email/DripEnrollmentService.php:124-140; /Users/vapvarun/dev/repos/buddynext-pro/includes/Email/Controllers/DripController.php:70-190)_
- **[low/expectation-vs-reality/confirmed-in-code]** No test-send / preview of a step before enabling, and no send logging specific to drip beyond Free's generic bn_email_log row keyed to type 'bn.drip_step' (which, given gap 1, would only ever log if a template existed). Admin cannot verify a step renders correctly. This is below the production bar but not plan-breaking. _(/Users/vapvarun/dev/repos/buddynext-pro/includes/Admin/DripAdmin.php:516-545)_
- **[low/plan-vs-reality/confirmed-in-code]** Mid-tick crash safety: as the journey itself notes (lines 212), if a tick advances current_step but a later failure occurs there is no transactional guard; combined with the enrolled_at-based delay math, steps with the same elapsed window could behave unexpectedly. Lower risk in practice since one step advances per enrollment per tick. _(/Users/vapvarun/dev/repos/buddynext-pro/includes/Email/DripService.php:291-349; /Users/vapvarun/dev/repos/buddynext-pro/docs/journeys/drip-welcome.md:212)_

### 🟡 Membership Tiers & Gated Spaces (pro) — partial

**Headline:** Backend, gating, webhook sync, and admin are real and wired; the member-facing half — paywall rendering, front-end checkout/upgrade UI, and space-gating UI — is missing, so the configured paywall is never shown and members have no purchase path.

**Plan source:** /Users/vapvarun/dev/repos/buddynext-pro/docs/journeys/membership-gated-spaces.md (dedicated journey doc); quality bar from /Users/vapvarun/dev/repos/buddynext/docs/MASTER_DEVELOPMENT_PLAN.md "The Standard We Are Building To"

**Plan promised:** The journey doc promises an admin-driven membership system: create tiers (bnpro-membership-tiers), gate spaces via a required_ability column read by GatedSpacesIntegration on Free's buddynext_can_join_space filter, sync externally-granted tier abilities into bn_subscriptions via WebhookSubscriptionSync on buddynext_ability_granted, and a daily expiry cron. It explicitly states that when a gated join is denied the user gets "a bn-paywall HTML fragment or JSON error containing paywall content" produced by PaywallRenderer, and a Paywall Settings admin page configures global + per-space CTA copy. The doc itself flags as "open" that setting a space's required_ability has no UI (SQL/WP-CLI only) and that join-button/paywall selectors are deferred to a design phase.

**Owner expects:**
- Define paid/free tiers and gate spaces behind them (parity with BuddyBoss/membership-plugin space restriction)
- When a non-member hits a gated space, see a clear paywall/upgrade prompt with a working CTA — not a generic 'you cannot join' error
- A front-end path for members to view available tiers and start checkout/payment (Stripe/WooCommerce) without an admin
- Set which ability/tier a space requires from the space admin UI, not raw SQL
- Gated spaces visibly marked in directories/listings so members understand what is locked
- Admin can see, filter, and revoke subscriptions, and tiers map to payment processor products
- Subscriptions auto-expire and gating updates accordingly

**What we have:** Backend and admin are substantially built and wired. Tier CRUD: includes/Membership/MembershipTierService.php (create/update/delete/list, slug validation, caching). Subscriptions: includes/Membership/SubscriptionService.php (create/expire/read, user_has_tier, daily expiry cron registered). Gating: includes/Membership/GatedSpacesIntegration.php hooks buddynext_can_join_space and calls buddynext_can(); wired in includes/Core/Plugin.php:152. Webhook sync: includes/Membership/WebhookSubscriptionSync.php on buddynext_ability_granted, wired Plugin.php:153; Free fires it at /Users/vapvarun/dev/repos/buddynext/includes/Outbound/AccessWebhookController.php:197. Free side is real: required_ability column (Installer.php:486) and filter (SpaceMemberService.php:77,181). REST: TiersController, SubscriptionsController, CheckoutController (Stripe /me/checkout + /me/portal) registered Plugin.php:279-281; AdminTierTestCheckoutController for admin test links. Admin: includes/Admin/MembershipAdmin.php builds Tiers, Subscriptions (filter+revoke), Paywall Settings (global + per-space CTA) pages plus a Stripe price linker, all nonce/manage_options-guarded and exposed as Monetization Hub tabs. PaywallRenderer exists (includes/Membership/PaywallRenderer.php) and reads the configured CTA options.

**Gaps:**
- **[critical/both/confirmed-in-code]** PaywallRenderer is never instantiated anywhere in the codebase. The gated-join denial in Free's SpaceMemberService returns a generic WP_Error ('You cannot join this space.', SpaceMemberService.php:79) with no paywall content. The journey's promised 'bn-paywall HTML fragment / paywall content' on a 403 is not produced, and the entire Paywall Settings admin page (global + per-space CTA/label/description) configures output that is never rendered to any user. _(grep for 'new PaywallRenderer'/'PaywallRenderer(' returns zero callers; includes/Membership/PaywallRenderer.php (defined only); /Users/vapvarun/dev/repos/buddynext/includes/Spaces/SpaceMemberService.php:78-83 returns generic WP_Error)_
- **[high/both/confirmed-in-code]** No front-end member-facing UI for the feature. The Pro plugin ships no templates/ directory; no template, block, or JS calls POST /me/checkout, lists tiers, or renders an upgrade/join-gated affordance. Members cannot view tiers or start Stripe checkout from the site front-end — the checkout REST endpoint (CheckoutController) is API-only with no UX wiring. The journey doc itself defers join-button/paywall selectors to an unbuilt 'design phase'. _(templates/ and assets/ greps for 'me/checkout','tier_slug','upgrade' return nothing; Pro repo has no templates/ dir; includes/Membership/Controllers/CheckoutController.php exists but no caller)_
- **[high/both/confirmed-in-code]** No admin UI to set a space's required_ability. Gating a space requires direct SQL / WP-CLI; the Paywall Settings page only lists already-gated spaces read-only and cannot assign required_ability. This blocks a non-technical owner from gating any space. _(docs/journeys/membership-gated-spaces.md step 4 'only settable via direct SQL or WP-CLI (Free does not expose a UI)'; includes/Admin/MembershipAdmin.php render_paywall_page() reads required_ability but provides no input to set it)_
- **[medium/expectation-vs-reality/confirmed-in-code]** Gated spaces are not visually marked anywhere on the front-end (no badge/lock in space directory or space hero). A member only discovers a space is gated by attempting to join and getting an error. _(grep 'required_ability' in /Users/vapvarun/dev/repos/buddynext/templates returns no matches; no paywall/gated markup in templates)_
- **[low/plan-vs-reality/confirmed-in-code]** Journey REST table is inaccurate vs Free's actual route: it documents 'GET /buddynext/v1/spaces/{id}/join -- 403 when gated', but Free registers join as POST-only (SpaceController.php:107). Minor doc drift but signals the journey's REST walk was not verified against the live route. _(docs/journeys/membership-gated-spaces.md 'REST surface walked' section; /Users/vapvarun/dev/repos/buddynext/includes/Spaces/SpaceController.php:13,107)_

### 🟡 Stripe Payments (pro) — partial

**Headline:** Stripe backend (SDK, webhook sync, checkout/portal REST, admin keys + tier price linker) is built, tested, and wired — but the member-facing checkout/portal UX is not connected: the paywall CTA is a static admin-typed URL, so the P1.2 'member clicks Upgrade → Stripe checkout' loop is not actually closed on the front end.

**Plan source:** docs/PRO-ROADMAP.md — Phase P1 "Stripe Membership SDK integration": P1.1 (SDK bootstrap + webhook receiver), P1.2 (customer portal + checkout link generation), P1.3 (tier admin UI: Stripe price ID linker).

**Plan promised:** P1 promised a closed Stripe subscription loop for membership tiers: a StripeClient SDK wrapper, a signature-validated webhook receiver that syncs customer.subscription.* and invoice.* events into bn_subscriptions and fires buddynext_ability_granted/revoked, a CheckoutService + PortalService exposed via REST (POST /me/checkout, POST /me/portal), and a tier-edit admin UI to link a Pro tier to a Stripe price/product ID. The P1.2 acceptance criterion is explicitly a member-facing flow: "logged-in member clicks Upgrade on a gated space, lands on Stripe checkout, completes payment in test mode, webhook fires, ability granted, space unlocked."

**Owner expects:**
- Subscription checkout that a logged-in member can actually trigger from the front end (paywall/upgrade button), landing on Stripe-hosted checkout
- Webhook-driven entitlement sync so paid members gain access and cancelled/failed members lose it without manual admin work
- Self-service billing portal so members can update cards, cancel, and view invoices
- Admin keys screen with test/live mode detection and a copy-paste webhook endpoint URL
- Admin ability to map each membership tier to a Stripe price ID
- Webhook signature verification so the endpoint cannot be spoofed
- Reasonable handling of payment failure / past_due and cancellation states

**What we have:** Backend is substantially built and runtime-wired. StripeClient (includes/Stripe/StripeClient.php) wraps the SDK, reads buddynextpro_stripe_secret_key, and derives test/live mode from the key prefix. WebhookController (includes/Stripe/WebhookController.php) registers public POST /buddynext-pro/v1/stripe/webhook, validates via \Stripe\Webhook::constructEvent (with a test short-circuit filter), and handles customer.subscription.created/updated/deleted, invoice.paid, invoice.payment_failed — upserting wp_bn_subscriptions, extending expires_at, flipping past_due/cancelled, and firing buddynext_ability_granted/revoked plus buddynextpro_stripe_subscription_synced. CheckoutService (includes/Stripe/CheckoutService.php) mints subscription Checkout Sessions, auto-provisions a Stripe customer (usermeta buddynextpro_stripe_customer_id), resolves price via option buddynextpro_tier_stripe_price_id_{slug}, and supports promo codes. PortalService (includes/Stripe/PortalService.php) mints Billing Portal sessions. REST CheckoutController (includes/Membership/Controllers/CheckoutController.php) exposes POST /me/checkout (login required) and POST /me/portal (login + customer link required). StripeAdmin (includes/Stripe/StripeAdmin.php) renders the keys screen, mode badge, and webhook URL under the Monetization tab. MembershipAdmin (includes/Admin/MembershipAdmin.php, lines 275-278, 808, 847-865) provides the per-tier Stripe price/product ID fields (P1.3). All classes are instantiated and routes registered in includes/Core/Plugin.php (lines 237-241, 279-327). An admin-only test-checkout button exists (AdminTierTestCheckoutController.php + assets/admin/tier-stripe-test.js). The member-facing surface is PaywallRenderer (includes/Membership/PaywallRenderer.php), which renders a static CTA whose href comes from a hand-entered option (bn_space_{id}_paywall_cta_url / buddynextpro_paywall_cta_url) — it does NOT call /me/checkout.

**Gaps:**
- **[critical/both/confirmed-in-code]** The member-facing checkout flow is not wired to the API. The only front-end paywall surface (PaywallRenderer) outputs a static <a> whose href is an admin-typed URL; no template, store, or JS in either repo POSTs to /me/checkout. The single checkout JS caller (assets/admin/tier-stripe-test.js) is admin-only behind manage_options. This directly misses the P1.2 acceptance flow ('member clicks Upgrade on a gated space, lands on Stripe checkout'). The endpoint is real and tested, but no member UX reaches it. _(includes/Membership/PaywallRenderer.php (lines 71-75, static cta_url); only assets/admin/tier-stripe-test.js calls a checkout route; grep of assets/js + Free templates for me/checkout returns nothing)_
- **[high/both/confirmed-in-code]** No member-facing billing portal entry point. POST /me/portal exists and is tested, but no front-end template/button lets a paying member open the Stripe Billing Portal to manage/cancel/view invoices. Owners expect self-service billing parity with mature platforms. _(includes/Membership/Controllers/CheckoutController.php (handle_portal) has no front-end caller; grep for me/portal across assets/ and templates/ yields no member-side hit)_
- **[high/expectation-vs-reality/needs-live-verification]** invoice.paid / invoice.payment_failed handlers key off invoice['subscription'], but Stripe API versions >= 2025-03 moved the subscription reference into invoice.parent.subscription_details and dropped the top-level invoice.subscription field. With a current Stripe account these handlers may silently no-op (expires_at never extended, past_due never set). Needs live verification against the pinned SDK/API version. _(includes/Stripe/WebhookController.php lines 356-358 and 403-405 read $invoice['subscription'] only)_
- **[medium/expectation-vs-reality/confirmed-in-code]** No one-time/lifetime purchase path: CheckoutService hardcodes mode='subscription' (line 159). The roadmap scopes P1 to subscriptions, so this is expectation-only — many community owners sell one-time access. Lower priority but a real parity gap vs BuddyBoss/MemberPress. _(includes/Stripe/CheckoutService.php line 159 'mode' => 'subscription')_
- **[medium/expectation-vs-reality/confirmed-in-code]** No automated tax / proration / refund handling and no admin subscription-management screen (view/cancel a member's sub from wp-admin). Webhook covers status transitions but there is no admin oversight UI. Plan did not promise these; mature platforms generally provide at least admin visibility into active subscriptions. _(grep for refund/proration/automatic_tax across includes/ returns nothing; no admin subscriptions list screen found in includes/Admin/)_
- **[low/expectation-vs-reality/needs-live-verification]** Secret/webhook keys are stored in plain options via sanitize_text_field with no encryption-at-rest and the secret key is echoed back into the admin input value (StripeAdmin render_text_row prints get_option value). Standard practice is masking the stored secret in the UI. Needs confirmation of AdminPageBase::render_text_row behaviour for sensitive fields. _(includes/Stripe/StripeAdmin.php lines 172-177 pass the raw secret-key option into render_text_row; SETTINGS_MAP uses sanitize_text_field with no masking)_

### 🟡 Advanced Profile Field Types (pro) — partial

**Headline:** Backend types, validation, and the admin option UI are genuinely wired to live Free seams — but the admin config never persists (Free ignores bn_field_options), no front-end JS exists for map/conditional/searchable-multiselect, file upload has no handler, and the promised completeness score is absent; functionally partial despite a SHIPPED label.

**Plan source:** PRO-ROADMAP.md Phase P11.1 (marked "SHIPPED"); buddynext/docs/journeys/profile-fields.md "Known limitations"; quality bar from buddynext/docs/MASTER_DEVELOPMENT_PLAN.md "The Standard We Are Building To".

**Plan promised:** P11.1 promised 6 Pro field types (date_extended, location, file, multi_select_advanced, number_advanced, conditional), each defining render/validate/sanitize/display, registered via Free's filter seams. It promised an admin field editor extended with the 6 type options plus a conditional builder, conditional fields rendering via data-bn-show-if attributes with a tiny vanilla JS helper, and a bonus profile-completeness score via AdvancedFieldRenderer::completeness_for_user(). It explicitly claimed status SHIPPED and that all three Free seams were "already live in Free."

**Owner expects:**
- Each advanced type stores and validates its value correctly (date format, lat/lng range, numeric min/max, allowed multiselect options, file URL).
- Per-type configuration in the admin field builder actually persists (file MIME/size, number unit/min/max/step, multiselect choices, conditional trigger field+value) so the type is usable, matching BuddyPress/BuddyBoss xprofile field config.
- Members get the real input UX on the edit form: a working date picker, a map/location picker with autocomplete, a real file upload (file selected -> uploaded -> URL stored), a searchable/grouped multiselect, and conditional show/hide that responds to the trigger field.
- File upload type has a server-side handler that stores the uploaded file and produces a URL; otherwise the type is non-functional.
- Production-ready per the master standard: no partial implementations, no TODO-implement-later, complete or not started.

**What we have:** Three Pro classes are wired at runtime from includes/Core/Plugin.php lines 184-187. includes/Profile/AdvancedFieldTypes.php registers the 6 slugs into both buddynext_profile_field_types (Free allowlist) and buddynext_field_types (Free FieldType engine metadata) — both filters confirmed live in Free (ProfileFieldsManager.php:194, FieldType.php:156). includes/Profile/AdvancedFieldRenderer.php hooks the real engine seams buddynext_field_render_input and buddynext_field_render_display (confirmed fired at FieldType.php:278 and :503) and emits per-type HTML (date input, location wrapper with .bn-location-map-placeholder, file input named %s_upload, <select multiple>, number+unit, conditional wrapper with data-trigger-field). includes/Profile/AdvancedFieldValidator.php hooks buddynext_field_sanitize (fired at FieldType.php:654) and the legacy buddynext_profile_field_validate (fired at ProfileService.php:384/439) with real per-type validation (date Y-m-d, lat/lng bounds, file URL, multiselect-against-options, number min/max). includes/Admin/AdvancedFieldsAdmin.php hooks buddynext_profile_field_type_labels (fired ProfileFieldsManager.php:965) and buddynext_profile_field_type_options (fired ProfileFieldsManager.php:1262/1362) and renders option inputs named bn_field_options[...]. So the type allowlist, dropdown labels, server-side validation/sanitize, and basic per-type edit/display HTML are genuinely wired. Storage uses Free's bn_profile_fields/bn_profile_values (journey doc).

**Gaps:**
- **[critical/both/confirmed-in-code]** Admin per-type option inputs are never persisted. The Pro admin renders inputs named bn_field_options[allowed_mime|max_size_mb|unit|min|max|step|trigger_field_id|trigger_value|choices_raw], but Free's save handler (ProfileFieldsManager::handle_create_field) only reads $_POST['options'] for choice/date types and sets $parsed_opts=null otherwise; it never reads bn_field_options. grep for bn_field_options in Free returns zero matches. Consequently every renderer/validator read of $field['options']['unit'|'min'|'max'|'choices'|'trigger_field_id'] is always empty: number_advanced has no unit/min/max, multi_select_advanced has no choices, conditional has no trigger. The configuration UI is cosmetic. _(buddynext/includes/Admin/Members/ProfileFieldsManager.php:421-430 (only $_POST['options']; null otherwise; no bn_field_options read anywhere in Free) vs buddynext-pro/includes/Admin/AdvancedFieldsAdmin.php:148-249 (inputs named bn_field_options[...]))_
- **[high/both/confirmed-in-code]** No client-side JS/CSS ships for any advanced type. Renderer emits a .bn-location-map-placeholder, a .bn-location-search input, a conditional wrapper with data-trigger-field/data-trigger-value, and a plain <select multiple> — but there is no map/geocoder, no autocomplete, no searchable multiselect, and no conditional show/hide helper. grep across assets/ and src/ for bn-location, conditional, datepicker, leaflet, maps, bn-field-- returns no matches; no wp_enqueue in includes/Profile/. The roadmap's promised 'tiny vanilla helper' for data-bn-show-if does not exist (and the rendered attribute is data-trigger-field, not data-bn-show-if). Location and conditional are non-functional UX; multiselect is a bare native control. _(buddynext-pro/includes/Profile/AdvancedFieldRenderer.php:228-372; grep of buddynext-pro/assets and src returned no profile-field JS/CSS)_
- **[high/both/confirmed-in-code]** File upload type is non-functional end-to-end. Renderer outputs <input type=file name="{name}_upload"> but no handler exists: grep for wp_handle_upload/media_handle_upload/_FILES across all of buddynext-pro/includes returns zero. The validator expects $value to already be a URL (validate_file). There is no path that turns a selected file into a stored URL, and the docblock's mentioned fallback REST endpoint (GET .../profile-fields/{id}/render) is not implemented. A member selecting a file results in no stored value. _(buddynext-pro/includes/Profile/AdvancedFieldRenderer.php:256-279 (file input) and AdvancedFieldValidator.php:254-267 (expects URL); no upload handler anywhere in buddynext-pro/includes)_
- **[medium/plan-vs-reality/confirmed-in-code]** Promised profile-completeness score does not exist. Roadmap P11.1 claims AdvancedFieldRenderer::completeness_for_user($user_id) surfaced on the edit page. grep for completeness_for_user / completeness in includes/Profile returns nothing. _(buddynext-pro/docs/PRO-ROADMAP.md:536 vs grep miss in buddynext-pro/includes/Profile/)_
- **[medium/both/confirmed-in-code]** Source-of-truth docs are stale/contradictory, masking the gaps. PRO-ROADMAP marks P11.1 SHIPPED while the three class docblocks still say 'Blocked/Partially gated on a missing Free seam' (those seams now exist) and the journey doc says the types 'render as no-op until Free fires the seam' (the seams fire). The net real state is neither 'SHIPPED' nor 'no-op' — config persistence and front-end JS are the actual blockers, which no doc states. Violates the master standard's 'no partial implementations / no TODO-implement-later'. _(PRO-ROADMAP.md:530 ('SHIPPED'); AdvancedFieldsAdmin.php:5 ('Blocked on a missing Free seam'); journeys/profile-fields.md:247 ('render as no-op'); MASTER_DEVELOPMENT_PLAN.md:104-107)_
- **[low/plan-vs-reality/confirmed-in-code]** date_extended underdelivers vs its own contract. Type metadata/docs promise time zone + age display and the journey calls it enhanced, but render_date_extended emits a plain <input type=date> (no time, no timezone, no age) and the validator only accepts Y-m-d, rejecting any time component. It is functionally identical to Free's date type. _(buddynext-pro/includes/Profile/AdvancedFieldRenderer.php:210-218; AdvancedFieldValidator.php:177-198; AdvancedFieldTypes.php:14)_

### 🟡 Custom Reactions (pro) — partial

**Headline:** Admin CRUD + filter merge are fully built and wired, but custom reactions are invisible end-to-end: the front-end picker hardcodes the 6 built-ins and never reads the filter, label/color are dead data, and custom slugs have no glyph — so members can never use them.

**Plan source:** /Users/vapvarun/dev/repos/buddynext-pro/docs/journeys/custom-reactions.md (dedicated journey doc); quality bar from /Users/vapvarun/dev/repos/buddynext/docs/MASTER_DEVELOPMENT_PLAN.md "The Standard We Are Building To"

**Plan promised:** Pro lets an admin define custom reaction slug/label/color rows, stored in the WP option `buddynextpro_custom_reactions`, with slug/label/color validation and a 20-total cap. Pro merges these into Free's reaction set by hooking the `buddynext_reaction_types` filter (slug-only merge by design). An admin page provides add/remove with nonce + manage_options checks. The journey explicitly defers the front-end picker selector ("frontend selector TBD") and claims the merged set is observable via `GET /buddynext/v1/reactions` and that members can pick the custom reaction. The doc states there are no dedicated Pro REST routes; extension is filter-only.

**Owner expects:**
- A custom reaction defined in admin actually appears in the front-end reaction picker that members use
- The custom reaction renders a visible glyph/icon (and the configured color is honored) on both the picker and the per-reaction count summary
- Members can select the custom reaction and have it persist and count, like the 6 built-in reactions
- Parity with BuddyBoss/BuddyPress emoji reaction behavior: the configured set drives the actual UI, not a hardcoded list
- Per the master plan: a shipped feature is complete end-to-end (admin -> storage -> picker -> count -> summary), not admin-only scaffolding

**What we have:** Backend + admin are fully built and runtime-wired. CustomReactionsService (`/Users/vapvarun/dev/repos/buddynext-pro/includes/Reactions/CustomReactionsService.php`) implements get/add/remove with slug regex, label length, hex-color validation, duplicate detection, and a 20-total cap in `extend_reaction_types()`. CustomReactionsAdmin (`/Users/vapvarun/dev/repos/buddynext-pro/includes/Admin/CustomReactionsAdmin.php`) renders the table + add form, has admin-post handlers with `manage_options` + nonce checks, and registers an AdminHub tab. Plugin.php (`/Users/vapvarun/dev/repos/buddynext-pro/includes/Core/Plugin.php` lines 141-147) adds the `buddynext_reaction_types` filter and instantiates the admin page at load. Unit tests exist (`/Users/vapvarun/dev/repos/buddynext-pro/tests/Reactions/CustomReactionsServiceTest.php`). On the Free side, the filter is real: `ReactionService::reaction_types()` (`/Users/vapvarun/dev/repos/buddynext/includes/Reactions/ReactionService.php` line 201) applies `buddynext_reaction_types` over a flat slug array. HOWEVER the front-end picker template `/Users/vapvarun/dev/repos/buddynext/templates/parts/post-actions.php` (lines 115-132) hardcodes the 6 reaction labels in a local `$reaction_labels` array and loops over that — it never calls `reaction_types()` or applies the filter, so custom reactions never reach the picker UI. The merge passes slug strings only; the stored label/color are discarded. Glyphs come from `IconService::render_emoji()` reading `assets/emoji/<slug>.svg` (`/Users/vapvarun/dev/repos/buddynext/includes/Core/IconService.php` lines 87-119), which returns empty string for any slug without a bundled SVG, so a custom slug has no icon. The Free REST controller (`/Users/vapvarun/dev/repos/buddynext/includes/Reactions/ReactionController.php`) exposes only `/reactions/toggle`, `/reactions` (count for one object), and `/reactions/list` — there is no endpoint returning the reaction-types list.

**Gaps:**
- **[critical/both/confirmed-in-code]** Custom reactions never appear in the front-end reaction picker. The picker template hardcodes the 6 built-in reactions in a local $reaction_labels array (post-actions.php lines 115-132) and never calls ReactionService::reaction_types() or the buddynext_reaction_types filter, so the entire Pro merge has no UX effect. The journey doc itself flags the selector as 'TBD', so the member-facing half was never wired. _(/Users/vapvarun/dev/repos/buddynext/templates/parts/post-actions.php lines 115-132 (hardcoded loop, no filter call); /Users/vapvarun/dev/repos/buddynext-pro/docs/journeys/custom-reactions.md lines 47-51,108-109 ('frontend selector TBD'))_
- **[high/both/confirmed-in-code]** Stored label and color are never consumed by any renderer. The filter merges slug strings only (extend_reaction_types returns slugs), Free's reaction_types() is a flat slug array, and glyphs are resolved purely from a bundled SVG file by slug. The 'color' admin field is dead data; a custom slug has no icon asset so render_emoji() returns an empty string. _(/Users/vapvarun/dev/repos/buddynext-pro/includes/Reactions/CustomReactionsService.php lines 156-179 (slug-only merge); /Users/vapvarun/dev/repos/buddynext/includes/Core/IconService.php lines 87-95,116-119 (empty string when assets/emoji/<slug>.svg missing))_
- **[high/expectation-vs-reality/confirmed-in-code]** No mechanism for an admin to supply a glyph/SVG/image for a custom reaction. Built-in reactions rely on vendored Fluent SVGs keyed by slug; the admin form collects only slug/label/color, so even if the picker iterated the filtered set, custom reactions would be glyph-less. This is the root blocker to a production-credible custom-reaction feature. _(/Users/vapvarun/dev/repos/buddynext-pro/includes/Admin/CustomReactionsAdmin.php lines 218-280 (form fields: slug, label, color only); /Users/vapvarun/dev/repos/buddynext/includes/Core/IconService.php lines 87-95)_
- **[medium/plan-vs-reality/confirmed-in-code]** Journey doc step 5 is factually wrong about the API: it claims GET /wp-json/buddynext/v1/reactions returns an array of reaction slugs including the custom one. That route requires object_type+object_id and returns reaction counts for a single object, not the types list. No endpoint exposes the merged reaction-type list at all. _(/Users/vapvarun/dev/repos/buddynext/includes/Reactions/ReactionController.php lines 59-79 (get_count) and 144-158 (returns count, not types); doc lines 39-46)_
- **[low/expectation-vs-reality/confirmed-in-code]** Storage is a single autoloaded-no WP option with slug/label/color; no per-reaction icon, ordering, enable/disable, or scope (which object types it applies to). Compared to mature platforms this is a thin model, though adequate for a v0.1 scope if the UX were wired. _(/Users/vapvarun/dev/repos/buddynext-pro/includes/Reactions/CustomReactionsService.php lines 38,104-110)_

### 🟡 Advanced Search Filters (pro) — partial

**Headline:** Backend is built and wired (table, CRUD, REST, Free SQL seam) but the feature is dead end-to-end: no UI, the five filter args are never sourced from any request, and run_search() ignores the saved filters it was meant to replay.

**Plan source:** /Users/vapvarun/dev/repos/buddynext-pro/docs/PRO-ROADMAP.md (Phase P12.1, lines 542-553)

**Plan promised:** P12.1 "Saved searches + filters" promises: (1) a per-user saved-search store on bn_saved_searches with a value object and CRUD service plus run() returning live result counts; (2) AdvancedSearchFilters that hooks Free's buddynext_search_query_args to translate Pro filters (the roadmap names profile_field, activity_since, in_space, member_tier, has_label; the code ships tier_slug, space_id, member_label, joined_after, active_within_days); (3) a REST controller for list/create/get/delete. Acceptance criterion: a member saves "designers in London who posted in last 7 days", the saved-search URL preserves the filters, and visiting it returns the live result set. Roadmap marks P12.1 as SHIPPED.

**Owner expects:**
- A member-facing UI to compose advanced filters (membership tier, space, label, join date, recent activity) and run them against the member directory/search
- Filter values actually applied to results end-to-end (set a filter -> see narrowed results), matching BuddyPress/BuddyBoss advanced member search
- Save a filtered query, see it in a list, re-run it and get the live result set (the roadmap's own acceptance test)
- REST or query-param surface so filters survive a shareable URL
- Production-complete per the master plan: no dead args, no TODO seams, UI matches mockups

**What we have:** Backend scaffolding is real and wired, but the feature is not reachable at runtime. Confirmed in code: bn_saved_searches table is created (/Users/vapvarun/dev/repos/buddynext-pro/includes/Core/Installer.php:491-500). SavedSearchService CRUD + run_search() exists (/Users/vapvarun/dev/repos/buddynext-pro/includes/Search/SavedSearchService.php). SavedSearch value object exists (/Users/vapvarun/dev/repos/buddynext-pro/includes/Search/SavedSearch.php). REST controller registers 6 routes under buddynext-pro/v1/me/saved-searches (/Users/vapvarun/dev/repos/buddynext-pro/includes/Search/Controllers/SavedSearchController.php) and is wired in Plugin.php:307. AdvancedSearchFilters::register() is called from Plugin.php:202 and hooks buddynext_search_query_args (/Users/vapvarun/dev/repos/buddynext-pro/includes/Search/AdvancedSearchFilters.php:53-54). The Free seam IS implemented: /Users/vapvarun/dev/repos/buddynext/includes/Search/SearchService.php:223-311 fires the filter and builds EXISTS subqueries for all five Pro args (tier_slug, space_id, member_label, joined_after, active_within_days) when type is user/member. What is missing: no code anywhere sources these five args from a request. Free's unified search endpoint (/Users/vapvarun/dev/repos/buddynext/includes/Search/SearchController.php:149-180) reads only q/type/per_page/page. AdvancedSearchFilters::apply_pro_args() only sanitizes keys already present in the incoming $args array (it never reads $_GET/request), and Free passes that array as only ['per_page','page','type','viewer_id'] (SearchService.php:223-233), so the isset() guards in apply_pro_args never fire. There are no Pro templates and no Pro search JS (only assets/admin and assets/js stores for realtime/ai-reply/profile-views; no templates/ dir in the Pro repo). SavedSearchService::run_search() (SavedSearchService.php:239-251) passes only query/type/per_page/page into SearchService::search() and never injects the saved query_args into the filter, so even a saved filtered query runs unfiltered.

**Gaps:**
- **[critical/both/confirmed-in-code]** No member-facing UI for advanced search filters or saved searches. Pro repo has no templates/ directory and no search-related JS; the only JS is admin/realtime/ai-reply/profile-views stores. The roadmap acceptance test (compose a filter, save it, revisit URL) cannot be exercised by any user. _(/Users/vapvarun/dev/repos/buddynext-pro/ (no templates/ dir; find for search JS returns nothing); roadmap acceptance at docs/PRO-ROADMAP.md:553)_
- **[critical/both/confirmed-in-code]** Advanced filter args are dead at runtime. The five args (tier_slug, space_id, member_label, joined_after, active_within_days) are honored by Free's SQL builder but are never sourced from any request. AdvancedSearchFilters::apply_pro_args only sanitizes pre-existing keys; Free seeds the filter array with just per_page/page/type/viewer_id, so the isset() branches never run and no filter ever applies. _(/Users/vapvarun/dev/repos/buddynext-pro/includes/Search/AdvancedSearchFilters.php:89-134; /Users/vapvarun/dev/repos/buddynext/includes/Search/SearchService.php:223-233; /Users/vapvarun/dev/repos/buddynext/includes/Search/SearchController.php:149-180)_
- **[critical/both/confirmed-in-code]** SavedSearchService::run_search() does not apply the saved filters. It extracts only query/type/per_page/page from the stored query_args and calls SearchService::search(); it never injects the saved tier_slug/space_id/etc. into the buddynext_search_query_args filter, so a re-run of a filtered saved search returns unfiltered results. Directly contradicts the P12.1 acceptance criterion. _(/Users/vapvarun/dev/repos/buddynext-pro/includes/Search/SavedSearchService.php:239-251)_
- **[high/plan-vs-reality/confirmed-in-code]** Filter set mismatch between roadmap and code. Roadmap P12.1 names profile_field={k}={v}, activity_since=7d, in_space={id}, member_tier={slug}, has_label={slug}; code ships tier_slug, space_id, member_label, joined_after, active_within_days. profile_field (arbitrary xprofile field filtering) is not implemented at all in the Free SQL builder. _(docs/PRO-ROADMAP.md:550 vs /Users/vapvarun/dev/repos/buddynext/includes/Search/SearchService.php:255-311)_
- **[low/plan-vs-reality/confirmed-in-code]** run_search() returns full result set but the roadmap (P12.1, line 549) specifies run() returns the current result COUNT for the saved query. Implementation returns the SearchService results array, not a count, and does not store/expose a count for display. _(docs/PRO-ROADMAP.md:549 vs /Users/vapvarun/dev/repos/buddynext-pro/includes/Search/SavedSearchService.php:226-267)_
- **[low/expectation-vs-reality/confirmed-in-code]** No admin management screen for saved searches; controller docblock explicitly defers it ('Admin page: TODO'). Minor for a member feature but the master plan forbids // TODO seams. _(/Users/vapvarun/dev/repos/buddynext-pro/includes/Search/Controllers/SavedSearchController.php:15-16)_

### 🟡 AI (ranking, embeddings, semantic search, replies) (pro) — partial

**Headline:** All four P2 AI features are genuinely wired end-to-end (services, REST, admin, Interactivity island) — not stubs — but semantic search is crippled by no backfill/reindex and post-only embedding, and there is no cross-feature cost guardrail.

**Plan source:** /Users/vapvarun/dev/repos/buddynext-pro/docs/PRO-ROADMAP.md — "Phase P2 — AI Engine" (P2.1 Feed ranking, P2.2 Moderation classifier, P2.3 Semantic search, P2.4 Smart replies)

**Plan promised:** PRO-ROADMAP Phase P2 promises a four-part AI engine, all marked SHIPPED. P2.1: AiRankedFeedService rebinds Free's `feed` key when toggled and re-ranks by engagement+decaying affinity from a SignalsCollector writing to bn_ai_signals. P2.2: ContentClassifier (OpenAI/Anthropic/local heuristic) feeds ModerationAi into Free's buddynext_safeguard_check at priority 20, plus a /ai/classify admin test bench. P2.3: SemanticSearchService rebinds `search`, boosting baseline FULLTEXT results by query-to-object cosine similarity; EmbeddingProvider (OpenAI) + EmbeddingIndexer keep bn_ai_embeddings warm. P2.4: ReplyGenerator + ReplyController (/ai/reply-suggestions, daily quota) + an Interactivity island next to the comment form suggesting reply chips. Each item lists admin settings and a test count.

**Owner expects:**
- Feed ranking that visibly surfaces more relevant content per member and reverts cleanly to chronological when off — without degrading feed performance at scale
- Semantic/AI search that improves recall over keyword search across ALL searchable content (posts, members, spaces), including content that already existed before AI was turned on
- A way to backfill/reindex existing content into the embedding store — owners enable AI on an established community, not an empty one
- AI moderation that augments the existing rules engine, never blocks legitimate posts on provider outage (fail-open), and is testable before going live
- Smart replies that are opt-in, cost-controlled (per-member quota), and only appear when configured
- Clear admin controls: provider, API key (masked), model, thresholds/quotas, enable toggles — matching the BuddyBoss-class settings bar
- Cost visibility / guardrails so a misconfiguration cannot run up an unbounded provider bill
- Production-grade code: no TODOs, WPCS clean, tests present (master plan standard)

**What we have:** All four P2 sub-features are wired end to end (not stubs). Feed: includes/AI/AiRankedFeedService.php (extends Free FeedService, PHP re-rank by engagement+exp-decayed affinity) rebound on the `feed` container key only when buddynextpro_ai_feed_enabled is on (includes/Core/Plugin.php:459-467); includes/AI/SignalsCollector.php registered unconditionally (Plugin.php:97) writing react/comment/follow/bookmark rows to bn_ai_signals (table+ENUM widen in includes/Core/Installer.php:131-165,340-349). Moderation: includes/AI/ContentClassifier.php (openai/anthropic/local-heuristic/disabled, fail-open safe verdict), includes/AI/ModerationAi.php hooks buddynext_safeguard_check at priority 20 and returns WP_Error 422 when score>=threshold AND tone!=safe, REST POST /ai/classify in includes/AI/Controllers/AiModerationController.php, admin includes/Admin/AIModAdmin.php (wired Plugin.php:213-215,312-314). Search: includes/AI/SemanticSearchService.php (extends Free SearchService, cosine boost >0.5 = similarity*100) rebound on `search` only when enabled (Plugin.php:482-489); includes/AI/EmbeddingProvider.php (OpenAI only; local/disabled return null; 1h cache); includes/AI/EmbeddingIndexer.php hooks buddynext_post_created (async via Action Scheduler) + buddynext_post_deleted (inline) + public buddynext_index_embedding; bn_ai_embeddings created in Installer.php:238-260. Free SearchService.search() returns object_type/object_id keys (verified /Users/vapvarun/dev/repos/buddynext/includes/Search/SearchService.php:418-419), so the join key is correct. Replies: includes/AI/ReplyGenerator.php, includes/AI/Controllers/ReplyController.php (POST /ai/reply-suggestions, permission is_user_logged_in + buddynext_can comment, per-day usermeta quota 429), includes/AI/ReplyButtonRenderer.php (Interactivity island on buddynext_part_post_comment_form_after, gated on ReplyGenerator::is_enabled), assets/js/ai-reply/store.js (buddynextpro/ai-reply store), includes/AI/AiReplyAssets.php (script module + l10n). Admin includes/Admin/AIAdmin.php exposes every promised option (feed toggle, decay days, semantic toggle, embedding provider/key/model, reply provider/key/model/quota) on Free AdminPageBase v2 chrome.

**Gaps:**
- **[high/both/confirmed-in-code]** Semantic search has NO backfill/reindex path for pre-existing content. EmbeddingIndexer only embeds on buddynext_post_created going forward; there is no admin 'reindex' button and no WP-CLI command (grep for reindex/backfill in includes/Admin returns nothing; no WP_CLI::add_command anywhere in the plugin). An owner who enables AI on an established community gets embeddings only for posts created AFTER enabling, so semantic boost is effectively dead on their entire existing corpus until those posts happen to be recreated. The only reindex hook (buddynext_index_embedding) is documented as for tests/tooling with no UI driving it. _(includes/AI/EmbeddingIndexer.php:66-104 (only post_created/deleted + tooling hook); grep of includes/Admin and whole tree shows no reindex/backfill UI or WP_CLI command)_
- **[medium/expectation-vs-reality/confirmed-in-code]** Semantic search only boosts object_type='post'. EmbeddingIndexer.load_post_text reads bn_posts and dispatches object_type 'post' exclusively, but Free's search index (bn_search_index) also returns users and spaces (SearchService.php DISTINCT object_type). Members/spaces never receive embeddings, so semantic re-ranking silently does nothing for those result types — a partial vs the owner expectation of AI search improving recall across all content. _(includes/AI/EmbeddingIndexer.php:91-104,245-262 (post-only); /Users/vapvarun/dev/repos/buddynext/includes/Search/SearchService.php:99-103 indexes/searches multiple object_types)_
- **[medium/expectation-vs-reality/confirmed-in-code]** No cost/usage visibility or spend guardrail for embeddings or moderation. Smart replies have a per-member daily quota (ReplyController), but embedding indexing (one OpenAI call per published post, fired on every create) and the moderation classifier (one provider call per safeguard check) have no rate cap, budget ceiling, or usage dashboard. On a busy community this is an unbounded provider bill — an owner-credibility concern not addressed by the plan. _(includes/AI/EmbeddingIndexer.php:91-104 (call per post create, no cap); includes/AI/ContentClassifier.php classify() (call per check); no usage meter in includes/Admin/AIAdmin.php or AIModAdmin.php)_
- **[medium/expectation-vs-reality/needs-live-verification]** ReplyButtonRenderer depends on Free emitting buddynext_part_post_comment_form_after AND a textarea carrying [data-comment-input="{postId}"]. The store.js selector findCommentTextarea targets '.bn-post-card' and '[data-comment-input="N"]'. If Free's current comment-form part does not emit that exact action/attribute, the chips render but clicking one fills nothing. API + island presence does not prove the textarea-fill wiring works against the live Free template — needs runtime confirmation. _(assets/js/ai-reply/store.js:151-160; includes/AI/ReplyButtonRenderer.php:50-51 (hook name) — Free-side emission of buddynext_part_post_comment_form_after + data-comment-input not verified in this repo)_
- **[low/expectation-vs-reality/confirmed-in-code]** Embedding 'local' provider is a non-functional placeholder. EmbeddingProvider returns null for any provider != 'openai', and the admin select labels it 'Local (reserved — falls back to FULLTEXT)'. So the only working embedding backend is OpenAI; an owner without an OpenAI key cannot use semantic search at all. Plan (P2.3) lists local/disabled as returning null, so this is plan-consistent but still an expectation gap for owners wanting on-device/no-API AI. _(includes/AI/EmbeddingProvider.php:72-78,173-180; includes/Admin/AIAdmin.php:297)_
- **[low/plan-vs-reality/needs-live-verification]** Plan marks all P2 items 'verified'/'SHIPPED' with specific test files (e.g. tests/AI/SemanticSearchServiceTest.php, EmbeddingIndexerTest.php, ReplyControllerTest.php). Test presence/pass not confirmed in this audit; master plan mandates TDD + WPCS-clean. Cannot assert tests exist or pass from code read alone. _(docs/PRO-ROADMAP.md:100,120,135,150 list test files; tests/AI/ contents not read)_
- **[low/plan-vs-reality/confirmed-in-code]** Open TODO in shipped path violates the master-plan 'no TODO' standard. Plugin.php carries a multi-line TODO about whether \BuddyNext\Core\Container::instance() is the correct accessor — exactly the kind of in-flight marker the standard forbids in production code, and a latent fragility for the entire container-rebind mechanism the AI feed/search features rely on. _(includes/Core/Plugin.php:110-112 (TODO comment); /Users/vapvarun/dev/repos/buddynext/docs/MASTER_DEVELOPMENT_PLAN.md:107 ('No // TODO: implement later'))_

### 🟡 Push Notifications (pro) — partial

**Headline:** Server-side FCM push is fully built and wired, but the member-facing delivery loop is incomplete: wrong body copy for the 3 most common notification types, a dead admin test button, no web-push path despite advertising it, and no front-end UX.

**Plan source:** /Users/vapvarun/dev/repos/buddynext-pro/docs/PRO-ROADMAP.md — Phase P4 (P4.1 "FCM/APNs bootstrap" SHIPPED v0.4.0; P4.2 "Push preference UI" SHIPPED 0.4.0-beta1). P4.3 (front-end pref toggles) explicitly deferred. App-side delivery covered later under the mobile-app phase (lines 613-620).

**Plan promised:** P4 promised server-side mobile push via FCM HTTP v1: a JWT/OAuth2 FCM client with stale-token cleanup, member-owned push-token CRUD REST endpoints plus an admin test-push diagnostic, a dispatcher hooked to Free's buddynext_notification_created that builds title/body for follower/reaction/comment/mention, an admin Push settings page (toggle + project ID + service-account JSON), and a bn_push_tokens table. P4.2 added per-user per-type push prefs (usermeta opt-out model), a PushPrefsController (GET/PUT /me/push-prefs), a pref gate in the dispatcher, and an admin Push Prefs page. The roadmap itself states front-end pref UI and end-to-end app delivery are deferred to later phases.

**Owner expects:**
- Members can actually receive push notifications on their devices without a separate native app build — i.e. browser web-push (VAPID/service worker) so the feature works on a stock BuddyNext web install
- Notification body text is meaningful per event type (X followed you / reacted / commented / mentioned), matching parity with BuddyBoss app pushes
- A way for members to register/unregister devices and toggle push prefs from the front-end (notifications/settings screen), not only via raw REST or wp-admin
- Admin can configure FCM and verify it works with a functioning test button before going live
- Pushes respect per-user, per-type opt-out so members are not spammed
- Stale/unsubscribed tokens are cleaned up automatically

**What we have:** Server-side FCM is fully built and wired. PushClient (/Users/vapvarun/dev/repos/buddynext-pro/includes/Push/PushClient.php) implements RS256 service-account JWT auth, 50-min access-token transient cache, send()/send_to_user(), and 404/410 stale-token deletion. PushController (includes/Push/PushController.php) registers GET/POST /me/push-tokens, DELETE /me/push-tokens/{id} (owner-checked), POST /me/push-tokens/test (manage_options). PushDispatcher (includes/Push/PushDispatcher.php) hooks buddynext_notification_created (priority 20) and fans out, consulting the pref gate. PushPrefService (includes/Push/PushPrefService.php) stores bn_push_pref_{type} usermeta (opt-out default-on) and injects push_enabled into Free's buddynext_notification_prefs filter. PushPrefsController (includes/Push/Controllers/PushPrefsController.php) serves GET/PUT /me/push-prefs. Admin: PushAdmin.php + PushPrefsAdmin.php exist and are wired. bn_push_tokens table created in includes/Core/Installer.php (lines 207-236). All wired in includes/Core/Plugin.php (lines 242-250, 329-331). Free fires the action with the correct schema columns (verified in /Users/vapvarun/dev/repos/buddynext/includes/Notifications/NotificationService.php lines 83-109).

**Gaps:**
- **[high/expectation-vs-reality/confirmed-in-code]** Notification body copy mismatch: the dispatcher's build_snippet() switch matches 'follower'/'bn.follower', 'reaction'/'bn.reaction', 'comment'/'bn.comment', but Free actually emits 'bn.new_follower', 'bn.post_reacted', 'bn.post_commented' (verified in buddynext/includes/Notifications/NotificationListener.php). These fall through to the default branch, so follower/reaction/comment/share pushes deliver with the generic body 'You have a new notification.' instead of 'X started following you' etc. Only 'bn.mention' matches correctly. The pref gate is unaffected (it uses the live type string), so delivery still happens — just with wrong copy. _(/Users/vapvarun/dev/repos/buddynext-pro/includes/Push/PushDispatcher.php lines 159-199 vs /Users/vapvarun/dev/repos/buddynext/includes/Notifications/NotificationListener.php (bn.new_follower / bn.post_reacted / bn.post_commented))_
- **[high/both/confirmed-in-code]** Admin 'Send test push' button is dead — its data-bn-push-test attribute has no JS handler anywhere. assets/admin/ contains realtime-test.js and tier-stripe-test.js but no push-test.js, and grep for 'bn-push-test' / 'push-test' returns only the PHP markup in PushAdmin.php (and nothing in Free assets). The admin cannot verify FCM config via the documented button. _(/Users/vapvarun/dev/repos/buddynext-pro/includes/Admin/PushAdmin.php lines 242-245; assets/admin/ listing (no push-test.js); repo-wide grep for bn-push-test matches only PushAdmin.php)_
- **[high/expectation-vs-reality/confirmed-in-code]** No web-push delivery path despite admin UI advertising 'web, Android, and iOS' (PushAdmin subtitle + 'web' platform enum). There is no VAPID key, no service worker, and no client JS that calls getToken()/registers a token (grep for vapid/web-push/serviceWorker/firebase/getToken across assets/ and includes/ returns nothing but the unrelated pusher.min.js). Token registration is REST-only, so in practice pushes require a native FCM-SDK mobile app to POST a token. On a stock web-only BuddyNext install the feature delivers to zero devices. Plan acknowledges app delivery is a later phase, but the admin copy over-promises web support. _(/Users/vapvarun/dev/repos/buddynext-pro/includes/Admin/PushAdmin.php line 163 ('web, Android, and iOS'); grep for vapid/web-push/serviceWorker/firebase/getToken in assets/ and includes/ returned no results)_
- **[medium/both/confirmed-in-code]** No front-end UX for token registration or push-pref toggles. The only pref surface is the admin's own BuddyNext → Push Prefs page (PushPrefsAdmin); ordinary members can change prefs/devices only by hitting raw REST endpoints. The roadmap itself defers this (P4.3, gated on Free shipping buddynext_notif_prefs_row), so this is plan-acknowledged, but it is still a member-facing gap vs BuddyBoss parity. _(PRO-ROADMAP.md lines 212, 218, 645 (P4.3 deferred); no member-facing push template found (grep for push-token/push_token in templates returned nothing))_
- **[low/expectation-vs-reality/confirmed-in-code]** Pref gate only fires when a type string is present; typeless events bypass the gate (PushDispatcher lines 84-89). This is intentional/documented but means a member who opts out of a type can still receive a push if the upstream payload omits the type field. Low risk because Free always sets 'type', but worth noting as an opt-out leak. _(/Users/vapvarun/dev/repos/buddynext-pro/includes/Push/PushDispatcher.php lines 84-89)_

### 🟡 Bulk Moderation (pro) — partial

**Headline:** Core bulk moderation is built and wired (admin page + REST + JS + real Free service calls), but the 'Remove Content' action only resolves reports without taking content down — a high-severity behavioral gap, plus shipped TODO/fiction comments that breach the production standard.

**Plan source:** /Users/vapvarun/dev/repos/buddynext-pro/docs/journeys/bulk-moderation.md (dedicated journey doc); quality bar from /Users/vapvarun/dev/repos/buddynext/docs/MASTER_DEVELOPMENT_PLAN.md "The Standard We Are Building To"

**Plan promised:** Pro ships a standalone "Bulk Moderation (Pro)" admin page (admin.php?page=buddynextpro-bulk-mod) that lists pending wp_bn_reports with checkboxes and a bulk-action form to dismiss / remove content across selected reports, plus a "Bulk User Actions" section to warn/suspend a comma-separated list of user IDs. Actions post to admin-post.php (manage_options + nonce), delegate per-item to Free's ModerationService, and redirect with a "N succeeded, M failed" notice. A parallel REST endpoint POST /buddynext-pro/v1/moderation/bulk supports dismiss/remove/warn/suspend programmatically. The doc itself flags two open seams: Free exposes no queue-injection hook (so Pro ships a standalone page), and the exact dismiss/resolve status mapping was unconfirmed.

**Owner expects:**
- Select multiple items in the report queue and apply one action (dismiss/resolve/remove) in a single click — parity with BuddyPress/BuddyBoss bulk moderation
- 'Remove Content' must actually take the reported content down from public view, not merely mark the report resolved
- Bulk warn and suspend users (with suspension duration) and have those reflected in the user's real suspension/warning state
- Per-item success/failure feedback so a partial batch is visible and one bad ID doesn't silently break the rest
- Admin-only access enforced by capability + nonce on every action
- Auditability: each bulk action recorded against the actor (moderation log) and reversible where the platform supports it
- Production-quality: no TODO-gated behavior, no mislabeled buttons (master plan forbids partial implementations and // TODO markers)

**What we have:** Service layer is real and registered. BulkModService (/Users/vapvarun/dev/repos/buddynext-pro/includes/Moderation/BulkModService.php) implements bulk_dismiss/bulk_remove/bulk_warn/bulk_suspend, each looping with try/catch and returning {succeeded[], failed[]}, firing buddynext_pro_bulk_action_executed. REST controller (/Users/vapvarun/dev/repos/buddynext-pro/includes/Moderation/Controllers/BulkModerationController.php) registers POST buddynext-pro/v1/moderation/bulk with enum action validation and manage_options permission; it is wired in /Users/vapvarun/dev/repos/buddynext-pro/includes/Core/Plugin.php:290. Admin UI (/Users/vapvarun/dev/repos/buddynext-pro/includes/Admin/BulkModAdmin.php) is registered at Plugin.php:175, renders the pending-reports table (direct wp_bn_reports query, 25/page, pagination), the bulk-action dropdown (Dismiss / Remove Content), the Bulk User Actions form (warn/suspend, duration), enforces manage_options + nonce in require_admin_and_nonce(), and redirects with the success/fail summary notice. JS wiring exists and matches selectors: /Users/vapvarun/dev/repos/buddynext-pro/assets/js/admin/bulk-mod.js handles select-all (#bnpro_select_all), Apply (writes hidden action), and the user-ID list splitter. Free's ModerationService (/Users/vapvarun/dev/repos/buddynext/includes/Moderation/ModerationService.php) genuinely exposes dismiss(207), resolve(207-209), warn(341), and suspend(1076) with signatures matching the Pro calls; wp_bn_reports status ENUM('pending','dismissed','escalated','resolved') (Installer.php:744) confirms dismiss/resolve map to real statuses.

**Gaps:**
- **[high/both/confirmed-in-code]** 'Remove Content' does not remove content. BulkModService::bulk_remove() calls Free's resolve() (BulkModService.php:55), which only set_status() to 'resolved' (ModerationService.php:207-209). Free has a dedicated remove_content() (ModerationService.php:224) that ALSO fires buddynext_content_removed to take the post/comment down — bulk_remove never calls it. So the UI button labeled 'Remove Content' (BulkModAdmin.php:302) and REST action 'remove' resolve the report but leave the offending content publicly visible. _(/Users/vapvarun/dev/repos/buddynext-pro/includes/Moderation/BulkModService.php:54-56; /Users/vapvarun/dev/repos/buddynext/includes/Moderation/ModerationService.php:207-260)_
- **[medium/plan-vs-reality/confirmed-in-code]** Plan doc makes a false behavioral claim: it states bulk_remove 'Calls Free's ModerationService::resolve() ... which marks the report as resolved and triggers content-removed downstream actions' (mirrored in the code docblock). resolve() does NOT fire buddynext_content_removed; only remove_content() does. The plan's described outcome diverges from shipped behavior. _(/Users/vapvarun/dev/repos/buddynext-pro/docs/journeys/bulk-moderation.md:49-56; /Users/vapvarun/dev/repos/buddynext-pro/includes/Moderation/BulkModService.php:47-56)_
- **[medium/expectation-vs-reality/confirmed-in-code]** No selection of a row-level action for warn/suspend FROM the report queue: warn/suspend require manually retyping user IDs into a free-text field (BulkModAdmin.php:372-375) rather than acting on the reported user(s) of selected reports. Mature platforms let a moderator suspend the offender directly from a flagged item. This is a UX-parity shortfall, not a broken path. _(/Users/vapvarun/dev/repos/buddynext-pro/includes/Admin/BulkModAdmin.php:362-402)_
- **[low/expectation-vs-reality/confirmed-in-code]** Standing TODO comments ship in production code, violating the master plan's explicit 'No // TODO: implement later' / 'no partial implementations' standard. BulkModAdmin.php carries a multi-line TODO about deprecating the standalone page when Free adds queue hooks. _(/Users/vapvarun/dev/repos/buddynext-pro/includes/Admin/BulkModAdmin.php:16-18)_
- **[low/plan-vs-reality/confirmed-in-code]** BulkModService::bulk_warn docblock claims a fallback to the buddynext_user_warned action 'if not present', but no such fallback exists in the code — it calls $mod_service->warn() unconditionally inside try/catch. Free does expose warn() so this is currently harmless, but the documented resilience is fictional and would surface as a caught Throwable -> per-item 'failed' if Free's method ever changes. _(/Users/vapvarun/dev/repos/buddynext-pro/includes/Moderation/BulkModService.php:58-102)_
- **[low/expectation-vs-reality/needs-live-verification]** Admin page visibility depends on AdminHub: the submenu is registered with an empty menu title (hidden) and the real entry comes via AdminHub::register_tab under 'moderation/bulk' (BulkModAdmin.php:101-108, 141-151). If Free's AdminHub class/tab is not present or the moderation hub is gated, the page is reachable only by the legacy direct URL. Whether the tab actually appears for owners needs runtime confirmation against Free's AdminHub registration. _(/Users/vapvarun/dev/repos/buddynext-pro/includes/Admin/BulkModAdmin.php:101-151)_
- **[low/expectation-vs-reality/needs-live-verification]** Moderation-log/audit coverage for bulk actions is unverified. The service fires buddynext_pro_bulk_action_executed and Free's set_status writes status, but whether each bulk item lands in Free's ModerationLogService (attributing the actor) was not traced end-to-end from the bulk path. _(/Users/vapvarun/dev/repos/buddynext-pro/includes/Moderation/BulkModService.php:208-220; /Users/vapvarun/dev/repos/buddynext/includes/Moderation/ModerationLogService.php)_

### 🟡 Auto / Rule-based Moderation (pro) — partial

**Headline:** Core rule engine, REST CRUD, admin UI, DB table and Free hook-wiring are all shipped and genuinely wired — but the journey's headline 'flag = allow + create report' behavior is broken (flag silently rejects, no report), and threshold_remove suspend targets the wrong ID.

**Plan source:** docs/journeys/auto-moderation.md (dedicated journey doc, full happy-path + edge cases + REST surface)

**Plan promised:** Pro layers rule-based moderation on top of Free's safeguard pipeline via a bn_mod_rules table and a RulesService CRUD/evaluation engine. Four rule types: keyword_block (warn/flag/block), link_block (flag/block by domain), rate_limit (hourly post cap), threshold_remove (auto remove/suspend when report count crosses a threshold). Surfaces promised: an admin "Moderation Rules" page (buddynextpro-mod-rules) with create/edit/toggle/delete; a full REST CRUD+toggle surface under buddynext-pro/v1/mod-rules (admin-only); enforcement via the Free buddynext_safeguard_check filter; and auto-actions via the Free buddynext_moderation_auto_actions filter. The journey explicitly specifies that severity=block rejects the post (422) while severity=flag ALLOWS the post (200/201) and auto-creates a pending row in Free's wp_bn_reports.

**Owner expects:**
- Keyword/profanity blocklist that rejects or holds offending posts (BuddyPress/BuddyBoss parity)
- Link/domain blocklist to stop spam URL drops
- Per-user rate limiting to throttle flood/spam bots
- Auto-escalation: when content accumulates N reports, auto-hide/remove or escalate without a human
- An admin UI to manage all of the above without code, with enable/disable per rule
- A 'flag for review' tier that does NOT silently delete the user's content but routes it to a moderation queue
- Admin-only access control on all rule management
- Bulk actions on the moderation queue (dismiss/remove/warn/suspend) to clear backlogs

**What we have:** Fully wired feature, not a stub. RulesService (includes/Moderation/RulesService.php) implements CRUD + config validation for all four rule types and evaluates keyword_block/link_block/rate_limit in evaluate_safeguard() and threshold_remove in evaluate_post_report(). SafeguardIntegration (includes/Moderation/SafeguardIntegration.php) registers on Free's buddynext_safeguard_check filter; AutoActionsIntegration (includes/Moderation/AutoActionsIntegration.php) registers on buddynext_moderation_auto_actions. Both Free hooks genuinely exist and fire: buddynext/includes/Moderation/SafeguardService.php:75 and buddynext/includes/Moderation/ModerationService.php:138. Wiring confirmed in buddynext-pro/includes/Core/Plugin.php:172-174 (integrations + ModRulesAdmin) and 289-290 (REST controllers). DB table bn_mod_rules created in buddynext-pro/includes/Core/Installer.php:442. REST CRUD+toggle in includes/Moderation/Controllers/ModRulesController.php (all routes require manage_options). Admin UI in includes/Admin/ModRulesAdmin.php renders table + type-specific create/edit forms with nonces + cap checks. Bulk moderation (BulkModService.php + Controllers/BulkModerationController.php) delegates to Free's ModerationService dismiss/resolve/warn/suspend, all of which exist (buddynext/includes/Moderation/ModerationService.php:185,207,341,1076).

**Gaps:**
- **[high/both/confirmed-in-code]** The journey's central flag-severity behavior is contradicted by the code. Journey Part 3 (steps 7-9) promises severity=flag ALLOWS the post (HTTP 200/201) and auto-creates a pending wp_bn_reports row. In reality RulesService::evaluate_keyword_block (RulesService.php:441-445) returns a WP_Error('bnpro_keyword_flagged', status 202) for flag, and Free's PostService (buddynext/includes/Feed/PostService.php:110-116) rejects ANY WP_Error except code 'pending_review' — so the post is NOT created. No code path inserts a wp_bn_reports row when a safeguard rule flags content (grep for report insertion on the flag path returns nothing). Net: 'flag' behaves identically to 'block' (silent rejection) and never produces a moderation-queue entry. The same applies to link_block 'flag' (RulesService.php:494-498). _(buddynext-pro/includes/Moderation/RulesService.php:441-445,494-498 + buddynext/includes/Feed/PostService.php:110-116)_
- **[high/expectation-vs-reality/confirmed-in-code]** threshold_remove 'suspend' action targets the wrong ID. evaluate_post_report sets user_id = report['object_id'] (RulesService.php:335-337), but object_id for a content report is the post/content ID, not the author's user_id. A suspend auto-action would attempt to suspend a user whose ID equals a post ID. There is no lookup from object_id to the content's author_id. _(buddynext-pro/includes/Moderation/RulesService.php:335-339)_
- **[medium/expectation-vs-reality/needs-live-verification]** threshold_remove 'suspend' may not be consumed by Free. Free's buddynext_moderation_auto_actions consumer (buddynext/includes/Moderation/ModerationService.php ~138-175) shows switch cases firing buddynext_content_removed for 'remove' and buddynext_user_warned for 'warn'; a 'suspend' case was not visible in the read range. If Free does not handle the 'suspend' action slug, threshold_remove suspend rules are silently no-ops even after the ID issue is fixed. _(buddynext/includes/Moderation/ModerationService.php:138-175 (suspend case not confirmed in read window))_
- **[medium/expectation-vs-reality/confirmed-in-code]** keyword_block 'warn' severity is effectively a no-op. evaluate_keyword_block returns true for warn with only a code comment that 'Pro callers can log' (RulesService.php:435-438) — nothing is logged, recorded, or reported. The admin UI offers 'warn' as a first-class option (ModRulesAdmin.php:473 / description at :477 'allow but log') but no logging occurs, so an owner choosing 'warn' gets no audit trail. _(buddynext-pro/includes/Moderation/RulesService.php:435-438 + includes/Admin/ModRulesAdmin.php:477)_
- **[medium/expectation-vs-reality/confirmed-in-code]** Rule evaluation has no per-rule audit log / hit counter. There is no record of which rule fired, when, or against what content. Mature platforms (BuddyBoss) surface 'flagged by rule X' history. Owner cannot tune rules without visibility into what they catch. No bn_mod_rule_hits table or logging hook exists in the Moderation dir. _(buddynext-pro/includes/Moderation/ (no log table; Installer.php:442 only creates bn_mod_rules))_
- **[medium/expectation-vs-reality/needs-live-verification]** rate_limit and link_block enforcement depends on the caller passing content/link_url through the safeguard filter, and only the post-creation path (PostService) invokes buddynext_safeguard_check. Comments/replies, profile fields, messages, and group content are not shown to route through this filter, so rule-based moderation likely covers only feed posts, not all member-generated content an owner would expect to moderate. _(buddynext/includes/Moderation/SafeguardService.php:75 is invoked only from buddynext/includes/Feed/PostService.php:105; other content services not confirmed to call it)_
- **[low/expectation-vs-reality/confirmed-in-code]** Keyword matching is naive case-insensitive substring (str_contains, RulesService.php:426). This produces the classic Scunthorpe problem and no word-boundary/regex/wildcard support. The admin description (ModRulesAdmin.php:466) honestly states 'substring match', so it is documented, but it falls short of mature-platform expectations for whole-word/regex matching. _(buddynext-pro/includes/Moderation/RulesService.php:422-426)_
- **[low/plan-vs-reality/confirmed-in-code]** The bulk moderation queue is a standalone Pro page (buddynextpro-bulk-mod) rather than integrated into Free's reports table, because Free does not yet expose row-action/column hooks. Documented as a known seam in the journey (lines 174-176), but it means owners manage reports in two disconnected screens. _(buddynext-pro/includes/Admin/BulkModAdmin.php:57 + docs/journeys/auto-moderation.md:174-176)_

### 🟡 Member Labels (pro) — partial

**Headline:** Backend is complete and correctly wired (CRUD, REST, admin label management, DB, data injection); the feature is half-shipped at the UX layer — labels never render on profiles, there is no admin UI to assign a label to a member (REST-only), byline chips are unstyled, and the icon is never drawn.

**Plan source:** /Users/vapvarun/dev/repos/buddynext-pro/docs/journeys/member-labels.md (dedicated journey doc, thorough); quality bar from /Users/vapvarun/dev/repos/buddynext/docs/MASTER_DEVELOPMENT_PLAN.md "The Standard We Are Building To" (lines 97-108)

**Plan promised:** Admin defines editorial labels (Verified, Expert, Staff) with slug/name/hex-color/Lucide-icon/sort-order, stored in wp_bn_member_labels. Labels are assigned/unassigned to users (wp_bn_member_label_assignments) via a REST surface (8 endpoints under buddynext-pro/v1). A ProfileLabelInjector hooks Free's buddynext_profile_extra_data filter so the user's labels ride along in the profile API response. The doc explicitly defers frontend badge display ("color swatch + icon") to "the design guidelines phase" and states only that the data is present in the API response. Validates CRUD, assignment, member-count, and orphan-purge-on-delete.

**Owner expects:**
- Admin UI to create/edit/delete labels with color and icon (parity with BuddyBoss profile-type/verified-badge style features)
- A way to actually assign a label to a specific member from the admin (member list row action, user-edit screen, or a per-user picker) without hand-writing curl/REST calls
- Labels visibly rendered as badges on the member profile header and in member directory cards
- Labels rendered next to the author name on activity/post cards
- Member directory filter or search by label (find all Verified/Staff members)
- Production-grade UX: badges styled (CSS), icons rendered, accessible — not just data in a JSON payload

**What we have:** Backend is complete and wired at boot. LabelService (CRUD + validation + member_count) at includes/Members/LabelService.php; LabelAssignmentService (assign/unassign/get_user_labels/get_label_users/delete_by_label, fires buddynext_pro_label_assigned/unassigned actions) at includes/Members/LabelAssignmentService.php; Label value object at includes/Members/Label.php. REST: LabelsController registers all 8 documented routes at includes/Members/Controllers/LabelsController.php (write routes gated by require_manage_options). Admin label CRUD page (label table + add/edit/delete forms, color swatch, member count) at includes/Admin/MemberLabelsAdmin.php, also registered as an AdminHub 'members/labels' tab. DB tables created in includes/Core/Installer.php (lines 459, 472). All services + injector + admin instantiated in includes/Core/Plugin.php::wire_extensions() (lines 178-181) and the REST controller at line 293. ProfileLabelInjector (includes/Members/ProfileLabelInjector.php) registers two seams: inject_labels() appends $extra['labels'] to buddynext_profile_extra_data, and render_chips_from_part() echoes .bn-badge chips on the buddynext_part_post_byline_after action (Free's post-byline part does fire this with author_id, confirmed in buddynext/templates/parts/post-byline.php:236 and is rendered by post-card.php:419).

**Gaps:**
- **[high/both/confirmed-in-code]** Labels are not rendered on the member profile. ProfileLabelInjector::inject_labels() writes $extra['labels'] as a {slug,name,color,icon} array onto the buddynext_profile_extra_data filter, but no Free profile template consumes extras['labels']. The profile hero renders the 'stats' descriptor array (4 canonical tiles + bridge tiles appended via buddynext_part_profile_stats_strip_args), a different filter and shape; grep of buddynext/templates/profile/ found no consumer of extras['labels']. The label data rides in the API response but never reaches the profile UI as a badge. _(includes/Members/ProfileLabelInjector.php:134-153 (writes to buddynext_profile_extra_data, not stats); buddynext/templates/parts/profile-stats-strip.php (renders 'stats' array only); grep of buddynext/templates/profile/ shows no extras['labels'] consumer)_
- **[high/expectation-vs-reality/confirmed-in-code]** No UI to assign a label to a member. Assignment exists only via the REST endpoints POST/DELETE /users/{id}/labels/{slug} (manage_options). The admin page (MemberLabelsAdmin) does label CRUD only — no per-user picker, no members-list row action, no user-edit metabox. A site owner must hand-craft curl/REST calls (exactly as the journey doc demonstrates). This is the core operational gap: the feature cannot be used by a non-developer admin. _(includes/Admin/MemberLabelsAdmin.php (only add/edit/delete label handlers; no assignment UI); grep for assign in includes/Admin/ and templates/ found only delete_by_label purge calls)_
- **[medium/plan-vs-reality/confirmed-in-code]** Post-byline label chips have no styling shipped. render_chips_from_part() emits <span class="bn-badge bn-post-card__label" data-label-slug ...> but grep for bn-post-card__label / data-label-slug across buddynext-pro/assets and buddynext/assets returned zero CSS. The chips render as unstyled inline spans (only the inline background-color from $label->color applies); .bn-post-card__labels wrapper and the icon are unstyled, and the Lucide icon slug is never actually rendered as an icon anywhere. _(includes/Members/ProfileLabelInjector.php:104-114; grep of buddynext-pro/assets and buddynext/assets for bn-post-card__label/data-label-slug found nothing)_
- **[medium/plan-vs-reality/confirmed-in-code]** The configured Lucide icon is stored, validated, and returned in API/admin but never rendered as an icon on any surface. Admin table shows the icon slug as plain text (MemberLabelsAdmin.php:347), the byline chip renders only name+color (no icon), and there is no profile badge. The icon field is effectively dead weight at the UX layer. _(includes/Admin/MemberLabelsAdmin.php:347 (icon shown as text); includes/Members/ProfileLabelInjector.php:104-114 (chip omits icon))_
- **[medium/expectation-vs-reality/needs-live-verification]** No directory filter/search by label. Owners expect to filter the member directory to 'all Verified members'. get_label_users() exists in the service but is not wired to any directory query, AdvancedSearchFilters, or member-loop filter. Parity feature in mature platforms; not present. _(includes/Members/LabelAssignmentService.php:186 get_label_users() defined; no caller found in directory/search code during this audit (full Search/ tree not exhaustively read))_
- **[low/both/confirmed-in-code]** Doc claims '## Known limitations: None' yet the same doc (line 206) admits frontend badge display is deferred, and the journey's Part 3 verification only checks the API JSON, never a rendered badge. The plan's own acceptance criteria stop at the data layer, below the master plan's 'Premium UX is the baseline' / 'no partial implementations' standard. _(docs/journeys/member-labels.md:203-206 (Known limitations 'None' vs 'Frontend display ... deferred'); MASTER_DEVELOPMENT_PLAN.md:104-105)_

### 🟡 Scheduled Posts (pro) — partial

**Headline:** Backend, REST, cron, admin page, and Free-side notification/feed guards are all shipped and wired; the gap is the missing member-facing scheduling UX, leaving the feature API-only for end users.

**Plan source:** /Users/vapvarun/dev/repos/buddynext-pro/docs/journeys/scheduled-posts.md (dedicated journey doc, detailed)

**Plan promised:** The journey doc promises a Pro feature that lets a member schedule an existing draft post for future publication via REST: POST/DELETE /buddynext-pro/v1/posts/{id}/schedule, GET /me/scheduled-posts, GET /posts/scheduled (admin). A 5-minute cron worker (tick()) promotes overdue scheduled posts to published and re-fires buddynext_post_created. An admin page (buddynextpro-scheduled-posts) lists scheduled posts with Cancel / Publish Now / Publish Overdue Posts Now actions. Free's bn_posts.status ENUM and scheduled_at column back the state. The doc itself is REST/admin-centric and never describes a member-facing composer UI for scheduling.

**Owner expects:**
- A member can schedule a post from the normal post composer/UI (pick a date/time), not only via raw curl REST calls
- Scheduled posts are hidden from feeds until publish time and then appear normally
- A member can see and manage (reschedule/cancel) their own scheduled queue from the front end
- Scheduled time respects the site/user timezone, or is clearly labeled UTC, so posts publish when the user expects
- Notifications for a scheduled post fire at publish time, not at schedule time
- Admin can view, cancel, and force-publish scheduled posts
- Reliable publishing even if WP-Cron is unreliable (or at least an admin manual trigger)

**What we have:** Backend is fully built and wired. Service /Users/vapvarun/dev/repos/buddynext-pro/includes/Feed/ScheduledPostsService.php implements schedule_post(), cancel_schedule(), get_scheduled_posts(), and static tick() with a 5-min cron interval (register_cron, CRON_HOOK buddynextpro_publish_scheduled). Integration /Users/vapvarun/dev/repos/buddynext-pro/includes/Feed/ScheduledPostsIntegration.php hooks buddynext_post_created at priority 5 to flip status to 'scheduled' when scheduled_at is future. REST controller /Users/vapvarun/dev/repos/buddynext-pro/includes/Feed/Controllers/ScheduledPostsController.php registers all four documented routes. Admin page /Users/vapvarun/dev/repos/buddynext-pro/includes/Admin/ScheduledPostsAdmin.php renders the queue with Publish Now / Cancel / Publish Overdue Posts Now and admin_post handlers. All wired at boot in /Users/vapvarun/dev/repos/buddynext-pro/includes/Core/Plugin.php (lines 191-194 integration/cron/admin, 296-297 REST controller). Free's notification guard the plan flagged as a 'known limitation' is in fact implemented: /Users/vapvarun/dev/repos/buddynext/includes/Notifications/NotificationListener.php:535 suppresses notifications for status='scheduled' future posts, and the feed query /Users/vapvarun/dev/repos/buddynext/templates/feed/home.php:192 excludes future scheduled_at rows. No member-facing scheduling UI exists in either repo.

**Gaps:**
- **[high/expectation-vs-reality/confirmed-in-code]** No member-facing UX to schedule a post. The composer/feed front end (Free templates and assets/js/feed/store.js) contains no 'Schedule' button or call to POST /buddynext-pro/v1/posts/{id}/schedule, GET /me/scheduled-posts, or DELETE .../schedule. The feature is reachable only by raw REST calls. A site owner expects members to schedule from the UI; as shipped, members cannot use this feature at all without external tooling. _(grep across /Users/vapvarun/dev/repos/buddynext-pro/assets,templates,src and /Users/vapvarun/dev/repos/buddynext/assets/js/feed/store.js found no calls to the schedule endpoints; only event/voice-room scheduling exists in store.js (lines 1529-1592))_
- **[medium/expectation-vs-reality/confirmed-in-code]** Timezone correctness is unproven. schedule_post() validates with strtotime()/time() (server-UTC assumption) but tick() and ScheduledPostsIntegration compare scheduled_at against SQL NOW(), which returns the MySQL session local time, not guaranteed UTC. If the DB session timezone is not UTC, posts publish at the wrong wall-clock time. No gmdate/current_time/wp_timezone normalization exists in the Feed code. _(/Users/vapvarun/dev/repos/buddynext-pro/includes/Feed/ScheduledPostsService.php tick() query uses 'scheduled_at <= NOW()' (line ~272); schedule_post uses time() (line 123); grep for gmdate/current_time/wp_timezone in includes/Feed returned nothing)_
- **[medium/expectation-vs-reality/confirmed-in-code]** No reschedule/cancel surface for members and no listing of their own queue in any UI; combined with the missing schedule entry point, the entire member lifecycle (create -> schedule -> view -> cancel) is API-only. Admin lifecycle is complete. _(GET /me/scheduled-posts and DELETE /posts/{id}/schedule exist in ScheduledPostsController.php but no front-end consumer found in either repo's assets/templates)_
- **[low/both/confirmed-in-code]** post_owner_permission() in the REST controller only checks is_user_logged_in() and returns true for any logged-in user; it does not assert post ownership at the permission layer (the method name implies it does). Ownership is enforced one layer down in the service (assert_owner), so this is defense-in-depth weakness / misleading naming rather than an exploitable hole, but a non-owner request returns the service's 403 rather than being rejected at the permission gate. _(/Users/vapvarun/dev/repos/buddynext-pro/includes/Feed/Controllers/ScheduledPostsController.php lines 135-145 (post_owner_permission returns true once logged in); ownership re-checked in ScheduledPostsService::assert_owner lines 347-362)_
- **[low/plan-vs-reality/confirmed-in-code]** Plan-vs-reality (in the plan's favor): the journey doc's 'Known limitations' claims Free does not yet guard notifications for scheduled posts and that recipients may get premature alerts. That guard IS implemented in Free, so the doc is stale/over-pessimistic. Worth correcting the doc; no functional defect. _(/Users/vapvarun/dev/repos/buddynext/includes/Notifications/NotificationListener.php lines 531-540 suppress notifications for future scheduled posts, contradicting docs/journeys/scheduled-posts.md lines 205-207)_

### 🟢 Multi-pin Posts (pro) — shipped

**Headline:** Shipped as planned: a clean filter-only Pro extension that raises the pin cap to 10, fully wired and tested in code — but the journey's space-scope pinning scenario is unreachable via REST (Free never passes space_id), and member-UI wiring is unverified.

**Plan source:** /Users/vapvarun/dev/repos/buddynext-pro/docs/journeys/multi-pin-posts.md (dedicated journey doc); quality bar from /Users/vapvarun/dev/repos/buddynext/docs/MASTER_DEVELOPMENT_PLAN.md "The Standard We Are Building To".

**Plan promised:** Pro raises Free's per-scope pinned-post cap from 1 to 10 via a single filter (`buddynext_post_pin_limit`), with no new Pro DB tables and no new Pro REST routes. The journey explicitly frames this as a filter-only extension: `ProPinService::register()` hooks the filter at priority 10, `pro_pin_limit()` returns `max($limit, 10)` so it never lowers a higher value, and Free's `PostService::pin()` enforces the cap and returns a `pin_limit_reached` WP_Error at the boundary. The doc itself flags multiple unverified assumptions (TODO: confirm Free's pin storage schema, the exact REST route path, whether space-scope pinning works).

**Owner expects:**
- Members can pin more than one post (the marketed Pro value) and the cap is actually enforced server-side, returning a clean error at the boundary rather than a silent failure or DB duplicate.
- Pinning is reachable from the actual member UI (post options menu / pin button), not only via raw REST/CLI — Premium UX is the master-plan baseline.
- Pin cap applies consistently across the scopes the platform supports (profile feed and space feed), matching the granularity BuddyPress/BuddyBoss owners expect.
- An admin-facing way to see or configure the limit, or at least documented predictable behavior, so an owner can reason about the cap.
- Pinned posts surface correctly (ordered first / visually marked) wherever feeds render.

**What we have:** Pro ships exactly the filter-only extension described. `ProPinService` (/Users/vapvarun/dev/repos/buddynext-pro/includes/Feed/ProPinService.php) defines PRO_PIN_LIMIT=10, registers `add_filter('buddynext_post_pin_limit', ..., 10, 3)` in `register()`, and `pro_pin_limit()` returns `max($limit, 10)`. It is actually wired at boot in /Users/vapvarun/dev/repos/buddynext-pro/includes/Core/Plugin.php:190 (`\BuddyNextPro\Feed\ProPinService::register();`). The Free consumer is real: /Users/vapvarun/dev/repos/buddynext/includes/Feed/PostService.php:366 applies the filter, then counts existing `is_pinned=1` rows per scope (lines 370-388) and returns `WP_Error('pin_limit_reached')` when count >= limit before the `is_pinned=1` update. The REST surface is Free's: /Users/vapvarun/dev/repos/buddynext/includes/Feed/PostController.php:70-81 registers POST/DELETE `/posts/(?P<id>\d+)/pin`. Integration test exists at /Users/vapvarun/dev/repos/buddynext-pro/tests/Feed/ProPinServiceTest.php (10 pins succeed, 11th fails; skips if Free absent). No Pro DB tables, no Pro REST routes, no Pro templates/admin screens for pinning — consistent with the plan.

**Gaps:**
- **[high/plan-vs-reality/confirmed-in-code]** The journey's central scenario (Part 2 'Pin 10 posts in the space', plus the 'Profile scope vs space scope' edge case) cannot be exercised through the REST API: PostController::pin_post (PostController.php:279-282) calls `(new PostService())->pin($post_id, $user_id)` and never passes `$space_id`. So `pin()` always computes the count with `space_id IS NULL` (PostService.php:371) — every REST pin is a profile-scope pin. There is no REST path to pin within a space scope. The doc's space-scope verification queries (`WHERE space_id = (...alpha-space)`) would always show 0 pinned because nothing is ever pinned with a non-null space_id via the API. _(/Users/vapvarun/dev/repos/buddynext/includes/Feed/PostController.php:282 (no space_id passed); /Users/vapvarun/dev/repos/buddynext/includes/Feed/PostService.php:348,371)_
- **[medium/expectation-vs-reality/needs-live-verification]** No verification that the member-facing UI exposes multi-pin. Pin/unpin buttons appear in Free templates (post-options-menu.php references pin), but whether the Pro-raised cap of 10 is reflected/usable in the UX (e.g., no client-side cap of 1, pin control stays enabled past the first pin) is not provable from the audited Pro code. Pro ships only the filter; the entire UX path lives in Free templates/JS not inspected here. API-level presence (filter raises cap) does not prove UX-level wiring. _(/Users/vapvarun/dev/repos/buddynext/templates/parts/post-options-menu.php (pin control present, behavior not read); Pro contributes no template)_
- **[low/expectation-vs-reality/confirmed-in-code]** No admin screen or setting to view/configure the pin limit. The cap is a hardcoded constant (PRO_PIN_LIMIT=10) with no filter override surfaced to owners and no admin UI. Acceptable per the plan (flat constant by design) but below the parity an owner may expect for a configurable limit. Plan explicitly scopes this out, so it is an expectation gap, not a plan gap. _(/Users/vapvarun/dev/repos/buddynext-pro/includes/Feed/ProPinService.php:32,62-68 (constant, no settings/admin))_
- **[low/plan-vs-reality/confirmed-in-code]** WP_Error from pin() is mapped to HTTP 403 (PostController.php:285 add_data status 403), not a 4xx that semantically reads as 'limit reached'. The journey (step 6) expects a 'non-200 response ... indicating the pin limit of 10 has been reached'. 403 is technically non-200 and the error code is `pin_limit_reached`, so this satisfies the letter of the plan but a limit-exceeded condition returning 403 (forbidden) rather than 409/422 is a minor semantic mismatch a client could mis-handle. _(/Users/vapvarun/dev/repos/buddynext/includes/Feed/PostController.php:284-286)_

### 🟡 White Label (pro) — partial

**Headline:** Hue/font/custom-CSS white-label and admin relabeling are shipped and fully wired, but the logo never renders on the front-end, the per-space Brand tab is hidden from non-admin space owners, it duplicates Free's existing Branding tab, and login/email branding is absent.

**Plan source:** docs/PRO-ROADMAP.md Phase P6 (P6.1 Brand-override service, P6.2 Per-space brand, P6.3 Custom-domain mapping [REMOVED], P6.4 Backend label rewriting). Quality bar from buddynext/docs/MASTER_DEVELOPMENT_PLAN.md "The Standard We Are Building To".

**Plan promised:** P6 promised admin-side white-label branding (not request routing). P6.1: site-wide brand config (logo URL, OKLCH hue 0-360, font family, custom CSS <=10k chars) persisted to wp_options, with a wp_head/admin_head priority-1 :root override that rotates the entire OKLCH token system, a 12-swatch admin picker, and a REST preview endpoint. P6.2: per-space brand overrides stored in a new bn_space_meta table, layered over the site brand on space pages, exposed via REST GET/POST /spaces/{id}/brand and a Brand tab on the per-space settings template (owner OR manage_options). P6.3 (custom-domain mapping) was explicitly removed pre-release. P6.4: rewrite the "BuddyNext" string in admin menu, plugin row, admin title, and BN-domain gettext to a configured brand name.

**Owner expects:**
- Replace product/brand name in wp-admin chrome (menu, plugin row, page titles) with own brand
- Set a logo that actually appears on the front-end (header/login), the way BuddyBoss white-label swaps the logo
- Apply brand color across the whole community UI
- Optional custom CSS without touching theme files
- Per-space/per-group branding for multi-community sites
- Safe input handling (CSS/logo sanitization, capability gating)
- Login/wp-admin footer + email branding parity with mature platforms (BuddyBoss white-labels login screen, admin footer, help text)

**What we have:** Site-wide brand service is real and wired: includes/WhiteLabel/BrandService.php (get_brand/save_brand/is_overridden, hue 0-360 clamp, font + custom-CSS sanitizers, 10k cap, 60-char brand-name cap). Front-end + admin injection via includes/WhiteLabel/HueOverride.php (wp_head+admin_head priority 1, emits <style id="bn-brand-override"> with --bn-hue, optional --bn-font-ui, plus sanitized custom CSS). Admin screen includes/Admin/WhiteLabelAdmin.php (12-swatch picker, brand-name/logo/font/CSS fields, admin-post save). REST preview includes/WhiteLabel/Controllers/PreviewController.php (POST /buddynext-pro/v1/admin/whitelabel/preview, manage_options). Backend relabeling includes/WhiteLabel/AdminLabelRewriter.php (admin_menu/all_plugins/admin_title/gettext, bails when no brand name). Per-space: BrandService get_space_brand/save_space_brand/space_is_overridden/can_manage_space_brand backed by bn_space_meta table (created in includes/Core/Installer.php maybe_alter_tables, line ~195); REST includes/WhiteLabel/Controllers/SpaceBrandController.php (GET/POST /spaces/{id}/brand); UI includes/Admin/SpaceBrandAdmin.php injects a "brand" tab via Free's buddynext_part_space_settings_tabs_args filter, panel dispatched at buddynext/templates/spaces/settings.php:642. Shared live-preview/REST-save JS assets/admin/whitelabel.js. All classes instantiated in includes/Core/Plugin.php lines 253-257, 334, 337. Per-space hue layering reads bn_resolved_space_id via HueOverride::current_space_id().

**Gaps:**
- **[high/both/confirmed-in-code]** Configured logo_url is stored and previewed in admin but NEVER rendered on the front-end. HueOverride::render() emits only --bn-hue, --bn-font-ui, and custom CSS; logo_url is in the config array but never output (grep for OPTION_LOGO/logo_url consumption finds only BrandService, the admin forms, and controllers - no front-end/header/login emission). So 'swap the logo' silently does nothing for visitors. _(includes/WhiteLabel/HueOverride.php (render() lines 109-129 omit logo); grep across includes/ shows no logo_url consumer outside BrandService.php/WhiteLabelAdmin.php/SpaceBrandAdmin.php/Controllers)_
- **[high/plan-vs-reality/confirmed-in-code]** Per-space Brand tab is hidden from the very space owners it targets. SpaceBrandAdmin::register_brand_tab adds the tab with cap => 'manage_options' (line 126), but the nav part buddynext/templates/parts/space-settings-tabs.php:95 hides any row failing current_user_can($cap). A space owner WITHOUT manage_options passes can_manage_space_brand (ownership) so the tab is registered, but the nav then suppresses the link, contradicting the roadmap's 'visible to space owner OR manage_options' (P6.2). _(includes/Admin/SpaceBrandAdmin.php:126 (cap 'manage_options'); buddynext/templates/parts/space-settings-tabs.php:95 (current_user_can gate))_
- **[medium/both/confirmed-in-code]** Duplicate branding surface on space settings. Free already ships a built-in 'branding' tab (slug 'branding', label 'Branding', icon 'image') whose panel explicitly invites Pro to inject controls via buddynext_part_space_settings_panel_branding_after. Pro instead registers a SEPARATE 'brand' tab, so a space-settings page shows two branding tabs (Free's upsell 'Branding' + Pro's 'Brand'). The intended seam in Free's branding panel is left unused. _(buddynext/templates/spaces/settings.php:375-379 (builtin 'branding' tab); buddynext/templates/parts/space-settings-panel-branding.php header (Pro should hook buddynext_part_space_settings_panel_branding_after); includes/Admin/SpaceBrandAdmin.php:121-127 registers a new 'brand' tab)_
- **[medium/expectation-vs-reality/confirmed-in-code]** No login-screen or admin-footer/email white-labeling. BuddyBoss-class white-label rebrands the wp-login.php logo/link, admin footer text, and notification email branding. Pro only rewrites admin menu/plugin-row/title/gettext strings and front-end CSS hue. No login_enqueue_scripts, login_headerurl, admin_footer_text, or email-template branding hooks exist in the module. _(includes/WhiteLabel/AdminLabelRewriter.php register() only hooks admin_menu/all_plugins/admin_title/gettext; no login_* or admin_footer_text hooks anywhere in includes/WhiteLabel/)_
- **[medium/expectation-vs-reality/needs-live-verification]** Per-space brand override only repaints when bn_hub === 'spaces' AND bn_resolved_space_id is set. Whether Free's PageRouter actually sets bn_resolved_space_id on every space sub-route (members, settings, individual content) is not verifiable from the Pro code alone; if some space routes don't set it, those pages fall back to site brand. Needs a live walk of space sub-pages to confirm the override paints everywhere within a space. _(includes/WhiteLabel/HueOverride.php:87-98 (current_space_id depends on bn_hub + bn_resolved_space_id query vars set by Free's router))_
- **[low/expectation-vs-reality/confirmed-in-code]** gettext rewrite of the literal 'BuddyNext' substring is broad and brittle: it str_replaces every occurrence inside any buddynext/buddynext-pro domain string (e.g. 'Connect with BuddyNext members'), and runs on every translated string render. Correct UX/perf for niche owners is fine, but it can produce odd phrasings and adds a gettext filter cost on all BN strings whenever a brand name is set. _(includes/WhiteLabel/AdminLabelRewriter.php:158-166 (rewrite_translatable_strings str_replace on every BN-domain string))_

### 🟡 Unlimited Webhooks (pro) — partial

**Headline:** Cap-removal half is genuinely shipped and wired; the promised Pro-event catalog wiring is missing, so Pro events fire but never leave the site.

**Plan source:** /Users/vapvarun/dev/repos/buddynext-pro/docs/PRO-ROADMAP.md (P13.1, lines 559-566); supporting contract in /Users/vapvarun/dev/repos/buddynext/docs/specs/features/FREE-PRO-CONTRACT.md (row 13, line 140)

**Plan promised:** P13.1 (status "SHIPPED") promised two things: (1) remove Free's hard cap of 1 outbound webhook endpoint by filtering buddynext_outbound_webhook_limit to PHP_INT_MAX, and (2) "Subscribe Pro hooks buddynext_pro_subscription_created, buddynext_pro_broadcast_dispatched, buddynext_pro_label_assigned, etc. into Free's outbound dispatcher so the full event catalog (Free + Pro) flows out." Admin UI: explicitly nothing new -- Pro reuses Free's existing webhooks admin page. The Free seam (apply_filters('buddynext_outbound_webhook_limit', 1)) was stated to already exist in Free.

**Owner expects:**
- Register more than one outbound webhook endpoint (the headline Pro promise)
- Each Pro-specific lifecycle event (subscription created, broadcast dispatched, member label assigned) actually delivers an outbound HTTP POST to subscribed endpoints, not just Free events
- Be able to pick which events an endpoint subscribes to from the admin UI, including Pro events
- HMAC-signed payloads, delivery logging, retry on failure, auto-deactivate dead endpoints -- all provided by Free's engine and inherited by Pro
- Parity with BuddyBoss/Zapier-style integrations: a usable catalog of events covering the platform's real activity, not a partial list

**What we have:** Only the cap-removal half shipped. /Users/vapvarun/dev/repos/buddynext-pro/includes/Outbound/UnlimitedWebhooksIntegration.php is a 3-line filter: register() adds buddynext_outbound_webhook_limit returning PHP_INT_MAX. It is wired at runtime via /Users/vapvarun/dev/repos/buddynext-pro/includes/Core/Plugin.php:201 (UnlimitedWebhooksIntegration::register()). Free's engine (/Users/vapvarun/dev/repos/buddynext/includes/Outbound/OutboundWebhookService.php) reads that filter at register() (line 105) and enforces the count, so the cap genuinely lifts -- confirmed in code, no live check needed. The full delivery engine (HMAC-SHA256 signing, bn_outbound_webhooks + bn_outbound_webhook_log tables, 5-min cron retry, 3-failure auto-deactivate) lives in Free and is inherited. The Free->dispatch() entry point is fed by /Users/vapvarun/dev/repos/buddynext/includes/Outbound/OutboundWebhookListener.php, which subscribes 14 Free domain events. Pro fires its three events (BroadcastService.php:379, Members/LabelAssignmentService.php:91, Membership/SubscriptionService.php:143) but NO Pro code subscribes them to the dispatcher: grep for add_action on those three actions in Pro returns nothing, and buddynext_service('webhooks')->dispatch() is never called anywhere in Pro (the only ->dispatch( hits are an unrelated AI EmbeddingIndexer method). There is no Pro Outbound listener class -- the Outbound dir contains only UnlimitedWebhooksIntegration.php.

**Gaps:**
- **[high/both/confirmed-in-code]** Roadmap P13.1 claims Pro events (subscription_created, broadcast_dispatched, label_assigned) are subscribed into Free's outbound dispatcher 'so the full event catalog (Free + Pro) flows out'. This is not implemented. Pro fires these as do_action but no add_action routes them to buddynext_service('webhooks')->dispatch(). The only Pro Outbound file is the cap-removal filter. So Pro lifecycle events never produce outbound webhook deliveries -- the second half of a 'SHIPPED' item is missing. _(includes/Outbound/UnlimitedWebhooksIntegration.php (only file; cap filter only); grep shows no add_action on buddynext_pro_* events and no ->dispatch() call in buddynext-pro/includes; events fired at includes/Email/BroadcastService.php:379, includes/Members/LabelAssignmentService.php:91, includes/Membership/SubscriptionService.php:143; roadmap claim at docs/PRO-ROADMAP.md:563)_
- **[medium/both/confirmed-in-code]** The admin webhook event-subscription UI offers a hardcoded catalogue of only 5 slugs (post.created, comment.created, user.followed, space.joined, notification.sent) and is not filterable. This both undercounts Free's own 14 listener events and gives Pro no way to expose subscription/broadcast/label events for selection. An owner cannot target an endpoint at a Pro event even if the dispatch were wired. _(/Users/vapvarun/dev/repos/buddynext/includes/Admin/Settings.php:1293-1299 (hardcoded $catalogue, no apply_filters); listener defines 14 slugs at /Users/vapvarun/dev/repos/buddynext/includes/Outbound/OutboundWebhookListener.php:31-44)_
- **[low/plan-vs-reality/confirmed-in-code]** P13.1 is marked 'SHIPPED' in the roadmap but only ~half (cap removal) is actually shipped; the event-catalog wiring is absent. Status label is inaccurate relative to code. _(docs/PRO-ROADMAP.md:561 ('Status: SHIPPED') vs absent dispatcher subscription in buddynext-pro/includes/Outbound/)_
- **[low/expectation-vs-reality/needs-live-verification]** Cap removal returns PHP_INT_MAX unconditionally with no license/entitlement gate in this class. Whether licensing is enforced upstream (only loading Pro on licensed sites) is not verifiable from this file alone. _(includes/Outbound/UnlimitedWebhooksIntegration.php:39-41 (unconditional PHP_INT_MAX); registration at includes/Core/Plugin.php:201 with no visible license check around it)_

### 🟢 Analytics (pro) — shipped

**Headline:** Analytics is shipped and wired end-to-end (collector, service, 6 REST endpoints, charted 4-view admin dashboard, member profile-views widget, indexed table) and exceeds the journey doc — but the doc is stale: it describes a Spaces/Content/Members text-table layout that was replaced by Overview/Cohorts/Funnel/Profile-Views charts, there is no Spaces admin tab, and logins are not actually recorded as events.

**Plan source:** docs/journeys/analytics-dashboard.md (dedicated journey doc); quality bar from buddynext/docs/MASTER_DEVELOPMENT_PLAN.md "The Standard We Are Building To".

**Plan promised:** The journey doc promised an event-collection layer (AnalyticsCollector hooking 25+ Free actions into wp_bn_analytics_events), an AnalyticsService computing DAU/WAU/MAU, top content, top members, space health, and member growth, an AnalyticsAdmin page with 4 tabs (Overview, Content, Members, Spaces) rendering text tables plus CSV export, and 6 REST endpoints (overview, content/top, members/top, spaces/{id}/health, me/profile-views, users/{id}/profile-views). The doc explicitly deferred visual charts ("Visual charts and graphs are deferred to the design phase. The current admin dashboard displays text tables only.").

**Owner expects:**
- Activity is captured automatically across the platform (posts, reactions, joins, follows, registrations) without manual instrumentation
- Site-level health metrics: active users (DAU/WAU/MAU), new-member growth over time
- Content insight: which posts/discussions drive engagement
- Member insight: who the most active members are
- Space/group insight: joins, leaves, posts, net membership
- Visual dashboard (charts, not just raw tables) reachable from wp-admin, matching BuddyBoss-style reporting
- Export to CSV for offline analysis
- Member-facing 'who viewed your profile' with a privacy opt-out
- Admin-only access to aggregate data; queries safe and performant on large event tables
- Data survives empty/zero state without SQL errors

**What we have:** Fully wired and shipped, exceeding the journey doc. Collection: includes/Analytics/AnalyticsCollector.php registers ~24 handlers on Free actions (post.created, reaction.added, comment.created, space.member_joined/left, user.followed, user.registered via user_register, moderation events, profile.viewed, search.performed, etc.) writing dotted-namespace event_types to wp_bn_analytics_events via record(). Wired in includes/Core/Plugin.php (AnalyticsCollector::register() at line 200). Queries: includes/Analytics/AnalyticsService.php provides count_events, dau/wau/mau, top_content (reaction.added+comment.created), top_members, space_health, member_growth, engagement_rate — all $wpdb->prepare'd. Two extra services beyond the doc: includes/Analytics/CohortService.php (weekly retention matrix) and includes/Analytics/FunnelService.php (activation funnel). REST: includes/Analytics/Controllers/AnalyticsController.php registers all 6 documented routes under buddynext-pro/v1, admin routes gated by require_admin (manage_options), profile-views by require_logged_in; registered in Plugin.php line 301. Admin UI: includes/Admin/AnalyticsAdmin.php renders an SVG-charted dashboard (LineChart/BarChart/DonutChart) with 4 views — Overview (6 stat tiles + daily activity line chart + top content + top members), Cohorts (retention heat matrix), Funnel (bar + table), Profile Views (line/bar/donut) — plus CSV export (admin_post bnpro_export_analytics, nonce-checked) for overview/content/members. Registered into AdminHub 'growth' tab. Member-facing: includes/Analytics/ProfileViewsWidget.php hooks Free's buddynext_part_profile_hero_after; includes/Analytics/ProfileViewService.php enforces bn_pro_hide_profile_views opt-out. DB: includes/Core/Installer.php creates wp_bn_analytics_events with 4 composite indexes (event_type+created_at, actor_id+created_at, target_type+target_id, created_at) — properly indexed for the query patterns.

**Gaps:**
- **[medium/plan-vs-reality/confirmed-in-code]** The journey doc describes the admin dashboard as 4 tabs (Overview, Content, Members, Spaces) of text tables, with CSV exports per tab and a dedicated Spaces tab showing per-space joins/leaves/posts/net. The shipped AnalyticsAdmin instead ships 4 different views (Overview, Cohorts, Funnel, Profile Views) with SVG charts. There is NO Spaces admin tab — space_health() is reachable only via REST, not via any admin UI screen. Content and Members are merged into Overview cards, not standalone tabs. _(includes/Admin/AnalyticsAdmin.php resolve_view() allowed=overview/cohorts/funnel/profile-views; render_content() switch has no 'spaces'/'content'/'members' case; docs/journeys/analytics-dashboard.md Parts 3-5 expect those tabs.)_
- **[medium/both/confirmed-in-code]** The journey doc (Part 1) instructs firing do_action('wp_login', ...) to generate a 'login' analytics event and expects event_type 'user_login' rows. AnalyticsCollector registers NO wp_login / user_login handler, so logins themselves are never recorded. DAU/WAU/MAU count distinct actor_id across ANY recorded event, not logins; a returning user who only reads (no post/reaction) is invisible to active-user metrics. Doc's documented event_type names (user_login, post_created, space_joined) also do not match the shipped dotted names (user.registered, post.created, space.member_joined). _(includes/Analytics/AnalyticsCollector.php register() — no add_action('wp_login') and no login handler; event_types are dotted (user.registered, space.member_joined). Contradicts docs/journeys/analytics-dashboard.md lines 23-40, 167-169.)_
- **[medium/expectation-vs-reality/needs-live-verification]** Engagement / DAU metrics depend entirely on Free firing the ~24 named action hooks with the exact signatures the collector expects. If any Free action is renamed/dropped, collection silently stops (no fallback, no error). on_notification_created already documents a Free signature mismatch (3 args vs spec's 4). Whether every hook actually fires at runtime in current Free cannot be proven from Pro code alone. _(includes/Analytics/AnalyticsCollector.php lines 33-78 (hook list) and lines 408-433 (notification arg-count note); requires Free source / live verification that each action fires.)_
- **[medium/expectation-vs-reality/needs-live-verification]** No data retention / pruning for wp_bn_analytics_events. Every reaction, impression, comment, search writes a row indefinitely; on a busy community the table grows unbounded with no cron purge. Master plan's 'production-ready enterprise' bar implies a retention story; none found in Analytics code. _(No pruning logic in includes/Analytics/*; AnalyticsCollector::record() only inserts. No scheduled-event grep hit within Analytics dir — needs confirmation that no cron prunes elsewhere.)_
- **[low/plan-vs-reality/confirmed-in-code]** Spaces-tab CSV export and a space-specific export are absent. handle_csv_export() only handles content/members and a default (member_growth) case; there is no 'spaces' branch. The doc itself flags this (lines 143, 212) as a known gap to file a TODO for. _(includes/Admin/AnalyticsAdmin.php handle_csv_export() switch cases: content, members, default only.)_
- **[low/expectation-vs-reality/needs-live-verification]** top_content is hardcoded to target_type 'post' engagement (reaction.added + comment.created) and the admin Top Content card resolves titles via get_the_title(); for BuddyNext activity/space posts that are not WP posts this may yield '#id' fallbacks. Whether target_id maps to a real WP post (vs an activity row) is not guaranteed by the schema. _(includes/Admin/AnalyticsAdmin.php resolve_post_title() uses get_the_title(); includes/Analytics/AnalyticsService.php top_content() filters target_type=%s with no join to a posts table — needs live check of what target_id references.)_
